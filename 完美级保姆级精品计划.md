# 万象 · 完美级保姆级精品计划

> 版本：Craft Polish Plan v1  
> 基线：`c674ffb` 之后的稳定 UI 架构  
> 范围：**只打磨既有功能，不新增产品能力，不扩 IA，不制造新入口。**  
> 角色：Principal Product Designer + Senior Interaction Designer + UI Systems Designer + Senior Frontend Architect + UI QA Lead  
> 核心目标：把“已经稳定、已经正确”的界面，打磨成一件**长期使用仍然安静、顺手、精准、有辨识度**的桌面工具。

---

# 0. 这轮到底要做什么

前一轮解决的是：

> **不要坏。**

这一轮解决的是：

> **不要普通。**

但“不要普通”不等于：

- 加渐变；
- 加玻璃；
- 加更大圆角；
- 加更多动画；
- 加更多按钮；
- 加更多信息；
- 做成另一套 SaaS 模板；
- 让截图比真实使用更重要。

这一轮要追求的是：

```text
稳定骨架
    ↓
精确比例
    ↓
信息节奏
    ↓
交互连续性
    ↓
视觉克制
    ↓
局部质感
    ↓
长时间使用舒适度
    ↓
形成产品独有气质
```

最终体验应该更接近：

> **一支做工很好的钢笔、一把经过调校的机械键盘、一张排版成熟的纸。**

而不是：

> “又一个 AI Chat App”。

---

# 1. 精品阶段的产品北极星

## 1.1 一句话气质

**安静的纸面工作台，精准的墨蓝操作层。**

万象当前已经具备非常值得保护的视觉基因：

- 暖纸 `canvas / rail`；
- 墨蓝单一强调；
- Sarasa Gothic SC 正文；
- Sarasa Term SC 代码；
- assistant 内容以正文为主，而不是全部套卡片；
- user message 才形成清晰气泡；
- reading column 有明确上限；
- code / tool / reasoning 才获得更强容器语义；
- 边框、阴影、surface 已较克制。

精品化必须**顺着这些基因继续精炼**，不做视觉转向。

## 1.2 用户真正应该感受到的五件事

### 第一感：清楚

打开应用 1 秒内知道：

- 当前在哪个会话；
- 当前是否正在生成；
- 下一步在哪里输入；
- 哪些东西是正文，哪些是系统状态。

### 第二感：安静

没有多余颜色、边框、卡片、动画、通知争抢注意力。

### 第三感：顺手

高频动作永远离手近：

- 切会话；
- 搜索；
- 输入；
- 发送 / 停止；
- 复制；
- 重试；
- 切模型。

### 第四感：可信

请求中、已提交、失败、断线、上传中等状态不会含糊。

### 第五感：耐看

连续读 1~3 小时长对话时，仍然像在读一份排版成熟的文档，而不是浏览一堆 UI 组件。

---

# 2. 本轮 Non-goals：明确不做什么

这份计划严格禁止通过“新增功能”制造完成感。

## 不新增产品能力

- 不加新的会话能力；
- 不加新的 AI 反馈体系；
- 不加新的编辑器能力；
- 不加新的侧栏模块；
- 不加新的 Dashboard；
- 不加新的工具市场；
- 不加新的快捷命令面板；
- 不加新的主题种类；
- 不加新的设置分类。

## 不做大规模视觉换肤

- 不改掉暖纸 + 墨蓝；
- 不把所有 surface 改成玻璃；
- 不追随 macOS / Linear / Notion / Arc 的外观复制；
- 不引入新的装饰性字体；
- 不把圆角扩大成“AI 产品默认 16~24px”；
- 不给所有容器加 shadow；
- 不给所有按钮加背景。

## 不做 Patch Architecture

- 不为单张截图加入局部 magic offset；
- 不为一个 breakpoint 写第二套页面；
- 不为一个 hover 状态改变几何尺寸；
- 不用 `overflow hidden` 遮挡不明来源的溢出；
- 不用越来越大的 ZIndex 解决 layer 问题。

---

# 3. 当前基础：哪些东西必须保护

这轮不是推倒重来，以下都是保护对象。

## 3.1 空间骨架

当前结构：

```text
Wide / Normal
┌──────── Sidebar ────────┬─ Splitter ┬──────── Chat ───────────────┐
│                         │            │ Header                       │
│ Search                  │            │                              │
│ Conversation List       │            │ Message Reading Surface      │
│                         │            │                              │
│ Status / Settings       │            │ Composer                     │
└─────────────────────────┴────────────┴──────────────────────────────┘

Compact
Navigation Surface  ⇄  Chat Surface
```

这个模型继续保留。

## 3.2 现有空间 Contract

- Sidebar preferred：284；
- Sidebar min/max：232 / 420；
- 阅读列：748；
- 主 Header：52；
- desktop icon action：30；
- compact primary action target：38；
- compact breakpoint：720；
- form 单列 breakpoint：440；
- detail first-stage max-height：360；
- Composer attachment draft max-height：104；
- Toast body max-height：160。

精品阶段可以**微调值**，但必须通过系统性对比实验，而不是凭局部截图随手改。

## 3.3 现有稳定性防线

以下能力不得回退：

- Overlay viewport clamp；
- Popup flip / shift；
- focus trap / restore；
- disabled semantic contract；
- ActionBorder / Toggle automation semantics；
- attachment draft atomicity；
- history prepend anchor preservation；
- long-title finite sizing；
- virtualized Sidebar；
- reduced-motion；
- 390 / 540 / 720 / 900 / 1440 viewport coverage；
- 125% / 150% scale QA；
- 真实 PWA 七态 QA。

---

# 4. 精品化的总设计策略

这一轮分成六个层级，不允许越级。

```text
Layer A  Typography & Rhythm
Layer B  Optical Geometry
Layer C  Interaction Continuity
Layer D  State Communication
Layer E  Material & Depth
Layer F  Motion & Delight
```

顺序非常重要。

如果 A/B 没做好，不允许先做 F。

---

# 5. Layer A：Typography & Rhythm —— 先把“读”做到极致

聊天产品 80% 的视觉面积最终是文字。

因此最值得投入工匠时间的不是 Button，而是阅读体验。

## 5.1 字体职责重新确认

保持：

- UI / 正文：Sarasa Gothic SC；
- Code / 参数 / ID：Sarasa Term SC。

不得为了“高级”引入第二套 display font。

## 5.2 建立真正的 Reading Rhythm Spec

不要继续让 Markdown 各 block 自己算一个近似 line height。

建议整理成明确的阅读语义：

| 内容 | 目标 | 原则 |
|---|---|---|
| 正文 | 长时间阅读 | 行距舒展但不散 |
| 列表 | 扫读 | 项间距小于段落间距 |
| Heading H1/H2/H3 | 层级 | 通过上下留白 + 字号，而非粗重 |
| Quote | 辅助语义 | 左缘 + muted，不另做 Card |
| Inline code | 词级差异 | 背景轻、字宽稳定 |
| Code block | 工具阅读 | 代码区域紧凑，header 弱化 |
| Table | 对比 | 行距比正文紧，表头不抢蓝色 |
| Tool JSON | 技术阅读 | mono + 稳定滚动 |

### 执行要求

对 `MarkdownRenderer` 做一次完整 rhythm audit：

- paragraph top/bottom spacing；
- heading before/after spacing；
- list nesting spacing；
- quote spacing；
- code block 与前后正文的距离；
- table 与前后正文的距离；
- math 与正文 baseline；
- inline link baseline；
- 中英文混排 baseline。

### 精品标准

一条包含：

```text
H2
正文
列表
代码
正文
表格
引用
公式
```

的长回答，截图时应该更像一篇经过排版的技术文章，而不是 7 种组件拼在一起。

## 5.3 控制行长

当前 `readingWidth = 748` 是正确方向。

精品阶段要验证的不是“748 好不好看”，而是：

- 中文普通段落平均一行字数；
- 中英混排；
- 14.5 / 16 / 18 pt 用户字号；
- 125% / 150% scaling；
- table/code 宽度例外。

### Decision

正文继续遵守 reading column。

### Exception

以下允许使用阅读列内更多宽度：

- 表格；
- 代码；
- 极长工具结果。

但它们仍不能突破 Chat viewport。

## 5.4 CJK 排版细节

重点验证：

- 中文标点不会孤立到下一行；
- 英文单词不会被粗暴 break-all；
- URL 可以 anywhere wrap；
- emoji 不造成 line-height 抬升；
- inline code 不造成上下 baseline 跳动；
- bold / italic 中文不产生宽度异常；
- mono 中文 fallback 不出现 tofu。

## 5.5 元信息降噪

时间、token usage、模型状态、附件大小属于第三层信息。

原则：

> **能读到，但不能先看到。**

检查所有 `fontMicro / textFaint` 是否真的处于第三层，而不是因为太淡导致“像坏了”。

---

# 6. Layer B：Optical Geometry —— 像素正确之后做“视觉正确”

几何稳定不代表视觉已经精确。

下一阶段要做 optical alignment。

## 6.1 建立 Optical Alignment Audit

逐个检查：

- icon 光学中心；
- 文字 baseline；
- 头像与正文首行；
- Chevron 与 caption；
- Switch knob；
- Search icon 与输入文字；
- Send icon；
- Sidebar pin / running dot；
- code language label 与 toolbar；
- error icon 与错误标题；
- Toast accent bar 与第一行文字。

### 禁止方法

不允许通过大量：

```text
Margin = -2
TranslateY = 1
```

逐个肉眼补偿。

### 推荐方法

优先修：

- icon viewBox / geometry；
- parent VerticalAlignment；
- line height；
- shared control height；
- baseline container。

## 6.2 统一交互控件垂直节奏

现有：

- 30 desktop icon action；
- 38 compact action；
- 32-ish text button；
- 44 Sidebar row。

精品化不要求全部同高。

要求的是形成**两个明确密度档**：

### Compact control rhythm

用于：

- message action；
- code toolbar；
- sidebar chrome；
- header utilities。

### Comfortable control rhythm

用于：

- Primary form action；
- destructive confirm；
- empty-state primary action；
- touch / compact layout。

## 6.3 边框审计

每一条边框必须回答：

> 没有它，用户会误解结构吗？

若答案是否，则删。

重点区域：

- Composer shell；
- attachment chip；
- code block；
- table；
- tool card；
- dialog；
- toast；
- settings field；
- Sidebar rail separator。

### 目标

同一 viewport 内，强边框不形成“网格化”。

## 6.4 圆角审计

当前 radius：4 / 6 / 9 / 14 / 20 / pill。

继续保留有限阶梯。

推荐语义：

```text
Xs     微小 technical
Sm     chip / compact affordance
Md     control / row
Lg     composer / content card
Xl     modal only
Pill   status / circular action only
```

检查是否存在语义越级，例如普通 row 使用 Xl。

---

# 7. Layer C：Interaction Continuity —— 所有动作都像同一个产品

真正高级的交互不是动画多，而是动作之间没有“断层”。

## 7.1 定义四种交互反馈

所有 action 必须落入以下之一：

### Immediate

状态本地即可确定：

- 展开 / 收起；
- menu 打开；
- 输入焦点；
- Sidebar 展开；
- toggle 本机偏好。

应立即反馈。

### Pending

等待服务器权威：

- 删除；
- 置顶；
- 归档；
- 会话设置提交。

不提前报成功。

### Streaming

- 模型生成；
- 工具执行。

不把整个页面进入 loading。

### Recoverable Failure

- provider error；
- upload failure；
- reconnect。

错误提示紧邻恢复动作。

## 7.2 Focus continuity

定义每个动作完成后的焦点归宿。

| 动作 | 完成后焦点 |
|---|---|
| 新建会话 | Composer input |
| 选择会话 | 保持导航上下文或进入阅读，不随机丢焦点 |
| Close Dialog | 返回 trigger |
| Close Popup | 返回 anchor |
| Rename commit | 返回 title action |
| Attachment picker | 返回 Composer |
| Remove attachment | 下一附件 remove / input |
| Settings close | 返回原设置入口 |
| Error retry | 不把焦点丢到页面顶端 |

### 精品验收

纯键盘完整跑一遍，不出现“我现在不知道焦点在哪”。

## 7.3 Pointer continuity

高频动作要避免状态变化后目标位置移动。

已经正确的：

- send ↔ stop 同位置。

继续检查：

- code copy 状态切 check 后 hit box 不变；
- reasoning 展开后 header 不移动；
- tool detail 展开后 header 仍在原位置；
- hover action 不推正文；
- Sidebar more 出现不压标题；
- validation 不横向改变 field geometry。

## 7.4 Discoverability ladder

操作按重要性分三级：

### Level 1 常驻

- 输入；
- send/stop；
- new conversation；
- search；
- settings；
- model selection。

### Level 2 低调常驻或上下文显现

- message copy；
- regenerate；
- attachment download；
- title edit affordance。

### Level 3 Overflow / Context

- archive；
- delete；
- export；
- fork variants。

不允许 Level 1 退化成 hover-only。

---

# 8. Layer D：State Communication —— 状态要“说人话”

精品体验往往死在状态文案。

## 8.1 状态词统一

建立统一词汇表：

```text
正在连接
已连接
连接已断开
正在重连

上传中
上传完成
上传失败

生成中
已停止
生成失败

正在保存
已保存
保存失败
```

避免同一状态不同界面写：

```text
处理中 / loading / 正在提交 / 等待中
```

## 8.2 Loading feedback 分级

不要“一律 spinner”。

### < 300ms

尽量不闪 loading。

### 300ms ~ 2s

局部状态反馈。

### > 2s

给用户解释正在做什么。

### 长时间 streaming

正文就是进度，不额外堆 loading chrome。

## 8.3 Error card 文案工艺

顺序固定：

```text
发生了什么
→ 用户现在能做什么
→ 是否可重试
→ 技术细节（按需）
```

禁止：

- 直接展示 exception 第一行作为标题；
- 大段红底；
- 同一个错误同时 error card + toast 重复出现。

## 8.4 Toast 使用纪律

Toast 只用于：

- 跨上下文成功确认；
- 短暂系统状态；
- 当前视图没有更适合的位置。

Toast 不用于：

- 表单校验；
- 需要用户阅读 20 秒的信息；
- 可恢复错误的唯一入口；
- 已经在正文中表达的错误。

---

# 9. Layer E：Material & Depth —— 纸感不是米黄色背景

当前视觉语言的优势是“纸感”，但纸感不能靠统一 `#F5F4ED` 完成。

## 9.1 Surface hierarchy 要能闭眼解释

### canvas

工作台最底层。

### rail

Sidebar，仅比 canvas 半阶。

### surface

输入、轻卡片、字段。

### surfaceRaised

真正浮起的菜单、dialog、toast。

如果某元素没有“浮起”的空间意义，不准使用 `surfaceRaised + shadow`。

## 9.2 Shadow budget

一屏最多三种 shadow 语义：

- Composer / input：whisper；
- Popup：浮起；
- Dialog：modal depth。

禁止再添加“card shadow”。

## 9.3 Accent budget

墨蓝承担：

- primary action；
- focus；
- link；
- active edge；
- user bubble；
- small active state。

不承担：

- 大面积 section background；
- table header；
- tool card background；
- decorative gradients。

## 9.4 Dark theme 不是反色版本

专项审计：

- rail 是否足够区别 canvas；
- textMuted 是否过亮；
- code block 是否与 canvas 黏在一起；
- accent 是否刺眼；
- selected 是否形成冷蓝块；
- user bubble 是否过亮；
- focus ring 是否清楚但不发光。

---

# 10. Layer F：Motion & Delight —— 最后才允许谈“秀场”

秀场感来自**动作完成得漂亮**，不是动画数量。

## 10.1 Motion ledger

全应用只允许三类 motion：

### Feedback motion

按下 / hover 的微反馈。

### Continuity motion

同一对象状态变化，例如：

- send → stop；
- chevron expand；
- popup appear。

### Progress motion

真正等待中的 spinner。

不允许：

- 进入页面时所有东西 stagger；
- 消息逐张飞入；
- Sidebar 滑来滑去影响阅读；
- hover 放大按钮；
- scroll-jacking；
- decorative breathing everywhere。

## 10.2 Reduced Motion

每个 motion 都必须先回答：

> reduceMotion=true 时最终状态是什么？

不能只“速度变快”。

应该：

- decorative transition → 直接最终态；
- spinner → 静态进度符号；
- expand → 直接展开；
- focus → 仍保留清晰 ring。

---

# 11. 区域精品计划 A：Main Shell

## 11.1 目标

让外壳完全退居幕后。

用户感受到的是：

```text
Navigation | Conversation
```

而不是：

```text
Sidebar Card | Divider | Content Card
```

## 11.2 Sidebar 与 Chat 的关系

当前 Sidebar 是 rail，Chat 是 Primary Surface。

继续保持。

### 打磨项

- 检查 rail 与 canvas 色差是否刚好“看得出，不觉得切块”；
- splitter hover/focus 是否可发现但平时隐形；
- Sidebar collapse 时 Chat 是否自然接管空间，不出现视觉跳切；
- 232 / 284 / 420 三档下信息密度是否都合理；
- rail separator 是否需要比普通 border 更弱。

## 11.3 Window edge

桌面应用最容易忽略的是窗口边缘。

验证：

- 内容不会贴系统窗口边框；
- scrollbar 不与窗口边缘形成黑线；
- dark theme 窗口边缘不会出现不属于 palette 的灰边。

---

# 12. 区域精品计划 B：Sidebar

Sidebar 是“工作记忆”，不是文件管理器。

## 12.1 信息层级

第一眼：

- 当前会话；
- 搜索；
- 新建。

第二眼：

- 最近会话；
- running / pinned 状态。

第三眼：

- preview；
- more menu；
- connection status。

## 12.2 Conversation row 工艺

每行检查：

- title baseline；
- preview baseline；
- pinned/running slot 是否预留；
- more button 出现是否压缩 title；
- selected 左缘与圆角交界是否干净；
- hover 与 selected 是否能同时成立；
- keyboard focus 与 selected 是否区分清楚；
- 44 高度是否在 12.5px 字号下视觉居中。

### 关键原则

running dot / pin 必须使用相同 icon slot。

否则状态切换会让标题左右漂。

## 12.3 Search

打磨：

- placeholder 文字浓度；
- search icon baseline；
- clear button 出现时输入区域宽度稳定；
- 搜索结果刷新不跳 scroll；
- `Down` 进入结果后第一项完全可见；
- Escape 清空的反馈明确。

## 12.4 Virtualized list

精品阶段继续验证：

- recycle 后 hover 不串行；
- recycle 后 selected 不串行；
- recycle 后 tooltip 内容正确；
- recycle 后 context menu 操作的是当前 item；
- 5000 / 20000 summaries memory 稳定；
- 从 search 切回全列表 scroll policy 明确。

---

# 13. 区域精品计划 C：Chat Header

Header 是整个工作台的“仪表条”，必须安静。

## 13.1 元素优先级

```text
Sidebar Toggle
→ Conversation Title
→ Generating State
→ Stop
→ Session Settings
→ Fork
```

空间不足时按反向优先级牺牲。

## 13.2 Title

精品要求：

- 正常状态没有“像按钮”的视觉；
- hover 只给微弱 affordance；
- F2 / Enter / click 进入编辑；
- 编辑框与静态标题占据完全相同空间；
- title → edit 不引起右侧 action 横移；
- Escape 取消；
- Enter 保存；
- save 后 focus 回 title。

## 13.3 Generating state

Desktop：文字 chip 可以存在，但视觉比 title 弱。

Compact：不占 title 的主空间。

检查 spinner 是否真的必要；如果 streaming 文本已经足够表达“正在生成”，chip 可以进一步降低存在感。

---

# 14. 区域精品计划 D：Message Reading Surface

这是整个产品的主秀场。

## 14.1 Assistant message

原则：

> **内容本身就是界面。**

不要加 assistant bubble。

重点打磨：

- avatar 与第一行；
- message spacing；
- markdown rhythm；
- footer；
- action row；
- streaming caret。

## 14.2 User message

用户消息需要明显区分，但不能变成大型墨蓝按钮。

检查：

- 最大宽度；
- padding；
- 长段落可读性；
- code / URL 在 bubble 内的对比；
- focus/selection 是否清晰；
- 连续多条 user messages 是否太重。

## 14.3 Message spacing

当前 `space6 = 24`。

精品阶段做三种内容对比：

- 两条短消息；
- 长回答后短追问；
- tool/reasoning 很多的回答。

不要因为“24 是 token”就假设它永远正确。

如果调整，应形成 `messageGap` component semantic，而不是直接换成另一个 space token。

## 14.4 Footer

时间 / usage / actions 不应形成第二条 toolbar。

验收：

- 默认阅读时几乎不抢眼；
- 找的时候能找到；
- hover/focus 不改变 message 高度；
- action keyboard 可达；
- 24h 时间格式跨语言不产生奇怪宽度。

---

# 15. 区域精品计划 E：Markdown

Markdown 是最值得做视觉样板间的区域。

## 15.1 Heading

不用重粗体制造层级。

靠：

- size；
- top rhythm；
- bottom rhythm；
- ink density。

H1/H2/H3 必须一眼可区分，但不能像网页营销标题。

## 15.2 Lists

验证：

- 1 层 / 3 层 / 8 层；
- 数字 9 → 10 → 100 时 marker gutter；
- 中文长段落；
- inline code；
- link；
- nested quote。

## 15.3 Code blocks

目标：专业 IDE 的“代码阅读区域”，而不是高对比黑卡。

打磨：

- code header 高度；
- language label；
- copy / wrap action；
- code padding；
- horizontal scrollbar；
- scrollbar 与 card radius；
- very long single line；
- 中文 fallback；
- diff additions/deletions。

### 精品动作

复制后 check 状态：

- 图标切换；
- 不移动；
- tooltip 改“已复制”；
- 1.5s 后恢复；
- reduce motion 不影响语义。

## 15.4 Table

目标：像文档表格，不像 DataGrid。

避免：

- 每格 border；
- 冷灰背景；
- 表头过粗；
- 横向撑破。

专项数据：

- 2 列；
- 8 列；
- 50 行；
- 超长 URL；
- emoji；
- 中文 / 英文混排。

## 15.5 Link

Link 的交互风格必须统一：

- accent；
- underline；
- pointer；
- keyboard；
- focus；
- tooltip 仅在必要时显示目标 URL。

不要出现 heading 里的 link 与 paragraph 里的 link 行为不同。

## 15.6 Remote image policy

保持隐私优先：默认不主动加载第三方 Markdown 图片。

视觉上要像“明确策略”，不是“图片坏了”：

- 使用 muted image indicator；
- 显示 alt；
- 显示来源域名；
- 不使用 broken image icon。

注意：这轮不新增“加载图片”功能。

---

# 16. 区域精品计划 F：Reasoning / Tool / Technical Detail

这三者必须看起来属于同一个“辅助技术层”。

## 16.1 共同语法

```text
Summary Header
  icon / title / state
  chevron

Optional Summary

Detail viewport
  max 360
  internal scroll
  展开全部 / 收回限高
```

不要三套组件三种语言。

## 16.2 Reasoning

视觉应该像 margin note，而不是正文第二篇文章。

保留左缘语义。

检查：

- streaming 展开；
- completed 自动折叠；
- duration；
- 360 高度；
- expand all；
- keyboard toggle。

## 16.3 Tool Call

工具是“模型做了什么”的审计信息。

第一层要回答：

- 调了什么；
- 在运行还是完成；
- 参数大致是什么。

完整 JSON 是第二层。

## 16.4 Error technical detail

与 Tool JSON 使用相同 mono/detail rhythm。

不要为 error 自己发明另一种展开模式。

---

# 17. 区域精品计划 G：Composer

Composer 是每天使用次数最多的组件。

这里每 2px 都值得认真。

## 17.1 Composer 的视觉角色

它应该像：

> **桌面上的一张干净输入纸。**

而不是：

> “一个大 Card 里面套 toolbar”。

## 17.2 Shell

检查：

- surface 与 canvas 对比；
- border 是否必要；
- soft shadow 是否过强；
- focus ring 是否与 shell shadow 叠成双圈；
- input multi-line growth 时圆角视觉是否稳定；
- 最大 240 高之后内部 TextBox scroll 是否自然。

## 17.3 Input

重点：

- placeholder 不是第三个标题；
- caret 与墨蓝统一；
- 第一行与 send icon optical alignment；
- 1 / 2 / 8 / 20 行输入；
- IME；
- 中文候选；
- selection；
- emoji；
- URL；
- 粘贴巨长文本。

## 17.4 Send / Stop

继续保持同位置。

打磨：

- send icon optical center；
- stop icon optical center；
- enabled/disabled 反差；
- uploading 时 tooltip；
- generating 时 Automation name；
- compact 38 target。

## 17.5 Model picker

模型 chip 是低频但高价值动作。

目标：

- 不抢输入正文；
- 当前模型读得出；
- 长模型名 ellipsis；
- hover/focus 可发现；
- picker popup 与 chip 左缘/右缘 optical 对齐。

## 17.6 Keyboard hint

Desktop hint 是第三层信息。

检查：

- 不与 model chip 争空间；
- compact 隐藏后 footer 不失衡；
- Enter mode 改变后文案即时同步。

---

# 18. 区域精品计划 H：Attachments

## 18.1 草稿附件

状态：

```text
uploading
ready
removed
failed
```

视觉必须保持宽高稳定。

### 检查

- spinner → file icon 不改变 chip 高度；
- “上传中” → size 不让 remove 横移过大；
- 长文件名；
- 同名文件；
- 10 / 30 个附件；
- mixed ready/uploading；
- scrollbar 出现后 Composer 宽度不变。

## 18.2 已发送附件

目标：像消息内容的一部分，不像按钮。

可点击下载时：

- hover 提示足够；
- keyboard focus 清晰；
- missing 状态不再可点击；
- file name 与 size 层级明确。

---

# 19. 区域精品计划 I：Settings

Settings 不是“配置后台”，是专业用户调校工具。

## 19.1 IA 不动，层级打磨

保持 Dedicated Surface。

不新增分类。

主要优化：

- section title rhythm；
- label / hint / field；
- two-column alignment；
- single-column fallback；
- save action；
- validation；
- focus continuity。

## 19.2 Field group

统一：

```text
Label
Input
Hint or Error
```

不要有的字段 hint 在上、有的在下。

## 19.3 Validation

错误出现时：

- 只扩张当前 field group；
- border 不变粗；
- 第一错 focus；
- error 色不过饱和；
- 修正输入后 error 能自然消失。

## 19.4 Provider editor

最重要的是“安全与准确”。

检查：

- API Key 掩码；
- endpoint URL；
- models 多行；
- probe feedback；
- preset → custom 之间的 visual continuity；
- 保存按钮 pending 状态。

## 19.5 Appearance

外观设置本身应该成为设计系统展示区。

重点：

- theme selector；
- font scale；
- reduce motion；
- toggles；
- 当前值立即同步；
- 不 rebuild section。

---

# 20. 区域精品计划 J：Dialog / Popup / Menu / Toast

Overlay 已稳定，精品阶段做“空间礼仪”。

## 20.1 Dialog

### Layout Contract

```text
Viewport inset
→ Dialog surface
  → stable content scroll
    → form/content
```

检查：

- 360×480；
- 390×844；
- 720×500；
- 900×620；
- 150% scale；
- keyboard only。

### 工艺

- modal shadow 不过黑；
- scrim 不让背景变“黑洞”；
- title/action spacing；
- footer button alignment；
- close / cancel 语义一致。

## 20.2 Popup / Menu

重点：

- anchor optical alignment；
- item height；
- selected check slot；
- icon slot；
- danger item；
- separator；
- 40 项内部 scroll；
- 右下角 flip 后仍像“来自 anchor”。

## 20.3 Toast

Toast 是非常容易显得廉价的组件。

目标：像系统提示，不像网页 snackbar。

检查：

- accent bar 是否太重；
- shadow；
- max width；
- 1 行 / 6 行；
- stack spacing；
- focus；
- pointer pause；
- keyboard pause；
- live announce；
- failure 读完时间。

---

# 21. Empty States：不要让空白像没做完

当前有四种 Chat empty state。

精品化重点不是换插画，而是：

- title 文案；
- hint 长度；
- CTA weight；
- vertical position；
- Logo / icon 比例；
- 在 620 高与 480 高窗口的视觉重心。

## 21.1 空态原则

空态只有一个问题：

> **现在下一步做什么？**

不要讲产品哲学过多。

## 21.2 不新增装饰插画

现有 Brand logo 足够。

避免为了“秀场”加入大插画破坏工具气质。

---

# 22. Microcopy 工艺

把全产品 UI 文案做一次统一编辑。

## 22.1 文案原则

- 短；
- 可行动；
- 不技术腔；
- 不重复 UI 已经表达的信息；
- 不责怪用户；
- 危险动作明确结果。

## 22.2 Tooltip

Tooltip 只补充：

- icon meaning；
- shortcut；
- disabled reason。

不要给有文字的 Button 再重复 tooltip。

## 22.3 Dangerous copy

确认对话框必须说结果：

```text
删除这条消息
这条消息会从会话中移除，后续生成不再看到它。
```

而不是：

```text
确定吗？
```

---

# 23. Iconography 工艺

现有图标系统继续使用，不引入第二个 icon family。

## 23.1 Audit

逐个检查：

- stroke thickness；
- 15px 下清晰度；
- optical bounding box；
- filled / outlined 是否混用；
- stop / send / fork / copy / trash 的重量。

## 23.2 Stateful icon

状态变化保持同一 optical footprint：

- send ↔ stop；
- copy ↔ check；
- chevron right ↔ down；
- pin / running dot slot。

---

# 24. Design Token 精品化

当前 Tokens 已经是强基础。

精品阶段不增加几十个 token，而是补**语义层**。

## 24.1 三层结构

```text
Primitive
space1 / space2 / fontSmall / radiusMd

Semantic
textPrimary / textSecondary / surfaceRaised / actionTarget

Component
messageGap / composerPadding / sidebarRowHeight / popupItemHeight
```

## 24.2 何时提升 component token

满足任一条件才提升：

- 同一语义出现 3 次以上；
- 改它时必须同步改多个文件；
- 属于几何不变量；
- QA 明确依赖它。

不满足就保留局部值。

## 24.3 推荐候选

在实测后决定是否建立：

```text
messageGap
sidebarRowHeight
composerInnerPadding
headerActionGap
popupItemHeight
fieldVerticalGap
readingParagraphGap
```

不是现在立刻全部创建。

---

# 25. Accessibility 精品化

稳定阶段已经解决“可用”。

精品阶段解决“自然”。

## 25.1 Keyboard path

完整跑：

```text
启动
→ 搜索
→ 选会话
→ 读消息
→ 复制
→ Composer
→ 切模型
→ 发送
→ 停止
→ Settings
→ 修改 toggle
→ 打开 Dialog
→ 关闭
```

要求：

- tab order 与视觉顺序一致；
- 不需要鼠标完成；
- focus 不被 Composer / sticky UI 遮挡；
- virtualized row 的方向键 scroll into view 后完全可见。

## 25.2 Automation language

动态状态同步：

- send → stop；
- expand → collapse；
- enable → disable；
- selected state；
- toggle state。

## 25.3 Contrast

特别审计 `textFaint`。

第三层信息可以弱，但不能因为“高级感”变成不可读。

---

# 26. Performance 精品化

性能不是最后优化，是手感的一部分。

## 26.1 Interaction budget

不先拍脑袋设置绝对 ms。

先记录 baseline，再执行：

```text
任何纯 UI polish 不允许典型交互 p95 退化 > 15%
```

重点：

- Sidebar search；
- Sidebar active switch；
- Conversation switch；
- stream delta render；
- Markdown long message render；
- Settings section switch；
- popup open；
- theme switch。

## 26.2 Streaming

继续保护 CardKey cache / LCP mounting strategy。

精品代码不得因为视觉动画导致：

- 每 token 重建历史；
- 每 token 重算昂贵 shadow；
- 每 token 重新 syntax highlight 已提交 code。

## 26.3 Virtualized Sidebar

新增 recycle stress：

```text
5000 items
scroll top → bottom → top
search → clear
active switch
theme switch
```

检查：

- realized count；
- stale visual state；
- event leak；
- tooltip leak。

---

# 27. Failure Review：精品阶段最容易引入的新 Bug

| Risk | Cause | Prevention |
|---|---|---|
| 为视觉对齐加入 magic offset | optical problem 被当 layout problem | 修 icon geometry / baseline |
| 为“质感”增加 shadow 导致黑边 | surface depth 语义失控 | shadow budget |
| 增加动画导致 streaming 卡顿 | UI thread motion + render 竞争 | motion ledger + perf gate |
| 调 spacing 后 history anchor 漂 | extent 变化未覆盖 | prepend regression |
| 改 row template 后 virtualization 串状态 | recycled container 残留 | recycle stress test |
| 提高信息密度后 focus 看不清 | hover/selected/focus 混在一起 | state matrix |
| 暗色“高级化”后 textFaint 不可读 | 对比度被审美压低 | contrast audit |
| Composer 美化后多行输入跳动 | shell/padding/line-height 改动 | 1/2/8/20 line matrix |
| Popup 做动画后 anchor 漂移 | transform 改 containing geometry | 只 opacity 或不动 geometry |
| Validation 美化后表单 reflow | error 加在错误层级 | field-local expansion |

---

# 28. 精品执行批次

不要全应用一起“调感觉”。

按以下批次执行，每批单独 review / screenshot / test。

---

## Batch 0 — Baseline Freeze

### 目标

建立不可争辩的 before。

### 输出

截图矩阵：

```text
1440×900 Light
1440×900 Dark
1240×800 Light
900×620 Light
900×620 Dark
720-1 × 700
720+1 × 700
540×600
390×844
900×620 @ 125%
900×620 @ 150%
```

七态：

```text
connected
new conversation
streaming
markdown
tool
error
settings
```

### Gate

- 当前测试全绿；
- 工作树 clean；
- baseline shots 固定目录；
- 记录关键控件 Bounds。

---

## Batch 1 — Typography & Reading Rhythm

### 文件

- `Design/Tokens.fs`
- `Controls/MarkdownRenderer.fs`
- `Views/MessageCard.fs`

### 只做

- 字级；
- 行高；
- paragraph/list/heading spacing；
- code/table/quote rhythm；
- meta hierarchy。

### 不做

- Composer；
- Sidebar；
- Overlay。

### Gate

病态 Markdown test + 视觉长文样本全部通过。

---

## Batch 2 — Sidebar Optical Polish

### 文件

- `Views/Sidebar.fs`
- `Design/Tokens.fs`（仅必要 component semantics）

### 只做

- row optical alignment；
- selected / hover / focus；
- icon slot；
- search；
- footer；
- splitter affordance。

### Gate

- virtualization stress；
- 232/284/420 width；
- 5000/20000 item；
- keyboard path。

---

## Batch 3 — Chat Header + Reading Surface

### 文件

- `Views/ChatView.fs`
- `Views/MessageCard.fs`

### 只做

- header baseline；
- title edit continuity；
- action hierarchy；
- message gap；
- footer/actions；
- streaming visual。

### Gate

- 300 char title；
- generating true/false；
- 540/720/900 width；
- no action movement。

---

## Batch 4 — Composer Craft Pass

### 文件

- `Views/Composer.fs`
- `Controls/Primitives.fs`

### 只做

- shell material；
- input baseline；
- send/stop；
- model chip；
- hint；
- attachments visual rhythm。

### Gate

```text
0/1/10/30 attachments
1/2/8/20 lines input
uploading/ready mixed
disabled
generating
compact
125/150%
```

---

## Batch 5 — Technical Content Pass

### 文件

- `Views/MessageCard.fs`
- `Controls/MarkdownRenderer.fs`

### 只做

- reasoning；
- tool call；
- error technical detail；
- code toolbar；
- expand-all consistency。

### Gate

大 JSON / 360 max-height / expand / collapse / keyboard / reduced motion。

---

## Batch 6 — Settings Craft Pass

### 文件

- `Views/SettingsView.fs`
- `Views/SettingsGeneral.fs`
- `Views/SettingsProviders.fs`
- `Views/SettingsTools.fs`

### 只做

- section rhythm；
- field rhythm；
- validation；
- buttons；
- nav selected/focus；
- compact form。

### Gate

- 2-col / 1-col；
- long labels；
- invalid data；
- keyboard only；
- appearance state stable。

---

## Batch 7 — Overlay & Feedback Pass

### 文件

- `Controls/OverlayHost.fs`
- `Controls/Menu.fs`
- `Views/Dialogs.fs`

### 只做

- surface depth；
- popup/menu item rhythm；
- toast；
- dialog footer；
- focus transition。

### Gate

四角 anchor / 40 item / long label / 360×480 / low-height / keyboard。

---

## Batch 8 — Light/Dark Optical Calibration

### 文件

- `Design/Palette.fs`

### 原则

只做**极小颜色校准**。

不重做 palette。

### 流程

每次只改一种 semantic：

```text
rail
borderSoft
textMuted
textFaint
selected
hover
userBubble
```

一次最多调整 1~2 个。

### Gate

同一截图 light/dark 对照，确认层级一致。

---

## Batch 9 — Motion Pass

### 文件

- `Design/MotionPolicy.fs`
- `Controls/Primitives.fs`
- 需要状态连续性的局部 component。

### 原则

先列 Motion Ledger，再写一行动画。

### Gate

- normal；
- reduced motion；
- 60Hz；
- streaming；
- CPU stress。

---

## Batch 10 — Final Craft QA

不是“再看看截图”。

执行完整矩阵。

---

# 29. 每批实施的固定模板

每一批都严格按以下格式执行。

## Step 1 — User Task

这一批影响的用户任务是什么？

## Step 2 — Current Evidence

截图 + Bounds + 当前代码。

## Step 3 — Decision

明确：

```text
Decision
Alternatives
Trade-offs
Recommendation
```

## Step 4 — Geometry Contract

列：

```text
width
height
min/max
grow/shrink
overflow
scroll owner
state invariants
```

## Step 5 — Failure Review

至少列 3 个 Risk → Cause → Prevention。

## Step 6 — Implementation

小范围修改。

## Step 7 — Focused Tests

先跑最小测试。

## Step 8 — Screenshot

至少 before / after 同 viewport。

## Step 9 — Staff Review

用“不是自己写的”心态挑问题。

## Step 10 — Full Gate

batch 完成才进下一批。

---

# 30. Screenshot Review 不再靠“感觉”

每张图按 12 项打分，每项 0 / 1 / 2。

| 项目 | 0 | 1 | 2 |
|---|---|---|---|
| Hierarchy | 混乱 | 可用 | 一眼清楚 |
| Alignment | 明显漂 | 基本齐 | 光学齐 |
| Spacing | 随机 | 基本一致 | 节奏成熟 |
| Typography | 像组件 | 可读 | 像排版作品 |
| Density | 过松/挤 | 可用 | 专业耐用 |
| Color | 抢眼 | 正常 | 克制有辨识度 |
| Border | 网格化 | 可接受 | 语义精准 |
| Depth | 扁/重 | 可接受 | 空间层次自然 |
| Focus | 不明显 | 能看 | 明确不打扰 |
| State | 含糊 | 能懂 | 连续可信 |
| Long data | 破 | 勉强 | 自然 |
| Product character | 模板感 | 有一点 | 明确是万象 |

### Batch 通过线

总分至少：

```text
22 / 24
```

且不能存在任何 0 分项。

这不是为了数学化审美，而是防止“某张图整体不错”掩盖局部明显瑕疵。

---

# 31. Interaction State Matrix

所有交互组件必须过以下矩阵。

```text
default
hover
pressed
keyboard focus
selected
selected + hover
selected + keyboard focus
disabled
loading
error
compact
dark
```

不一定每个组件都有全部状态。

但有的状态必须被显式设计。

---

# 32. 数据压力矩阵

## Conversation

```text
0
1
30
300
5000
20000
```

## Message count

```text
0
1
20
200
1000 history
```

## Text

```text
空
1 字
普通
80 中文
300 英文无空格
URL
emoji
中英混排
```

## Attachments

```text
0
1
3
10
30
uploading
ready
failed
duplicate sha
long file name
```

## Tool JSON

```text
20 chars
2 KB
50 KB
200 KB
```

## Markdown

```text
5000 code lines
200k single line
800-row table
300 math
deep quote
2000 paragraphs
```

---

# 33. Viewport / Scale 精品矩阵

每个重要 batch 最少跑：

```text
1440×900
1240×800
900×620
720×700
719×700
540×600
390×844
900×500
```

Scale：

```text
100%
125%
150%
```

Dark / Light 至少各跑关键状态。

---

# 34. “坏掉的中间状态”专项

精品 QA 不只看典型尺寸。

特别拖窗口验证：

```text
1440 → 390 连续缩
390 → 1440 连续放
720 breakpoint 来回跨
dialog 打开时 resize
popup 打开时 resize
streaming 时 resize
30 attachments 时 resize
settings form error 时 resize
```

重点看有没有 1~2 帧：

- overlap；
- black strip；
- stale width；
- wrong ZIndex；
- popup outside viewport；
- toolbar jump。

---

# 35. 长时间使用专项

专业工具不能只做 30 秒 demo。

至少做一次 60 分钟模拟：

```text
20 次切会话
20 次搜索
30 条发送
10 次 stop/retry
20 次 popup
10 次 settings
10 次 theme/font preference
大量 scroll
多次 upload/remove
```

观察：

- event leak；
- focus drift；
- stale hover；
- duplicated toast；
- memory growth；
- spinner timer leak；
- virtualization stale item；
- visual state 不恢复。

---

# 36. 精品阶段代码纪律

## 每个 PR / Commit 应回答

1. 这次提升的具体体验是什么？
2. 修改了哪个 semantic contract？
3. 有没有改变外部 geometry？
4. 有没有改变 keyboard path？
5. 有没有改变 screen-reader state？
6. 有没有影响 reduced-motion？
7. 有没有扩大 UI thread 工作？
8. 哪些 viewport 验证过？
9. before/after 证据是什么？
10. 为什么没有引入新 token / wrapper / card？

## Diff 纪律

若一个“视觉小优化”一次改 15 个文件：

> 停下来。

先确认是不是 design primitive / component architecture 不够清楚。

---

# 37. 视觉反模式红线

出现以下任一情况，直接退回设计判断阶段：

- 新增大面积渐变；
- 新增 glassmorphism；
- 新增重 shadow；
- 新增第二强调色；
- 普通 section 加 Card；
- hover 才能找到核心操作；
- 通过 border 变粗表达 focus；
- 为对齐加入多个负 margin；
- 为动画使用 transform 导致 popup geometry 改变；
- 为“高级”把 textFaint 调到看不见；
- 为简洁删除必要状态说明；
- 为统一把所有按钮做成同一高度；
- 为极简把 tool / error 信息藏到不可发现；
- 为截图去掉 keyboard focus；
- 为视觉去掉 scrollbar contract。

---

# 38. 精品阶段的“少即是多”删减清单

每一批都主动问能不能删：

- 一个 border；
- 一个 shadow；
- 一个 caption；
- 一个 icon；
- 一个 divider；
- 一个 tooltip；
- 一个 wrapper；
- 一个颜色；
- 一个 animation；
- 一个特殊-case margin。

如果删掉后层级仍成立，删掉通常就是升级。

---

# 39. 代码文件地图

## Design SSOT

```text
Design/Palette.fs
Design/Tokens.fs
Design/LayoutPolicy.fs
Design/MotionPolicy.fs
```

## Interaction primitives

```text
Controls/Primitives.fs
Controls/Menu.fs
Controls/OverlayHost.fs
```

## Reading

```text
Controls/MarkdownRenderer.fs
Views/MessageCard.fs
Views/ChatView.fs
```

## Workbench chrome

```text
Views/MainLayout.fs
Views/Sidebar.fs
Views/Composer.fs
```

## Settings

```text
Views/SettingsView.fs
Views/SettingsGeneral.fs
Views/SettingsProviders.fs
Views/SettingsTools.fs
Views/Dialogs.fs
```

## State / orchestration

```text
Model/UiControllers.fs
AppShell.fs
```

## Verification

```text
tests/Wanxiang.Tests/UiStabilityTests.fs
tests/Wanxiang.Tests/ScrollLayoutTests.fs
tests/Wanxiang.Tests/FocusRingTests.fs
tests/Wanxiang.Tests/PathologicalRenderTests.fs
tools/qa_shot.js
```

---

# 40. 完美级 Definition of Done

不是“我觉得漂亮”。

必须同时满足。

## Product

- 核心任务没有因视觉打磨增加点击；
- 高频动作比现在更清楚或至少不退化；
- 低频信息更安静。

## Interaction

- Pointer / keyboard / touch 路径一致；
- focus 连续；
- async 反馈可信；
- state transition 不跳。

## Visual

- 暖纸 / 墨蓝辨识度增强；
- 不出现新视觉噪音；
- typography rhythm 成熟；
- alignment 经得起放大看。

## Geometry

- 所有状态外部尺寸稳定；
- 无 overlap；
- 无横向 page scroll；
- 无 accidental black border；
- 无 double border；
- 无 popup clipping；
- 无 title/action squeeze failure。

## Robustness

- 真实数据矩阵通过；
- 390~1440 通过；
- 100/125/150 通过；
- low-height 通过；
- long-session 通过。

## Performance

- UI polish 无典型交互性能明显回退；
- streaming 不被 motion 破坏；
- virtualized Sidebar 仍真正 virtualized。

## Maintainability

- 没有新增 Patch Architecture；
- 没有新增无语义 token；
- 没有新增重复状态机；
- 组件职责仍清楚。

---

# 41. 最终的秀场到底是什么

我们不是要让用户打开万象时说：

> “哇，好多视觉效果。”

理想反应是：

> “这个东西怎么这么顺。”

再用半小时后：

> “看起来很舒服。”

再用一周后：

> “换别的客户端总觉得哪里不对。”

这才是“方寸之地变成秀场”。

秀的不是装饰。

秀的是：

- 一条线为什么在这里；
- 一个字为什么这么大；
- 一个 action 为什么不移动；
- 一个 popup 为什么永远不会被裁；
- 一个错误为什么刚好说够；
- 一条长回复为什么读起来不累；
- 一个键盘用户为什么永远知道焦点在哪；
- 一个 150% scale 用户为什么没有感觉自己是二等用户；
- 一个用了半年、5000 个会话的用户为什么仍然觉得它轻。

---

# 42. 推荐实际开工顺序

严格按这个顺序：

```text
0  Baseline Freeze
1  Typography & Reading Rhythm
2  Sidebar Optical Polish
3  Chat Header + Message Surface
4  Composer Craft Pass
5  Reasoning / Tool / Error Detail
6  Settings Craft Pass
7  Overlay / Menu / Toast
8  Light / Dark Calibration
9  Motion Pass
10 Full Craft QA
```

不要同时开三个视觉方向。

每一批做到 22/24 screenshot score、focused tests 全绿，再进下一批。

---

# 43. 开工时第一条指令

正式实施本计划时，第一步不要改代码。

先做：

```text
Batch 0 — Baseline Freeze
```

然后从：

```text
Batch 1 — Typography & Reading Rhythm
```

开始。

因为万象真正的主角不是 Sidebar、Button、Dialog。

是：

> **用户和模型共同写出来的那一页内容。**

把这一页做到极致，万象才真正有自己的气质。

---

# 最后一条原则

每次准备“加一点东西让它更精致”之前，先问：

> **能不能通过删掉一个东西，让它更精致？**

如果可以，优先删。

工匠精神不是把每个角落都填满。

工匠精神是：

> **每个留下来的东西，都有理由；每个理由，都经得起变化。**
