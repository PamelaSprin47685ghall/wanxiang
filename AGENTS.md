1. 用 F# 和 Avalonia 重写 kelivo, 采用 agent-framework 作为 Agent 层, UI 基本不变, 支持多平台.
2. 采用 C/S 架构, 每个实例对等, 既是 C 又是 S, 可以自己指挥自己, 或者自己的 C 指挥别人的 S, 跨平台兼容.
# SSOT — 万象（wanxiang）单一事实来源

> 本目录是「万象」项目的**单一事实来源（Single Source of Truth）**。
> 内容整理自 2026-08-01 的项目决策讨论（132 轮问答），完整保留全部决策、用户回答、第一性原理、自动判断规则与 200 项自动问答，不丢失任何信息。

## 项目硬约束（背景）

- 用 **F# + Avalonia** 重写，UI 基本保持不变；
- 使用 **Microsoft Agent Framework** 作为 Agent 层；
- 采用每个实例同时充当客户端（C）和服务端（S）的**对等 C/S 架构**，可以自己指挥自己，或者自己的 C 指挥别人的 S；
- 跨平台兼容。
- 正式产品名：**万象**；英文及技术标识：**wanxiang**。新项目的一方命名、UI、协议、配置项、日志和文档不得出现旧名称（kelivo / agent-framework / avalonia 仅作为第三方依赖或灵感来源出现）。

## 对话来源与整理方式

- 原始记录：`ChatGPT-项目决策讨论.md`（132 轮问答，2026-08-01 11:11–13:38，ChatGPT Exporter 导出）。
- 初始会话采用 grilling 方式：AI 逐题提问（一次一问）、给出推荐答案，用户逐题确认；事实类问题由 AI 查证（含 Agent Framework 官方文档/源码核对），决策类问题由用户拍板。
- 首轮附带 `repomix-output(109).xml` 项目材料作为背景；硬约束（F#、Avalonia、Agent Framework、对等 C/S）由该材料确认。
- 用户在第 100 个决策后指示："你已经提了100个问题了。你根据我所有的回答，归纳反推出我决策的第一性原理，然后自动回答自己剩余问题，仅当置信度不够时候再问。"——由此产生 12 条第一性原理、16 条判断规则、5 条提问条件（41–43 号文件）。
- 用户随后指示"自动问答100个"——由此产生第 101–200 项自动问答（44–54 号文件）。
- 本目录按主题重新组织；决策原文、用户原话、AI 建议与确认结论均完整保留。AI 的中间检索/思考过程（如"正在搜索网页"）属于过程噪音，未收录；凡引用过的关键外部来源链接保留在各主题文件内。

## 文档地图

### A. 决策记录（01–40，按主题分组，覆盖对话中的决策 1–100）

| 文件 | 主题 | 覆盖决策 |
|---|---|---|
| [01.md](01.md) | 平台范围与总体架构 | 1–2 |
| [02.md](02.md) | 存储模型：NDJSON + CQRS、唯一权威 | 3 |
| [03.md](03.md) | 写入确认与 flush | 4 |
| [04.md](04.md) | 提交原子性 | 5 |
| [05.md](05.md) | 事件日志组织：全局流、UTC 分文件、全局 id | 6–7 |
| [06.md](06.md) | 日志恢复：空洞、静默截尾、损坏定义 | 8–10 |
| [07.md](07.md) | 状态快照 | 11 |
| [08.md](08.md) | 事件格式版本管理 | 12 |
| [09.md](09.md) | 命令幂等：commandId / invocationId | 13–16 |
| [10.md](10.md) | 模型生成作为长任务、消息记账 | 17 |
| [11.md](11.md) | 消息模型与 Agent Framework 透明映射 | 18–20 |
| [12.md](12.md) | 会话归属与并行生成 | 21–22 |
| [13.md](13.md) | 排队消息 | 22–24 |
| [14.md](14.md) | 传输协议：muxed WebSocket、fire-and-forget | 25 |
| [15.md](15.md) | 断线重连与同步 | 26–27 |
| [16.md](16.md) | 观察（Observe）模型 | 28–30 |
| [17.md](17.md) | 发送顺序、游标与慢客户端 | 31–34 |
| [18.md](18.md) | 写权限、陈旧检测与重试 | 35–38 |
| [19.md](19.md) | 提交协调器与故障恢复 | 39–40 |
| [20.md](20.md) | 配置持久化与密钥管理 | 41–42 |
| [21.md](21.md) | 配置热更新与冲突 | 42–44 |
| [22.md](22.md) | 认证：配对与令牌 | 45–47 |
| [23.md](23.md) | 命名边界与 clean-room | 48 |
| [24.md](24.md) | 运行模式：单进程与三开关 | 49–50 |
| [25.md](25.md) | doctor 与 --fix | 51 |
| [26.md](26.md) | PWA 模式与凭据存储 | 52–53 |
| [27.md](27.md) | 令牌即身份与轮换 | 54 |
| [28.md](28.md) | 令牌提交、吊销与并发连接 | 55–59 |
| [29.md](29.md) | 模式开关优先级与默认组合 | 60–61 |
| [30.md](30.md) | 连接目标选择与自动配对 | 62–65 |
| [31.md](31.md) | TLS 与监听端点 | 66–67 |
| [32.md](32.md) | WebSocket 帧格式与版本协商 | 68–70 |
| [33.md](33.md) | 附件传输与内容寻址 | 71–72 |
| [34.md](34.md) | 附件清理与删除记账 | 73 |
| [35.md](35.md) | 编辑即 fork | 74–77 |
| [36.md](36.md) | fork 语义深化 | 78–82 |
| [37.md](37.md) | 会话生命周期 | 83–87 |
| [38.md](38.md) | 生成取消与 generationId | 88–92 |
| [39.md](39.md) | Tool 执行 | 93–94 |
| [40.md](40.md) | Tool 来源与 MCP | 95–100 |

### B. 原则与规则（41–43）

| 文件 | 主题 |
|---|---|
| [41.md](41.md) | 反推出的第一性原理（12 条） |
| [42.md](42.md) | 我将自动采用的后续判断规则（16 条） |
| [43.md](43.md) | 只有这些情况我还会提问（5 条） |

### C. 自动问答（44–54，第 101–200 项，含结论）

| 文件 | 主题 | 条目 |
|---|---|---|
| [44.md](44.md) | 一、进程、启动与关闭 | Q101–110 |
| [45.md](45.md) | 二、日志格式与恢复 | Q111–120 |
| [46.md](46.md) | 三、投影与查询 | Q121–130 |
| [47.md](47.md) | 四、Observe、快照与慢客户端 | Q131–140 |
| [48.md](48.md) | 五、命令、幂等与错误 | Q141–150 |
| [49.md](49.md) | 六、Provider 与生成 | Q151–160 |
| [50.md](50.md) | 七、Tool 与 MCP | Q161–170 |
| [51.md](51.md) | 八、附件 | Q171–180 |
| [52.md](52.md) | 九、配置、认证与配对 | Q181–190 |
| [53.md](53.md) | 十、PWA、UI、测试与交付 | Q191–200 |
| [54.md](54.md) | 自动问答结论与首版完成定义 | 汇总 |

> 编号 55–99 预留，供未来新增主题使用。本目录是唯一权威来源；与任何其他文档冲突时，以本目录为准。

## 全局术语（本 SSOT 内统一含义）

- **万象 / wanxiang**：正式产品名。
- **S / Server**：Linux x64 服务端，承担 Agent 执行、NDJSON、内存投影、TOML、WebSocket 端点。
- **C / Client**：客户端（Linux 桌面 Client 或 PWA），只通过 muxed WebSocket 连接 S。
- **NDJSON**：事件日志永久存储，一条完整行 = 一次原子提交。
- **CQRS**：命令写入 NDJSON，查询读取事件折叠后的内存投影。
- **单写者 / Commit Coordinator**：进程内唯一的永久提交组件，与 NDJSON 单写者是同一者。
- **id**：全局连续递增的无符号 64 位提交序号（从 1 开始，跨 UTC 日期文件不归零）。
- **TOML**：唯一配置文件，含明文密钥，配置的唯一权威来源。
- **clean-room**：万象独立实现，原项目仅作灵感来源。
# 01. 平台范围与总体架构

> 覆盖对话决策 1–2。会话时间：2026-08-01 11:11–11:14。

## 决策 1：首个可交付版本支持哪些平台？

**AI 建议：** 第一阶段只承诺 **Windows、macOS、Linux 桌面端**；架构和业务层保持可移植，但暂不承诺 Android、iOS 和浏览器端。原因是桌面端是 Avalonia 当前最稳妥的交付路径，而移动端涉及额外 workload、平台生命周期、后台生成、通知、密钥存储和控件兼容性。若一开始把移动端纳入验收范围，会显著放大重写风险，也会妨碍先验证数据模型、Agent 层和对等 C/S 架构。

**用户回答（11:13:09）：** "第一版本 Linux x64(C+S) & PWA(C) 即可"

**已确认（11:13:17）：**
- **Linux x64：完整节点，同时具备 C + S**
- **PWA：仅作为 C**
- 暂不纳入 Windows、macOS、Android、iOS
- 这与项目的对等 C/S 总体方向兼容，但 PWA 本身不承担服务端职责，属于受限客户端。

## 决策 2：PWA 客户端是否保持"纯前端"？

**AI 建议：是。** PWA 不内置 Agent Framework、不直接访问模型供应商，也不保存 API Key。它只通过 **HTTPS + WebSocket/SSE** 连接一个或多个 Linux S，由 Linux S 负责：

- Agent 执行与工具调用
- 模型供应商访问
- 会话持久化
- MCP 与搜索能力
- 凭据和权限管理
- 流式响应

这样能避免把密钥暴露在浏览器中，也不会要求 .NET Agent Framework 在浏览器环境运行。

**用户回答（11:14:30）：** "是的。PWA 是纯前端客户端"

**已确认（11:14:41）：** **PWA 只负责界面与网络通信，不运行 Agent Framework，不持有模型或服务端密钥。** Linux x64 节点承担 Agent、工具、存储和服务端职责。
# 02. 存储模型：NDJSON + CQRS、唯一权威

> 覆盖对话决策 3。会话时间：2026-08-01 11:17。

## 决策 3：会话数据由谁作为唯一权威来源？

**AI 建议：** 以 Linux S 为唯一权威数据源。PWA 可以在浏览器本地缓存会话列表、消息和界面设置，以提升加载速度；但缓存不应被视为完整数据库。所有消息创建、编辑、重新生成、版本选择和删除操作，都由 Linux S 确认并持久化后才算成功。首版建议：Linux S 使用 SQLite 保存完整数据；PWA 使用 IndexedDB 做可丢弃缓存；断网时可以查看已缓存内容；首版不支持离线发送、离线编辑及稍后自动合并。

**用户回答（11:17:39）：** "Linux S 使用 NDJson + CQRS 作为永久存储，内存放事件积分后的状态作为日常使用，严格单进程（多开则互相完全无关），PWA 端缓存先不做，除了要显示的内容外都不存。断网则报错即可。"

**已确认（11:17:51）首版存储与运行模型：**
- **Linux S：NDJSON 事件日志 + CQRS**
- 日常查询读取事件折叠后的**内存状态**
- 单个 S **严格单进程**
- 多开实例之间完全隔离，不共享数据、不协调写入
- PWA 不做持久缓存，只在运行内存中保留当前显示所需内容
- 网络断开立即报错，不提供离线能力或自动重试队列

> 注：这覆盖了原数据模型中的消息版本、版本选择和会话顺序等状态，但永久存储方案明确以 NDJSON 事件模型为准，而不是材料中建议的 SQLite。
# 03. 写入确认与 flush

> 覆盖对话决策 4。会话时间：2026-08-01 11:18。

## 决策 4：何时向客户端确认写操作成功？

**AI 建议：** 采用"先持久化事件，再修改内存状态，再响应成功"的严格顺序：

```
校验命令 → 生成事件 → 追加 NDJSON → flush + fsync → 投影到内存 → 返回成功
```

这样即使进程在任意位置崩溃：
- 响应成功的操作一定已经落盘；
- 已落盘但尚未更新内存的事件，会在重启重放时恢复；
- 不会出现 PWA 收到成功、服务器重启后操作却消失的情况。

代价是每次写入执行 `fsync` 会降低吞吐量，但聊天应用写入频率通常不高，首版更应优先保证语义清晰和数据可靠。

**用户回答（11:18:37）：** "flush 即可，假设我有 UPS"

**已确认（11:18:48）：** 写入成功条件为 **NDJSON 追加并完成用户态 `flush`**，不执行 `fsync`；接受数据仍可能停留在操作系统页缓存中的风险，并以 UPS 作为断电保障。
# 04. 提交原子性

> 覆盖对话决策 5。会话时间：2026-08-01 11:19。

## 决策 5：一个命令产生多个事件时，如何保证原子性？

**AI 建议：** 每个成功命令只追加一条 NDJSON"提交记录"，记录内包含该命令产生的完整事件数组。例如：

```json
{
  "commitId": "01J...",
  "commandId": "01J...",
  "timestamp": "2026-08-01T03:18:00Z",
  "events": [
    { "type": "MessageAdded", "data": {} },
    { "type": "ConversationUpdated", "data": {} }
  ]
}
```

不要把同一命令的多个事件分别写成多行，否则进程可能在两行之间退出，留下只完成一半的业务操作。单行提交记录还能让启动恢复规则保持简单：文件末尾若是不完整 JSON 行，直接截断或忽略该尾行；此前的完整行全部视为已提交。

内存投影应在该整条记录完成 `flush` 后一次性应用；若应用失败，则将进程视为状态损坏并终止，而不是继续提供服务。

**用户回答（11:19:17）：** "可以"

**已确认（11:19:28）：** **一条 NDJSON 记录就是一次原子提交，可包含多个领域事件**。完整行视为已提交；末尾不完整行在恢复时忽略或截断。

> 注：此决策中"投影失败即终止进程"的建议后来被决策 40（故障恢复优先级：截尾 → poison → 崩溃）修正，见 19.md。
# 05. 事件日志组织：全局流、UTC 分文件、全局 id

> 覆盖对话决策 6–7。会话时间：2026-08-01 11:19–11:20。

## 决策 6：事件日志采用全局流还是按会话分流？

**AI 建议：** 采用单一全局有序提交流，物理上允许分段文件。例如：

```text
data/
  events-000001.ndjson
  events-000002.ndjson
  events-000003.ndjson
```

每条提交记录携带全局递增的 `sequence`：

```json
{
  "sequence": 1842,
  "commitId": "01K...",
  "events": [
    {
      "type": "MessageAdded",
      "aggregateType": "Conversation",
      "aggregateId": "conv-123",
      "aggregateVersion": 17,
      "data": {}
    }
  ]
}
```

不建议每个会话或聚合根单独维护文件，原因是：一次命令可能同时影响会话、消息、统计或配置；单一全局顺序可以确定性重放；原子提交不会横跨多个文件；备份、迁移和故障恢复更简单；单进程条件下不需要为多写入者优化。分段只解决文件无限增长问题，不改变它在逻辑上是一条事件流。

**用户回答（11:19:58）：** "按照 UTC 每天分文件"

**已确认（11:20:08）：** 事件日志按 **UTC 自然日**分文件，例如：

```text
events/
  2026-08-01.ndjson
  2026-08-02.ndjson
```

提交记录归属哪个文件，以服务器生成该提交记录时的 UTC 日期为准。跨过 UTC 零点后，下一次提交直接写入新文件；单次提交始终只占一行，因此不会跨文件。整体仍是一条逻辑事件流，只是按日做物理切分。

## 决策 7：跨日期文件是否维持全局序号？

**AI 建议：** 维持实例级、跨文件连续递增的 `sequence`。例如前一天最后一条是 `sequence: 1842`，次日第一条就是 `1843`。启动时按文件名日期排序，逐行重放，并检查序号严格连续。这样可以检测：文件缺失；文件顺序错误；中间记录被删除；重复追加；人工合并或恢复造成的日志断层。日期文件只是存储分区，不应重置事件顺序。

**用户回答（11:20:47）：** "可以，叫做 id 吧"

**已确认（11:20:53）：** 实例级、跨 UTC 日期文件连续递增的全局序号命名为 **`id`**，换日不归零。建议其语义固定为：

```json
{
  "id": 1843,
  "timestamp": "2026-08-01T03:20:00.000Z",
  "events": []
}
```
# 06. 日志恢复：空洞、静默截尾、损坏定义

> 覆盖对话决策 8–10。会话时间：2026-08-01 11:21–11:23。

## 决策 8：`id` 是否允许出现空洞？

**AI 建议：不允许。** 每条成功提交占用一个连续的无符号 64 位整数：
- 第一条提交为 `id = 1`
- 下一条必须为 `id + 1`
- 只有完成整行写入并 `flush` 后，内存中的下一个 `id` 才递增
- 启动重放时，若发现重复、倒序或空洞，立即停止启动并报告日志损坏
- UTC 日期切换只更换文件，不影响 `id`

不要预先分配 `id`，否则命令校验失败或写入失败会制造无意义空洞。单进程模型下，也不需要 UUID、雪花 ID 或时间戳来解决并发分配问题。

**用户回答（11:21:47）：** "同意，而且一旦损坏，自动截尾修复，保留有效前缀，损坏后不读后面的即使是正确行"

**已确认（11:21:55）日志恢复规则：**
- 按 UTC 日期与行顺序读取日志。
- `id` 必须从 1 开始严格连续。
- 遇到首个不完整 JSON、无法解析的提交、错误 `id`、非法事件或投影失败时，立即停止读取。
- 自动将日志截断到最后一条有效提交的末尾。
- 损坏点之后的所有内容一律丢弃，即使后面存在格式正确且 `id` 连续的记录，也不再尝试恢复。
- 截断后，下一个成功提交使用"有效前缀最后一个 `id` + 1"。

这使恢复结果由唯一的最长有效前缀决定，避免扫描后续内容时猜测事务边界或拼接出未经证明的状态。

## 决策 9：截断前是否保留损坏后缀？

**AI 建议：保留。** 自动修复时，先将从损坏字节开始到所有后续日期文件的原始内容移动到独立隔离目录（例如 `corrupt/2026-08-01T032100Z/` 下的 `.tail` 文件），随后再截断正式日志。隔离内容永远不参与自动重放，只用于人工诊断或编写专门恢复工具。

**用户回答（11:22:23）：** "不，静默截尾"

**已确认（11:22:32）：** **发现损坏时静默截尾，不保留损坏后缀，也不生成隔离副本。** 恢复规则因此是：
- 保留首个损坏点之前的最长有效前缀；
- 截断当前损坏文件；
- 删除损坏点之后所有日期文件；
- 不读取、不尝试恢复任何后续记录；
- 从最后有效 `id + 1` 继续写入。

## 决策 10：哪些问题算"日志损坏"？

**AI 建议：** 只把以下情况视为损坏并触发截尾：
- JSON 无法解析；
- `id` 不连续；
- 提交记录缺少必要字段；
- 事件类型未知；
- 事件数据无法反序列化；
- 事件违反结构性不变量，导致无法安全投影。

但**不要**因为普通业务状态不再符合当前版本规则就截尾。例如某旧事件在新版业务规则下已不允许，不应因此销毁后续历史；应通过事件版本迁移或兼容投影处理。

**用户回答（11:22:49）：** "同意"

**已确认（11:23:01）：** **只有无法确定性重放的结构性错误才触发静默截尾**；旧事件与新业务规则不兼容时，必须通过兼容读取或迁移处理，不能当作日志损坏。项目仍以事件日志重建内存状态，并保留所需的消息版本、会话顺序等语义。
# 07. 状态快照

> 覆盖对话决策 11。会话时间：2026-08-01 11:25。

## 决策 11：是否引入状态快照？

**AI 建议：** 首版支持快照，但快照仅是可丢弃的启动加速缓存，不是永久事实来源。建议规则：
- NDJSON 事件日志始终是唯一权威来源。
- 定期把当前内存投影写成一个完整快照。
- 快照记录其已包含的最后提交 `id`。
- 启动时先加载最新有效快照，再从 `id + 1` 开始重放日志。
- 快照缺失、版本不兼容或校验失败时，直接删除或忽略，改为从 `id = 1` 全量重放。
- 快照写入采用临时文件后原子重命名，避免半写文件被误用。
- 日志截尾后，任何覆盖到被截掉 `id` 的快照都必须失效。

**用户回答（11:25:00）：** "无需快照，启动时 replay 全部 ndjson。之后可能会引入每周把前面的旧事件日志用快照取代的功能"

**已确认（11:25:10）首版启动与持久化策略：**
- **不使用快照**；
- 每次启动从 `id = 1` 开始，完整 replay 全部 UTC 日志；
- replay 完成并构建内存状态后才进入正常运行；
- "每周用快照替换旧事件日志"只作为未来的压缩功能，不进入首版设计；
- 未来即使引入压缩，快照将成为被替换历史的权威基线，而不只是缓存，因此需要单独设计迁移与校验协议。
# 08. 事件格式版本管理

> 覆盖对话决策 12。会话时间：2026-08-01 11:26。

## 决策 12：事件如何进行格式版本管理？

**AI 建议：** 每个事件拥有独立的整数 `version`，提交记录本身也拥有 `formatVersion`。

```json
{
  "id": 1843,
  "formatVersion": 1,
  "timestamp": "2026-08-01T03:25:00.000Z",
  "events": [
    {
      "type": "chat.message-added",
      "version": 1,
      "data": {}
    }
  ]
}
```

建议语义：
- `formatVersion` 管理整条提交记录外壳的结构；
- `type` 是稳定且永不改义的事件名称；
- `version` 只管理该事件 `data` 的结构；
- 读取旧事件时，通过纯函数逐级升级，如 `v1 → v2 → v3`，再交给当前投影；
- 已写入的 NDJSON 不做原地批量改写；
- 同一个 `type + version` 一旦发布，其字段含义永久固定；
- 业务含义发生根本变化时，创建新事件类型，而不是复用旧名称。

**用户回答（11:26:05）：** "可以"

**已确认（11:26:19）版本策略：**
- 提交外壳使用 `formatVersion`；
- 每个事件使用独立 `version`；
- 已发布的 `type + version` 语义永久固定；
- 旧事件通过逐级升级函数转换为当前结构；
- 不原地改写历史 NDJSON；
- 根本性语义变化创建新的事件类型。
# 09. 命令幂等：commandId / invocationId

> 覆盖对话决策 13–16。会话时间：2026-08-01 11:26–11:32。

## 决策 13：写命令是否需要幂等标识？

**AI 建议：需要。** 每个修改状态的请求都携带客户端生成的 `commandId`。Linux S 将它写入提交记录，并在内存中维护已执行命令索引：

```json
{
  "id": 1843,
  "commandId": "0198...",
  "formatVersion": 1,
  "timestamp": "2026-08-01T03:26:00.000Z",
  "events": []
}
```

同一 `commandId` 再次到达时：
- 不追加新事件；
- 不再次执行副作用；
- 返回第一次执行的相同业务结果，或至少返回原提交 `id`；
- 重启后通过 replay 恢复幂等索引。

这主要解决：S 已完成 `flush`，但响应在网络中丢失，PWA 不知道操作是否成功而重试。没有幂等键，就可能重复发送消息、重复创建会话或重复批准工具调用。

**用户回答（11:26:43）：** "可以"

**已确认（11:26:54）：** 所有写命令都必须携带唯一 `commandId`，Linux S 会将其写入提交记录，并通过完整 replay 恢复幂等索引。重复命令不会重复追加事件或执行副作用。

## 决策 14：`commandId` 的作用域与生成方式

**AI 建议：** 由客户端生成 UUIDv7，并在该 Linux S 实例的整个历史中全局唯一。具体语义：
- PWA、Linux 本地客户端、其他远程客户端都在发出命令前生成 `commandId`；
- 同一次逻辑操作的网络重试必须复用原 `commandId`；
- 用户主动再次执行相同操作，必须生成新 `commandId`；
- S 永久保留已执行 `commandId → commit id` 的映射；
- 两个不同客户端若意外使用同一个 `commandId`，第二个请求直接视为重复命令；
- UUIDv7 只用于命令幂等，不参与事件排序；事件顺序仍以连续整数 `id` 为准。

不建议由服务器生成，因为客户端在响应丢失后将无法证明重试的是同一命令。

**用户回答（11:27:07）：** "可以"

**已确认（11:27:17）：** `commandId` 由客户端生成，采用 UUIDv7；同一逻辑操作重试必须复用原值，新操作必须生成新值。事件提交顺序仍只由连续整数 `id` 决定。

## 决策 15（第一问）：相同 `commandId` 但命令内容不同，如何处理？

**AI 建议：将其视为协议冲突并拒绝，绝不能当作普通重复命令。** 每次提交额外记录命令类型与规范化内容摘要：

```json
{
  "id": 1843,
  "commandId": "0198...",
  "commandType": "chat.send-message",
  "commandHash": "sha256:...",
  "events": []
}
```

收到已存在的 `commandId` 时：
- `commandId` 与规范化内容哈希（`canonicalCommandHash`）均相同：视为安全重试，不执行新操作，返回原提交 `id`；
- 任一不同：返回 `409 CommandIdConflict`，不写日志；
- 哈希应基于命令的规范化业务内容，不包含发送时间、认证令牌等传输元数据。

否则客户端 Bug、UUID 被错误复用或恶意请求可能把两个不同操作混成一次幂等操作，产生难以诊断的数据丢失。

**用户回答（11:29:05）：** "commandId 不是随机取的，而是结合了 hash 功能"

**已确认（11:29:10）：** `commandId` 不是随机 UUID，而是**由命令内容确定性计算出的哈希标识**。因此同一逻辑命令只要规范化内容一致，就天然得到相同 `commandId`；内容变化则标识变化。

## 决策 15（第二问）：`commandId` 是否包含客户端身份？

**AI 建议：包含。** 建议计算方式：

```text
commandId =
  base64url(
    SHA-256(
      protocolVersion
      + clientId
      + commandType
      + canonicalPayload
    )
  )
```

其中：
- `canonicalPayload` 使用固定的 JSON 规范化规则，例如对象键排序、UTF-8、数字格式固定、不保留无意义空白；
- 不包含发送时间、访问令牌、网络地址等传输数据；
- `clientId` 是客户端安装或会话的稳定身份；
- `protocolVersion` 防止未来规范化算法变化时发生跨版本碰撞。

加入 `clientId` 很重要：否则两个客户端发送内容完全相同的消息时，会被误判为同一次命令。比如两个用户都发送"你好"，其业务操作应该分别执行，而不是因为内容相同被去重。

**用户回答（11:29:55）：** "同一逻辑命令如果要真的执行两次呢？需要把时间注入或者更优雅的方法"

**AI 修正建议（11:30:06）：** 不要把"当前时间"直接当作去重依据。时间戳会带来精度、时钟回拨和重试重算等问题，而且会把"命令内容"与"执行次数"混在一起。更优雅的做法是拆成两个概念：
- **命令内容**：`commandType + canonicalPayload`
- **本次执行身份**：客户端为每次"确实想执行一次"生成一个 `invocationId`

```text
commandId =
  Base64Url(
    SHA-256(
      "command:v1"
      + invocationId
      + commandType
      + canonicalPayload
    )
  )
```

语义如下：
- 网络重试：复用同一个 `invocationId`，因此得到相同 `commandId`。
- 用户再次执行完全相同的操作：生成新的 `invocationId`，因此得到新的 `commandId`。
- 载荷被错误修改但仍复用 `invocationId`：哈希结果不同，可以检测为客户端协议错误。
- 时间可以存在于 UUIDv7 形式的 `invocationId` 中，方便观察和排序，但不能依赖时间本身保证唯一性。

示例（两次发送相同文本）：
```text
第一次：
invocationId = A
payload = "你好"
commandId = hash(A + SendMessage + "你好")

重试第一次：
invocationId = A
payload = "你好"
commandId 相同，不重复执行

用户再次发送：
invocationId = B
payload = "你好"
commandId 不同，正常执行第二次
```

**AI 建议：** `invocationId` 使用客户端每次操作生成的 UUIDv7；`commandId` 是包含该 `invocationId` 和规范化命令内容的 SHA-256。

## 决策 15（第三问）：把"执行次数"交给独立的 `invocationId` 表达

**问题：** 是否同意把"执行次数"交给独立的 `invocationId` 表达，而不是直接向命令内容注入时间？

**用户回答（11:30:59）：** "可以"

**已确认（11:31:07）幂等模型：**
- `invocationId` 表示"这一次确实想执行"；
- 同一次网络重试复用原 `invocationId`；
- 再执行一次相同逻辑命令时生成新的 `invocationId`；
- `invocationId` 推荐使用 UUIDv7；
- `commandId = SHA-256(协议版本 + invocationId + 命令类型 + 规范化载荷)`；
- 时间只可作为 UUIDv7 的组成特征，不直接参与业务去重。

## 决策 16：Agent 内部触发的命令如何标识？

**AI 建议：** 每个内部命令也生成独立 `invocationId`，同时记录因果关系：

```json
{
  "invocationId": "内部命令自己的 UUIDv7",
  "causationId": "直接导致它发生的 commandId",
  "correlationId": "最初用户命令的 commandId"
}
```

这样：每一步都能独立幂等重试；`causationId` 能还原直接调用链；`correlationId` 能把一次用户操作产生的完整 Agent 流程串起来；不应让所有子操作共用最初的 `invocationId`，否则不同命令会被错误合并。Agent Framework 只是执行层，最终状态变化仍必须通过这些正式命令和事件进入 CQRS 日志。

**用户回答（11:31:58）：** "invocationId 可以，因果链似乎不重要"

**已确认（11:32:04）：**
- Agent 内部命令也使用独立 `invocationId`；
- 首版不记录 `causationId`、`correlationId`；
- 不维护完整因果链；
- 需要排查时，依靠会话、消息、任务等领域 ID 关联即可。
# 10. 模型生成作为长任务、消息记账

> 覆盖对话决策 17（两个子决策）。会话时间：2026-08-01 11:32–11:34。

## 决策 17（第一问）：模型生成是否作为长任务处理？

一次模型调用可能持续很久、流式输出，也可能超时或失败，不能把网络调用包含在 NDJSON 原子提交中。

**AI 建议：拆成多个短提交。**
1. 接收"发送消息"命令，持久化用户消息和生成任务：`UserMessageAdded`、`GenerationStarted`
2. 完成 `flush` 后立即返回任务 ID。
3. 在内存中执行模型调用，并把流式内容推送给客户端；中间 token 默认不写日志。
4. 模型结束后再提交：`AssistantMessageCompleted` 或 `GenerationFailed` 或 `GenerationCancelled`

这样进程中途退出时，重启 replay 会看到一个未完成的 `GenerationStarted`，可以明确将其标记为中断，而不会留下半条助手消息。

**用户回答（11:32:50）：** "流式过程不记账，完成后才记账。但是客户端要流式展示。"

**已确认（11:33:00）生成写入语义：**
- 模型输出的流式 token **不写入 NDJSON**；
- Linux S 通过 SSE 或 WebSocket 将临时增量实时推送给客户端；
- PWA 仅在内存中拼接并展示临时内容；
- 生成完整结束后，才把最终助手消息作为一次原子提交写入事件日志；
- replay 后只会出现完整助手消息，不会出现半截流式内容。

## 决策 17（第二问）：用户消息何时记账？

**AI 建议：** 用户消息先持久化，助手消息完成后再单独持久化。流程为：

```text
用户发送
→ flush 用户消息事件
→ 启动模型生成
→ 向客户端流式推送临时助手内容
→ 生成成功
→ flush 完整助手消息事件
```

若生成期间进程退出、模型失败或用户取消：
- 用户消息仍永久存在；
- 临时助手内容全部消失；
- 日志中没有半成品助手消息；
- 用户之后可以针对该用户消息重新生成。

不建议等模型完成后再把用户消息和助手消息一起记账，因为长时间生成期间，用户刚发送的消息也没有可靠落盘；进程退出后，连用户输入都会丢失。

**用户回答（11:33:56）：** "用户消息，助手消息，tool 消息，tool result 消息，都分别触发记账"

**已确认（11:34:08）：** 以下每一类消息都独立触发一次原子提交并分配新的连续 `id`：
- 用户消息；
- 助手消息；
- Tool 调用消息；
- Tool Result 消息。

流式增量仍然只是临时展示；只有形成完整消息后才提交。因此一次典型流程可能产生四次或更多独立记账：

```text
UserMessage
→ AssistantToolCall
→ ToolResult
→ AssistantMessage
```

每一步完成 `flush` 后，才进入内存状态并向客户端确认。这样可以完整保留 Agent 与工具交互历史。
# 11. 消息模型与 Agent Framework 透明映射

> 覆盖对话决策 18（三个子决策）、19（两个子决策）、20。会话时间：2026-08-01 11:35–11:42。

## 决策 18（第一问）：重启后如何处理"已有 Tool 调用，但没有 Tool Result"？

**AI 建议：** 默认不自动重新执行。原因是工具可能包含发邮件、写文件、调用远端服务等副作用。进程可能在工具实际成功后、Tool Result 提交前退出；自动重跑可能造成重复副作用。建议 replay 后将其识别为"执行结果未知"：保留已经记账的 Tool 调用消息；不伪造 Tool Result；UI 标记为"执行中断／结果未知"；用户显式选择重试时创建新的 `invocationId` 和新的 Tool 调用消息；将来只有明确声明为纯读取或具备外部幂等保障的工具，才可选择自动重试。

**用户回答（11:35:07）：** "只要是完整消息，包括思考消息，也单独记账。已有 Tool 调用，但没有 Tool Result，诸如此类问题，我们先透明化处理，上游 provider 会处理的。"

**已确认（11:35:14）：**
- **任何完整消息都单独记账**，包括：
  - 用户消息
  - 助手文本消息
  - 助手思考／推理消息
  - Tool Call 消息
  - Tool Result 消息
  - Provider 返回的其他完整消息类型
- 流式增量不记账，只在一条消息完整后提交。
- 对"有 Tool Call、无 Tool Result""有思考、无最终回答"等不完整业务序列，应用层**不修复、不补写、不自动重试、不额外标记异常**。
- replay 后忠实呈现已有消息序列，并将其原样继续交给上游 Provider；如何续接或处理由 Provider 决定。

这意味着日志只保证每条已提交消息自身完整，不保证一次 Agent 回合在业务上完整闭合。

## 决策 18（第二问）：不同消息类型是否使用统一消息结构？

**AI 建议：** 使用一个统一的 `Message` 结构，通过 `kind` 区分消息类型（`user` / `assistant` / `assistant.reasoning` / `assistant.tool-call` / `tool.result` / `system`），各类型专有数据放入有版本的 `content` 项中，而不是分别为每种消息建立完全不同的数据模型。

**用户回答（11:36:19）：** "不同消息类型是否使用统一消息结构，这个问题，我建议直接参考 agent framework 的现有 schema，我们只做到对 ndjson 的透明映射。"

**已确认（11:36:29）：** **不自定义统一 `Message` 模型**。消息结构以 Agent Framework 现有 schema 为准，NDJSON 层只负责透明持久化与 replay，不重新解释或归一化消息类型。

## 决策 18（第三问）：是否原样保存 Agent Framework 消息 JSON？

**AI 建议：原样保存。** 提交记录只增加事件日志外壳：

```json
{
  "id": 1843,
  "formatVersion": 1,
  "timestamp": "2026-08-01T03:36:00Z",
  "commandId": "...",
  "events": [
    {
      "type": "agent-message-recorded",
      "version": 1,
      "payload": {
        "...": "Agent Framework 原始消息 schema"
      }
    }
  ]
}
```

应用层不拆分 `role`、content block、reasoning、tool call 或 tool result，也不转成自有 DTO。读取时将 `payload` 按对应 Agent Framework schema 反序列化；PWA 所需展示数据由查询层临时投影，不改变永久记录。

**用户回答（11:36:54）：** "可以"

**已确认（11:37:03）：**
- Agent Framework 消息 JSON 原样写入事件 `payload`；
- NDJSON 层只增加提交、版本、命令等事件外壳；
- 不拆解、不重塑、不维护自有消息 DTO；
- replay 时重新交给 Agent Framework 反序列化；
- PWA 展示所需结构由查询层即时映射，不反向影响持久化格式。

## 决策 19（第一问）：Agent Framework 升级后，旧消息如何兼容？

**AI 建议：** 每条消息事件额外记录产生它的 Agent Framework schema 标识（`framework.name` + `schemaVersion`），但不修改原始 `payload`。不要仅记录 NuGet 包版本，因为包版本不一定等于消息 schema 版本。读取路径：原始 payload → 按记录的 schemaVersion 解码 → 必要时通过适配器转换 → 交给当前 Agent Framework。否则未来升级框架后，一旦其 JSON 字段、判别器或内容块结构改变，完整 replay 可能失败，并触发静默截尾，造成大量有效历史被错误删除。

**用户回答（11:37:42）：** "Agent Framework 自己没有现有机制吗？"

**AI 调研结论（11:37:44，含官方源码核对）：** 有，但它解决的是**框架对象的序列化与恢复**，不是一套明确公开的"长期稳定消息 schema 版本协议"。Agent Framework 已提供：
- `agent.SerializeSessionAsync(session)`：把整个 `AgentSession` 序列化为 `JsonElement`
- `agent.DeserializeSessionAsync(json)`：由同一个 Agent 恢复 session
- `ChatHistoryProvider`：作为本地消息历史的读取、追加集成点

官方文档也明确建议持久化完整 session，而不是只保存消息文本或历史 ID。当前 .NET 实现中，`ChatClientAgentSession.Serialize/Deserialize` 直接使用框架的 JSON 配置进行序列化；`AgentJsonUtilities` 又复用了 `Microsoft.Extensions.AI` 的类型解析器，所以 `ChatMessage`、工具调用、工具结果等多态内容由现有框架机制处理。

但没有看到：session JSON 内明确的 `schemaVersion`、官方的旧版本逐级升级器、对序列化 JSON 格式长期稳定的兼容承诺。且官方说明 session 与具体 Agent／Provider 相关，不保证换 Agent 配置或 Provider 后仍然兼容。

**AI 修正建议：** **首版不自行给 Agent Framework 消息增加 schemaVersion，也不自定义消息转换。使用框架现有序列化机制；NDJSON 只透明保存框架产出的 JSON。** 其中：
- 每条完整消息通过自定义 `ChatHistoryProvider` 捕获并追加到 NDJSON；
- 序列化必须使用 Agent Framework／Microsoft.Extensions.AI 的 JSON 配置，不能用裸 `JsonSerializerOptions.Default`；
- session 中非消息状态使用 `SerializeSessionAsync` / `DeserializeSessionAsync`；
- 我们自己的 NDJSON 提交外壳仍保留 `formatVersion`，因为这是我们负责的格式。

**用户回答（11:40:28）：** "首版完全依赖 Agent Framework 自身的 session／消息序列化，不额外维护 Framework schemaVersion；只有我们的 NDJSON 外壳维护 formatVersion。另外，需要决策是消息级别序列化，还是 session 级别序列化"

## 决策 19（第二问）：完全依赖 Agent Framework 自身序列化

**问题：** 是否同意：首版完全依赖 Agent Framework 自身的 session／消息序列化，不额外维护 Framework schemaVersion；只有我们的 NDJSON 外壳维护 `formatVersion`？

**用户回答（11:40:28，同上）：** 确认同意。

**已确认（11:40:30）：** 首版不额外维护 Agent Framework 的 schemaVersion；只有 NDJSON 提交外壳维护 `formatVersion`。

## 决策 20：选择消息级序列化

**AI 建议：首版采用消息级序列化，不持久化整个 `AgentSession`。** 永久状态的基本单位是 Agent Framework 给出的每个完整 `ChatMessage`。一次回调若包含多条消息，则保持原顺序，为每条消息分别生成一次 NDJSON 提交：

```text
完整 ChatMessage
→ Agent Framework 的 JSON 序列化
→ 单独 NDJSON 提交
→ flush
→ 更新内存投影
```

Agent Framework 的 `ChatHistoryProvider` 本来就是外部存取消息历史的扩展边界，负责保持消息顺序与元数据；框架会把已经完成的请求和响应消息交给它。当前实现传递的是消息集合，因此 Provider 可以按顺序拆成逐消息提交。

不选择 session 级的原因是：
- `AgentSession` 不只是消息列表，还可能包含 `StateBag`、记忆、服务端会话 ID，以及 Agent 或 Provider 的专有状态；
- session 与具体 Agent/Provider 绑定，官方明确不保证跨 Agent 配置复用；
- 每产生一条消息就追加一次完整 session，会不断重复此前全部历史。对于本地历史，这会使日志体积近似二次增长；
- session 快照会模糊"每条完整消息单独记账"的既定语义，也与未来才考虑的日志压缩快照重叠。

对应的首版约束是：**应用自己通过 `ChatHistoryProvider` 管理完整历史，启动时 replay 消息事件构建内存历史，再创建新的运行时 `AgentSession`。不依赖 Provider 服务端保存的 conversation/session ID。** Agent Framework 同时使用本地 `ChatHistoryProvider` 与服务端历史可能发生冲突，其当前实现也将这两种模式视为替代关系。

因此：
- **NDJSON 权威数据：逐条 Agent Framework `ChatMessage` JSON**
- **内存状态：replay 后的有序消息集合**
- **`AgentSession`：运行时对象，可随进程退出而丢弃**
- **未来周度压缩：可引入 session 或应用状态快照，但不属于首版**

**用户回答（11:41:59）：** "同意首版采用消息级序列化，并限定为应用托管消息历史、不依赖 Provider 服务端 session 状态"

**已确认（11:42:08）：**
- 首版采用**消息级序列化**；
- 应用通过 `ChatHistoryProvider` 托管消息历史；
- NDJSON 保存 Agent Framework 的完整消息 JSON；
- 启动时 replay 全部消息，恢复内存历史；
- `AgentSession` 仅为运行时对象，不作为永久状态；
- 不依赖上游 Provider 的服务端 session/conversation 状态。

**相关参考来源（对话中引用）：**
- [Session | Microsoft Learn](https://learn.microsoft.com/en-us/agent-framework/agents/conversations/session)
- [ChatClientAgentSession.cs | microsoft/agent-framework](https://github.com/microsoft/agent-framework/blob/main/dotnet/src/Microsoft.Agents.AI/ChatClient/ChatClientAgentSession.cs)
- [ChatHistoryProvider Class | Microsoft Learn](https://learn.microsoft.com/en-us/dotnet/api/microsoft.agents.ai.chathistoryprovider?view=agent-framework-dotnet-latest)
- [ChatClientAgent.cs | microsoft/agent-framework](https://github.com/microsoft/agent-framework/blob/main/dotnet/src/Microsoft.Agents.AI/ChatClient/ChatClientAgent.cs)
# 12. 会话归属与并行生成

> 覆盖对话决策 21、22（第一问）。会话时间：2026-08-01 11:42–11:44。

## 决策 21：会话归属信息放在哪里？

Agent Framework 的原始消息 JSON 不一定包含本应用稳定的会话标识，但 replay、查询和向 Provider 提供上下文时，必须知道消息属于哪段对话。

**AI 建议：** 在 NDJSON 事件外壳记录应用级 `conversationId`，不修改 Agent Framework 原始 payload：

```json
{
  "id": 1843,
  "formatVersion": 1,
  "conversationId": "019c...",
  "commandId": "...",
  "timestamp": "2026-08-01T03:42:00Z",
  "events": [
    {
      "type": "agent-message-recorded",
      "version": 1,
      "payload": {
        "...": "Agent Framework 原始消息 JSON"
      }
    }
  ]
}
```

建议语义：
- `conversationId` 是应用自己的稳定 UUIDv7；
- 创建会话时生成，终身不变；
- 每条消息提交必须明确归属一个 `conversationId`；
- Agent Framework 的 `payload` 保持原样；
- replay 时按 `conversationId` 建立各自的有序消息集合；
- Provider 或模型的内部会话 ID 不作为本应用主键。

**用户回答（11:42:40）：** "同意"

**已确认（11:42:50）：**
- `conversationId` 是应用级稳定 UUIDv7；
- 写在 NDJSON 事件外壳中；
- 每条消息必须明确归属一个会话；
- Agent Framework 的消息 `payload` 保持原样；
- Provider 内部会话标识不作为应用主键。

## 决策 22（第一问）：同一会话是否允许并行生成？

**AI 建议：** 同一 `conversationId` 严格串行；不同会话可以并行。具体规则：
- 一个会话同一时刻最多存在一个进行中的 Agent/Provider 运行；
- Tool Call、Tool Result、思考消息和最终助手消息都属于该次运行；
- 当前运行未结束时，新的"发送消息"命令不立即启动另一次生成；
- 用户可先取消当前运行，再发送新消息；
- 会话标题修改、归档等不影响上下文的命令可以独立处理；
- 不同 `conversationId` 的生成任务可以同时运行。

原因是允许同一会话并行运行会产生难以定义的问题：两个运行读取相同历史后分别追加结果，完成顺序又可能不同，最终上下文与客户端看到的流式顺序都不确定。

**用户回答（11:44:00）：** "新的“发送消息”命令总在下一次可插入点插入，例如 tool result 返回 llm 的时候，或者 idle 之后。排队。"

**已确认（11:44:09）：** 同一会话不是简单"运行中拒绝新消息"，而是采用**单会话队列 + 协作式插入**：
- 模型流式输出期间，新用户消息进入队列；
- 到达可插入点时，按顺序加入下一次 Provider 请求；
- 可插入点包括：
  - 当前会话处于 idle；
  - Tool Result 已产生、准备再次调用 LLM；
  - 其他控制权已返回应用层、尚未发起下一次 Provider 调用的边界；
- 不在一次正在进行的 Provider 请求内部强行修改上下文；
- Agent Framework 作为执行层，但调用循环需要由应用层掌握这些边界。
# 13. 排队消息

> 覆盖对话决策 22（第二问）、23、24。会话时间：2026-08-01 11:44–11:46。

## 决策 22（第二问）：排队消息何时持久化？

**AI 建议：** 收到时立即持久化，但区分"已排队"和"已插入"。推荐流程：

```text
收到完整用户消息
→ 写入并 flush「用户消息已排队」
→ 返回客户端：accepted / queued
→ 到达下一个可插入点
→ 写入「消息已插入」
→ 将消息加入下一次 Provider 请求
```

这样能同时满足：用户消息一收到就不会因进程退出而丢失；replay 后可以恢复尚未插入的消息队列；不会因为日志中的物理提交顺序，错误地把用户消息塞进 Tool Call 与 Tool Result 之间；Agent Framework 原始消息 JSON 仍保持透明，排队状态放在我们自己的事件外壳中。只在插入时才落盘虽然更简单，但运行期间收到的排队消息会在崩溃后消失。

**用户回答（11:45:06）：** "只在插入时才落盘即可，排队消息可以容忍丢失"

**已确认（11:45:16）：** 排队中的用户消息仅存在于内存，**只有到达可插入点时才写入 NDJSON 并 `flush`**。服务器崩溃或进程退出时，尚未插入的排队消息允许丢失；replay 不恢复队列。

## 决策 23：客户端如何确认排队消息？

**AI 建议：** 区分临时接收与正式记账两种状态：
- 收到请求并放入内存队列后，立即返回 `queued`，但这不代表持久化成功；
- PWA 将消息显示为"等待插入"；
- 到达插入点、消息写入并 `flush` 后，服务器推送 `committed` 及对应日志 `id`；
- 只有 `committed` 才算发送成功；
- 若服务器在此之前断开，PWA 显示发送失败，用户可以重新发送；
- 重试必须复用原 `invocationId`，避免原消息其实已提交但确认包丢失时重复记账。

**用户回答（11:46:31）：** "可以"

**已确认（11:46:40）客户端状态语义：**
- 入队后返回 `queued`，仅表示进入当前进程内存队列；
- 到达插入点后，消息写入 NDJSON 并 `flush`；
- 随后推送 `committed` 和日志 `id`；
- 只有 `committed` 才表示正式发送成功；
- 断线前未提交的排队消息允许丢失。

## 决策 24：一次插入点处理几条排队消息？

**AI 建议：** 到达插入点时，将当时已经排队的所有用户消息按服务器接收顺序一次性排空。处理顺序：

```text
队列：[消息 A, 消息 B, 消息 C]
→ 分别按 A、B、C 顺序写入并 flush
→ 分别发送 committed
→ 使用包含 A、B、C 的最新历史发起一次 Provider 请求
```

这样可以避免每条排队消息都额外触发一次 LLM 调用，同时保持每条完整消息独立记账。插入开始后新到达的消息不加入本批次，留到下一个可插入点，形成明确边界。

**用户回答（11:46:49）：** "同意"

**已确认（11:46:58）插入点批处理语义：**
- 到达插入点时，截取当时队列中的全部消息；
- 严格按服务器接收顺序 FIFO；
- 每条消息仍分别写入 NDJSON、分别 `flush`、分别获得日志 `id`；
- 本批全部提交后，只触发一次后续 Provider 调用；
- 批处理开始后新到达的消息留到下一个插入点。
# 14. 传输协议：muxed WebSocket、fire-and-forget

> 覆盖对话决策 25（三个子决策）。会话时间：2026-08-01 11:48–11:51。

## 决策 25（第一问）：PWA 与 Linux S 使用什么通信方式？

**AI 建议：** 首版采用普通 HTTP 命令接口 + SSE 事件流，不使用 WebSocket。建议职责：

```text
HTTP POST
- 发送或排队消息
- 取消生成
- Tool 审批
- 修改配置
- 其他写命令

HTTP GET
- 会话列表
- 历史消息
- 当前运行状态

SSE
- queued / committed
- 流式模型增量
- 完整消息提交通知
- Tool Call / Tool Result 通知
- 生成结束、失败或取消
```

理由：SSE 天然适合服务器到 PWA 的流式输出；写操作继续使用独立 HTTP 请求，幂等和错误码更清晰；浏览器自动重连、反向代理和调试都比 WebSocket 简单；不需要维持一套双向帧协议；Linux C 调用其他 Linux S 时也能复用同一套协议；Agent Framework 本身已有基于 Responses/SSE 的 hosting 路径，可借鉴其传输方式，但应用仍保留自己的排队与 CQRS 边界。

**用户回答（11:48:23）：** "考虑到未来支持服务器端推送，直接用 muxed websocket (一个连接处理一切)"

**已确认（11:48:28）首版通信方式：**
- PWA 与 Linux S 之间使用**单条复用 WebSocket 连接**；
- 查询、命令、流式输出、状态通知、服务端主动推送都走同一连接；
- 不再区分 HTTP 命令通道与 SSE 推送通道；
- Linux C 调用远端 Linux S 时也应复用同一协议模型；
- 连接只是传输层，NDJSON/CQRS、`queued → committed`、消息级记账等语义保持不变。

## 决策 25（第二问）：复用协议采用请求 ID，还是逻辑子流 ID？

**AI 建议：** 同时保留 `requestId` 和 `streamId`，但职责严格分开：

```json
{
  "type": "command",
  "requestId": "019c...",
  "streamId": "conversation:019b...",
  "payload": {}
}
```

- `requestId`：标识一次请求及其直接响应，用于错误、确认和超时匹配；
- `streamId`：标识一个长生命周期逻辑通道，例如某个会话、生成过程或服务器通知流；
- 同一个 `streamId` 上可以连续出现 `queued`、`committed`、流式 delta、完整消息、tool 消息、完成或失败；
- 多个会话可在一条 WebSocket 上并发交错传输；
- 每个帧还应有 `type`，不要依赖字段组合猜测帧种类。

只使用 `requestId` 会让长时间生成和服务端主动推送很别扭；只使用 `streamId` 又不利于精确回应单次命令。

**用户回答（11:50:42）：** "这两个都不用，尝试全部设计成 fire-forget 协议"

**已确认（11:50:55）：** muxed WebSocket 不使用 `requestId` 或 `streamId`，整体按 **fire-and-forget 事件协议**设计。所有帧都是独立事件，不存在"请求/响应"概念；关联完全依赖已有业务标识：

```json
{
  "type": "chat.user-message.enqueue",
  "conversationId": "019c...",
  "invocationId": "019c...",
  "commandId": "sha256:...",
  "payload": {}
}
```

服务器之后可异步发送：

```json
{ "type": "chat.user-message.queued", "conversationId": "019c...", "invocationId": "019c..." }
```

```json
{ "type": "chat.user-message.committed", "conversationId": "019c...", "invocationId": "019c...", "id": 1843 }
```

流式 delta、完整消息、Tool Call、Tool Result、错误、服务器主动通知也都是同级事件。客户端不等待某个 transport response；它只根据 `type` 和领域 ID 更新本地状态。这样仍能在单连接中交错处理多个会话，同时避免重新引入隐性的 RPC 模型。

## 决策 25（第三问）：是否采用完全对称的事件协议？

**AI 建议：是。** C→S 与 S→C 使用同一个事件外壳和事件分发机制，不区分 command frame、response frame、notification frame；区别仅由 `type` 的命名和处理权限决定。

**用户回答（11:51:21）：** "是的"

**已确认（11:51:29）：** WebSocket 两端采用**完全对称的 fire-and-forget 事件协议**。
- 不存在请求、响应、通知三类传输概念；
- 所有消息都是独立事件；
- C→S 与 S→C 使用同一事件外壳和同一分发机制；
- 状态关联只依赖 `conversationId`、`invocationId`、`commandId` 及其他领域标识；
- 哪些事件允许由哪一端发送，由权限和事件类型规则决定；
- 一条连接可交错承载多个会话、流式增量和服务器主动推送。
# 15. 断线重连与同步

> 覆盖对话决策 26、27。会话时间：2026-08-01 11:51–11:52。

## 决策 26：WebSocket 断线后是否补发服务器事件？

**AI 建议：** 首版不提供传输层补发。WebSocket 只负责在线实时投递：
- 流式 delta 丢失后不补发；
- `queued` 属于临时状态，不补发；
- 已经写入 NDJSON 的完整消息和 `committed` 状态，可以在重连后通过一次"同步当前状态"事件重新获得；
- 服务端不保存每条连接的发送游标，也不维护 transport event 日志；
- 重连后的客户端应主动声明自己当前关注的会话，服务器随后推送这些会话的当前权威状态。

也就是说，**补偿依靠 CQRS 查询投影，而不是重放 WebSocket 帧**。否则实际上还要设计第二套带序号、确认、保留期限和重放游标的事件日志。

**用户回答（11:51:55）：** "同意"

**已确认（11:52:04）：**
- WebSocket 帧不做逐帧补发；
- 流式 delta 与临时 `queued` 状态断线即丢失；
- 服务端不维护连接级游标或传输事件日志；
- 重连后通过 CQRS 内存投影重新同步当前权威状态；
- NDJSON 仍是唯一永久事实来源。

## 决策 27：重连同步采用全量还是按需？

**AI 建议：** 首版采用按需同步，不在连接建立后推送全部会话和全部消息。客户端重连后发送独立事件，声明当前需要的视图：

```json
{ "type": "conversation.observe", "conversationId": "..." }
```

服务端随后推送：

```json
{
  "type": "conversation.snapshot",
  "conversationId": "...",
  "payload": {
    "messages": [],
    "runtimeState": "idle"
  }
}
```

会话列表单独通过：

```json
{ "type": "conversation-list.observe" }
```

同步原则：
- snapshot 是当前投影，不是 WebSocket 历史重放；
- 当前打开的会话同步完整消息；
- 会话列表只同步摘要，不携带全部消息；
- 客户端切换会话时再观察目标会话；
- 服务端可在被观察的会话发生变化时主动推送新 snapshot 或增量事件；
- PWA 不保存持久缓存，因此不需要"从客户端已有版本继续同步"的复杂协议。

**用户回答（11:52:30）：** "同意"

**已确认（11:52:36）：**
- PWA 通过显式 `observe` 事件按需订阅视图；
- 会话列表只同步摘要；
- 当前打开的会话同步完整消息与运行状态；
- snapshot 是当前 CQRS 投影，不是 WebSocket 历史重放；
- PWA 不保存持久缓存，因此不需要增量游标或客户端版本协商。
# 16. 观察（Observe）模型

> 覆盖对话决策 28、29、30。会话时间：2026-08-01 11:52–11:54。

## 决策 28：观察关系是否绑定 WebSocket 连接？

**AI 建议：** 绑定当前连接，并在断线时自动全部释放。语义如下：
- `conversation.observe` 将会话加入该连接的观察集合；
- `conversation.unobserve` 主动取消；
- WebSocket 断开后，服务端立即清除该连接的全部观察关系；
- 重连后客户端必须重新发送 `observe`；
- 不把观察关系写入 NDJSON；
- 不按用户或设备跨连接保存订阅；
- 同一连接重复 `observe` 应幂等，只重新推送一次最新 snapshot；
- 一个会话可以同时被多个连接观察，提交后向所有观察者推送。

观察关系属于临时传输状态，不属于业务事实；跨连接保存会引入无效订阅、离线消息积压和设备状态管理。

**用户回答（11:52:48）：** "同意"

**已确认（11:52:58）：**
- `observe` 关系仅绑定当前 WebSocket 连接；
- 断线后自动释放；
- 重连后客户端必须重新订阅；
- 观察关系不写入 NDJSON；
- 同一会话可被多个连接同时观察。

## 决策 29：观察期间推送全量快照还是增量更新？

**AI 建议：** 首次 `observe` 推送完整 snapshot，之后只推送增量事件。每个权威增量都携带对应的全局日志 `id`：

```json
{
  "type": "conversation.message-committed",
  "conversationId": "...",
  "id": 1843,
  "payload": {
    "...": "Agent Framework message JSON"
  }
}
```

建议规则：
- 首次观察：推送完整会话 snapshot，并附带当前最新 `id`；
- 持续观察：只推送新提交的完整消息、删除、重命名等增量；
- 流式 delta 没有日志 `id`，属于临时展示；
- 客户端若发现权威事件 `id` 与预期不符，不尝试补帧，重新发送 `observe` 获取完整 snapshot；
- 不在每次变化后重复推送整个会话，避免长对话产生大量重复数据。

**用户回答（11:53:26）：** "同意"

**已确认（11:53:38）：**
- 首次 `observe` 返回完整 snapshot；
- 后续只推送权威增量；
- 流式 delta 属于临时状态，不带永久日志 `id`；
- 客户端检测到状态不连续时，不补帧，重新 `observe` 获取完整 snapshot。

## 决策 30：如何判断会话增量是否连续？

这里不能直接要求全局日志 `id` 连续。全局 `id` 可能是：

```text
1843：会话 A 消息
1844：配置修改
1845：会话 B 消息
1846：会话 A 消息
```

观察会话 A 的客户端正常只会收到 `1843` 和 `1846`，中间的空洞并不代表丢帧。

**AI 建议：** 为每个会话投影维护连续递增的 `revision`：

```json
{
  "type": "conversation.snapshot",
  "conversationId": "...",
  "revision": 27,
  "lastCommitId": 1843,
  "payload": {}
}
```

```json
{
  "type": "conversation.message-committed",
  "conversationId": "...",
  "previousRevision": 27,
  "revision": 28,
  "commitId": 1846,
  "payload": {}
}
```

语义：`commitId` 对应 NDJSON 的全局 `id`，用于定位永久事实；`revision` 只表示该会话投影的变化次数；客户端当前是 `27`，只接受 `previousRevision = 27` 的增量；不匹配时丢弃该增量并重新 `observe`；`revision` 可由 replay 自然重建，不必成为独立事件字段；流式临时事件不推进 `revision`。

**用户回答（11:54:42）：** "乐观相信会话增量连续，除非被其他业务逻辑顺便证明错误，否则就相信它就行了。"

**已确认（11:54:52）：**
- 不引入会话级 `revision`；
- 不主动检测增量是否缺失或乱序；
- 客户端乐观相信当前 WebSocket 上收到的增量连续、有效；
- 只有其他业务逻辑明确发现状态矛盾时，才重新 `observe` 获取完整 snapshot；
- 已持久化事件仍可携带全局 NDJSON `id`，但客户端不据此进行连续性校验。
# 17. 发送顺序、游标与慢客户端

> 覆盖对话决策 31、32（两个子决策）、33、34。会话时间：2026-08-01 11:55–12:00。

## 决策 31：同一连接上的发送顺序如何保证？

**AI 建议：** 每条 WebSocket 连接只允许一个逻辑发送者，通过单一 FIFO 发送队列写 socket。生成 delta、消息提交、Tool 结果和主动推送等生产者都只能向队列投递，不得并发直接调用 WebSocket send。这样利用 WebSocket 自身的有序传输，保证服务器决定的事件顺序与客户端接收顺序一致，也不需要额外序号。

**用户回答（11:55:13）：** "同意"

**已确认（11:55:18）：** 每条 WebSocket 连接采用**单写者 FIFO 发送队列**，所有事件生产者只向队列投递，禁止并发直接写 socket。连接内顺序由服务器单点确定，不再额外引入传输序号。

## 决策 32（第一问）：慢客户端如何处理发送队列积压？

**AI 建议：** 发送队列设硬上限，超过后直接断开该 WebSocket。建议规则：
- 每连接队列按"帧数 + 总字节数"双重限制；
- 流式 delta 不做无限缓存；
- 队列达到上限后，服务端关闭连接；
- 不丢弃中间事件后继续发送，因为那会让客户端处于不可判断的不完整状态；
- 客户端重连后重新 `observe`，恢复权威 snapshot；
- 已排队但未提交的用户消息仍可能丢失，这与此前约定一致；
- NDJSON 写入和 Agent 执行不因某个慢客户端被阻塞。

**用户回答（11:57:24）：** "容忍任何慢客户端，但代价最多是个游标。同时可以多客户端连接同一个对话，各自一个游标即可。慢不怕，他慢他的，在追上最新之前，他只读。"

**已确认（11:57:42）慢客户端策略：**
- 不因发送慢而断开；
- 服务端不为客户端维护无限发送队列，只维护一个读取游标；
- 多个客户端可以同时观察同一 `conversationId`，各自拥有独立游标；
- 落后的客户端继续追赶已提交事件；
- 在追上当前最新状态前，该客户端只能读取，不能发送会改变状态的事件；
- 一个客户端变慢不会阻塞 Agent、NDJSON 写入或其他客户端；
- 这意味着权威增量必须能从内存投影或 NDJSON 提交流重新读取，而不能只是一次性 WebSocket 帧。

## 决策 32（第二问）：游标使用全局提交 `id`

**AI 建议：** 每个观察者使用一个全局 NDJSON 提交 `id` 作为游标，不再创建会话级序号。服务端从 `cursor + 1` 向后读取全局提交，过滤出该连接所观察会话相关的事件；无关提交直接跳过。追到当前全局最新 `id` 后，客户端恢复可写。优点是：NDJSON 已经天然提供严格连续的全局 `id`；不需要维护第二套会话序号；服务端只保存每个观察者的一个整数；UTC 日志分文件也不影响游标语义；多会话观察可以共用同一个全局游标。

**用户回答（11:57:59）：** "可以"

**已确认（11:58:09）：**
- 每个连接使用全局 NDJSON 提交 `id` 作为追赶游标；
- 服务端从游标之后扫描全局提交流，并过滤出该连接观察范围内的事件；
- 多个客户端观察同一会话时各自维护独立游标；
- 客户端尚未追到当前最新全局 `id` 时，只读；
- 追赶不会阻塞业务执行或其他客户端。

## 决策 33：游标代表"已发送"还是"客户端已应用"？

**AI 建议：** 游标代表客户端已经成功应用到本地界面的最后一个全局提交 `id`。客户端处理完一批权威事件后，发送独立事件：

```json
{ "type": "cursor.advanced", "id": 1846 }
```

服务器据此更新该连接的游标。只有满足：

```text
客户端游标 == 服务端当前最新提交 id
```

该连接才恢复写权限。

不建议以"服务器已经写入 WebSocket"作为游标，因为事件进入 socket 缓冲区不代表 PWA 已经解析并应用；慢客户端可能被过早视为已追平。`cursor.advanced` 仍是 fire-and-forget 事件，不构成请求-响应协议。首次收到 snapshot 后，客户端应用完整 snapshot，再把游标推进到 snapshot 携带的 `lastCommitId`。

**用户回答（11:58:42）：** "可以"

**已确认（11:58:53）：**
- 游标表示客户端**已经成功应用**的最后一个全局提交 `id`；
- 客户端通过独立的 `cursor.advanced` 事件推进游标；
- snapshot 应用完成后，客户端推进到其 `lastCommitId`；
- 只有客户端游标追到服务器当前最新提交 `id`，该连接才获得写权限。

## 决策 34：慢客户端的追赶流量如何限速？

**AI 建议：** 采用确认驱动的分批推送。服务端每次最多推送一个有限批次，例如不超过 256 KiB；随后暂停该连接的权威增量发送，直到客户端发送 `cursor.advanced`，再从新游标继续读取下一批。实时流式 delta 在客户端未追平时不发送，因为它不是可重放的权威状态；客户端追平后才重新接收实时流。这样任意慢客户端只占用连接状态和游标，不会形成无限内存队列，也不会拖慢其他客户端或服务端业务。

**用户回答（12:00:27）：** "可以"

**已确认（12:00:42）追赶机制：**
- 每批权威事件设大小上限；
- 服务端发送一批后暂停；
- 客户端应用完成并发送 `cursor.advanced` 后，再发送下一批；
- 未追平时不接收不可重放的实时流式 delta；
- 每个慢客户端只消耗连接状态、游标和有限批次缓冲。
# 18. 写权限、陈旧检测与重试

> 覆盖对话决策 35、36、37、38。会话时间：2026-08-01 12:01–12:04。

## 决策 35：写权限应比较全局最新 `id`，还是观察范围水位？

目前"客户端游标必须等于服务器全局最新 `id` 才能写"存在一个问题：客户端只观察会话 A，但会话 B、配置、统计等无关业务持续产生新提交时，它可能永远追不上不断前移的全局尾部，即使会话 A 的状态完全是最新的。

**AI 建议：** 游标仍使用全局提交 `id`，但写权限只比较该连接当前观察和操作范围的 `relevantWatermark`。服务端扫描全局日志时可以跳过无关提交，但仍将游标推进越过它们。写入某个会话时，只要求：

```text
客户端已应用该会话最后一条相关提交
```

而不是要求它理解整个服务器的所有业务状态。对于跨领域命令，则检查该命令涉及的所有投影水位。例如修改全局 Provider 配置，就必须先同步 Provider 配置投影。

**用户回答（12:01:23）：** "当然按命令涉及的投影是否追平判断"

**已确认（12:01:33）：** 写权限按**该命令实际涉及的投影是否追平**判断，而不是要求客户端追平整个服务器的全局尾部。例如：
- 向会话 A 发送消息：只检查会话 A 及其必要配置投影；
- 修改 Provider 配置：检查 Provider 配置投影；
- 跨会话操作：检查所有受影响投影；
- 无关会话产生的新提交，不影响当前命令的写权限。

## 决策 36：落后客户端发出的写命令如何处理？

**AI 建议：** 立即拒绝，不在服务端排队等待其追平。服务器发送独立事件，例如：

```json
{
  "type": "command.rejected.stale-projection",
  "invocationId": "...",
  "requiredCommitId": 1846
}
```

原因是命令可能基于旧状态作出；等客户端追平后自动执行，可能已不符合用户当时看到的上下文。客户端收到拒绝后继续同步，追平后由用户重新触发，使用新的 `invocationId`。

**用户回答（12:03:08）：** "服务器端拒绝，客户端保持住 UI 状态并 toast 通知，用户只需追平后再点击发送即可"

**已确认（12:03:18）落后客户端的写入处理：**
- 服务端立即拒绝；
- 不替客户端排队，也不在追平后自动执行；
- 客户端保留输入框、附件和其他待发送 UI 状态；
- 客户端通过 toast 告知"状态尚未同步，请追平后重试"；
- 追平相关投影后，由用户再次点击发送。

## 决策 37：重试时是否复用 `invocationId`？

**AI 建议：** 内容未变化时复用原 `invocationId`；用户修改内容后生成新的 `invocationId`。原因是第一次命令可能已成功执行，但 `committed` 事件在断线中丢失。复用原标识可让服务端通过 `commandId` 幂等识别，避免重复发送。明确修改内容则代表新的逻辑执行，应使用新标识。

**用户回答（12:03:32）：** "可以"

**已确认（12:03:47）：**
- 被拒绝或结果未知的发送操作，载荷未变化时复用原 `invocationId`；
- 用户修改载荷后生成新的 `invocationId`；
- `commandId` 随 `invocationId + 规范化命令内容` 确定性生成；
- 因断线导致结果未知时，幂等机制可以避免重复执行。

## 决策 38：写命令是否携带显式的预期版本？

**AI 建议：** 不携带 `expectedVersion`。服务端直接根据当前 WebSocket 连接已经确认应用的全局游标，以及命令涉及投影的最后相关提交，判断客户端是否过期。处理时再次检查：

```text
连接已应用的游标
≥ 命令涉及投影的最后相关 commit id
```

即使客户端点击时已经追平，但在命令被服务端处理前，另一个客户端修改了同一投影，检查也会失败并拒绝该命令。这样能够实现乐观并发控制，又不需要客户端维护和提交会话版本号。命令载荷只表达业务意图，不混入同步控制字段。

**用户回答（12:04:07）：** "可以"

**已确认（12:04:15）：**
- 写命令不携带 `expectedVersion`；
- 服务端在实际处理命令时，根据该连接已确认的游标与命令涉及投影的最新提交位置判断是否陈旧；
- 即使客户端发出命令时是最新的，只要处理前相关投影被其他客户端修改，也会拒绝；
- 命令载荷只表达业务意图，不混入同步版本字段。
# 19. 提交协调器与故障恢复

> 覆盖对话决策 39、40（三个子决策）。会话时间：2026-08-01 12:05–12:10。
> 本主题修正了决策 5 中"投影失败即终止进程"的早期建议（见 04.md）。

## 决策 39：所有永久写入是否经过单一提交协调器？

**AI 建议：是。** 虽然不同会话的模型调用和 Tool 执行可以并行，但所有会改变永久状态的操作都应进入一个进程内的 FIFO 提交协调器：

```text
并行执行 Provider / Tool
        ↓
生成完整消息或业务事件
        ↓
全局 Commit Coordinator
        ↓
重新校验前置条件
        ↓
分配连续 id
        ↓
追加当日 NDJSON + flush
        ↓
更新内存投影
        ↓
向观察者发布权威增量
```

这样可以天然保证：全局 `id` 严格连续；同一时刻只有一个任务写 NDJSON；文件换日不会发生竞争；内存投影顺序与日志顺序完全一致；多客户端并发命令能在最终提交点再次检查陈旧状态；不需要文件锁、原子计数器或复杂事务。Provider 流式调用、Tool 执行和查询不占用协调器；只有最终完整消息或其他永久事件进入协调器，因此不会把长任务串行化。

**用户回答（12:05:03）：** "可以，但这个就是和 ndjson 单写者是同一者"

**已确认（12:05:10）：** **Commit Coordinator 与 NDJSON 单写者是同一个进程内组件**，不再拆成两层。它独占并负责：
- 分配连续全局 `id`；
- 选择当前 UTC 日期文件并处理换日；
- 序列化整条原子提交；
- 追加并 `flush`；
- 按日志顺序更新内存投影；
- 向观察者发布权威增量。

Provider、Tool 和查询可以并行，但任何永久状态变化都必须交给这个单写者。

## 决策 40（第一问）：落盘成功但内存投影失败时怎么办？

**AI 建议：** 立即终止进程，不回滚日志，也不继续服务。因为提交完成 `flush` 后，NDJSON 已是权威事实。若内存投影无法应用：不发送 `committed`；不尝试从日志删除刚写入的记录；停止接受新命令；退出进程；下次启动从头 replay；若 replay 仍无法确定性应用该提交，则按既定规则把它视为首个损坏点并静默截尾。继续运行会造成日志与内存状态分叉；运行时倒改日志则会破坏追加式模型。

**用户回答（12:07:16）：** "best effort 继续服务，同时向 stderr 打印异常信息。将污染的部分设置为 poisoned，保持现场，不污染的部分照常工作。"

**已确认（12:07:27）运行时策略：**
- NDJSON 已 `flush` 后，内存投影失败时不回滚日志，也不终止进程；
- 将异常完整写入 `stderr`；
- 标记受影响部分为 `poisoned`，保留故障现场；
- 任何依赖该污染部分的查询与写命令都拒绝；
- 不相关的会话、配置和业务投影继续正常工作；
- 单写者继续处理后续可安全应用的提交。

（对话指出：这与此前"启动 replay 遇到投影失败就静默截尾"的规则存在冲突：同一条合法日志可能在运行时被保留，重启时却被截掉。）

## 决策 40（第二问）：启动 replay 时投影异常是否也采用 poisoned？

**AI 建议：是。** 重新定义损坏边界：
- JSON 不完整或无法解析、提交外壳非法、全局 `id` 不连续：属于**物理日志损坏**，静默截尾；
- Agent Framework payload 无法反序列化，或投影代码处理某条合法事件时抛异常：不截尾，记录 `stderr`，将相关部分标为 `poisoned`，继续 replay 其他互不依赖的部分；
- 后续涉及 poisoned 部分的事件保留但不应用，以维持现场；
- 不受影响的投影继续恢复并提供服务。

**用户回答（12:09:23）：** "第一性原理：截尾是代价最小的，poison 次之，崩溃最坏。自行推导这意味什么。"

**AI 推导结论（12:09:33）：** 错误处理按**"截尾 → poison → 崩溃"的固定优先级**执行，不根据"物理损坏还是投影异常"预先选择不同路线。具体意味着：
- 启动 replay 遇到首个无法可靠应用的提交：从该提交开始静默截尾，删除所有后续内容，以最后有效前缀继续启动。
- 运行时刚完成 `flush`，但该提交无法应用到内存投影：因为单写者尚未处理下一条提交，直接截掉刚写入的尾行，保持原内存状态并继续服务。
- 只有在**不能安全截尾**时才 poison，例如日志文件无法修改，但故障能明确隔离到某个会话或投影；此时相关部分停止读写，其他部分继续服务。
- 只有截尾失败且无法隔离、全局顺序或核心内存状态已经无法信任时，才崩溃退出。
- 所有异常都写入 `stderr`，但不额外保留被截掉的数据。

因此，合法 JSON、Agent Framework 反序列化失败、投影代码异常、事件结构不兼容等，在恢复策略上没有本质区别：**能通过丢弃尾部恢复一致性，就先截尾。**

## 决策 40（第三问）：运行时截掉失败提交后是否复用其 `id`？

**AI 建议：复用。** 截尾后该提交从权威历史中不存在，下一次成功提交继续使用相同的连续 `id`，以维持"从 1 开始无空洞"的不变量。

**用户回答（12:10:40）：** "可以。但 stderr 要忠实，否则再无证据"

**已确认（12:10:45）：**
- 运行时截尾后，下一次成功提交复用被删除提交的 `id`；
- `stderr` 是唯一剩余证据，因此必须尽可能忠实记录；
- 不保存损坏后缀、不生成隔离副本、不额外写诊断文件。

建议每次截尾至少输出一条结构化 JSON 到 `stderr`，包含：

```json
{
  "level": "error",
  "event": "ndjson-tail-truncated",
  "utc": "2026-08-01T04:10:00.000Z",
  "phase": "runtime-commit",
  "file": "events/2026-08-01.ndjson",
  "commitId": 1843,
  "byteOffset": 927441,
  "exceptionType": "...",
  "exceptionMessage": "...",
  "stackTrace": "...",
  "rawCommit": "...",
  "action": "truncate-and-reuse-id"
}
```

其中 `rawCommit` 应是即将被截掉的完整原始行；若原始内容可能包含密钥，则仍然忠实输出，因为我们已经选择"stderr 是最后证据"，运维侧必须把 stderr 当作敏感日志保护。
# 20. 配置持久化与密钥管理

> 覆盖对话决策 41（三个子决策）、42（第一问）。会话时间：2026-08-01 12:11–12:17。

## 决策 41（第一问）：`stderr` 是否允许包含完整敏感 payload？

**AI 建议：** 允许完整输出，不做脱敏。理由是自动脱敏可能再次丢掉定位问题所需的字段，也可能因 schema 透明而无法可靠识别敏感信息。代价是必须明确：stderr 仅写本机受限目标；文件权限至少为当前用户独占；不默认上传到第三方日志平台；文档中标明 stderr 可能包含对话、Tool 参数、API 返回及密钥。

**用户回答（12:11:41）：** "stderr 内容完整，密钥不存，因为密钥对调试无用。"

**已确认（12:11:48）：**
- `stderr` 忠实输出被截掉的完整原始提交、异常类型、消息、堆栈、文件位置和字节偏移；
- **密钥本身不进入 NDJSON，因此也不会出现在 `rawCommit` 中**；
- 日志不做面向业务内容的脱敏，但认证凭据属于结构上禁止持久化的数据，而不是事后打码的数据；
- 若异常对象或第三方库错误消息意外携带密钥，应在写 `stderr` 前只针对已知凭据值做精确替换，不删减其他上下文。

## 决策 41（第二问）：密钥永久保存在哪里？

**AI 建议：** 首版使用独立的本机 secret store，不进入事件日志，也不进入普通内存投影。Linux x64 可采用：首选系统 secret service（Secret Service / libsecret）；系统不可用时，退化为权限 `0600` 的独立加密文件；NDJSON 只保存稳定的 `credentialRef`；Agent 调用前按 `credentialRef` 临时取出密钥；PWA 永远不能读取已有密钥明文，只能提交"设置、替换、删除"动作；查询配置时只返回 `configured: true/false` 或掩码提示。

**用户回答（12:15:01）：** "本项目有唯一的 toml 配置文件，其中明文密钥，本项目监听配置文件的变化，并且可以写入配置文件。不仅存密钥，也存所有配置。"

**已确认（12:15:08）配置模型：**
- 项目只有一个 TOML 配置文件；
- 所有配置均存于其中，包括明文密钥；
- NDJSON 不保存密钥，也不作为配置存储；
- Linux S 启动时读取 TOML；
- 运行时监听文件变化；
- 应用自身也可以修改并写回 TOML；
- PWA 的配置操作最终由 Linux S 写入该文件。

## 决策 41（第三问）：TOML 更新失败时采用什么语义？

**AI 建议：** TOML 是配置的唯一权威来源，但运行时保留"最后一次有效配置"。具体规则：
- 每次检测到文件变化，都读取并完整解析整个 TOML；
- 只有语法、类型和必要约束全部通过，才一次性替换内存配置；
- 若文件不存在、读取失败或内容非法：向 `stderr` 忠实打印异常；继续使用最后一次有效配置；不部分应用可解析字段；不自动覆盖或修复用户编辑的文件；
- 应用写配置时采用"临时文件 → `flush` → 同目录原子重命名"，避免 watcher 读到半写文件；
- 配置变化不写入 NDJSON，因为 TOML 本身就是配置事实来源。

**用户回答（12:15:44）：** "可以"

**已确认（12:15:56）TOML 运行语义：**
- TOML 是全部配置的唯一权威来源；
- 文件变化后整份读取、解析和校验；
- 只有完整有效的新配置才替换当前内存配置；
- 文件缺失、读取失败或内容非法时，向 `stderr` 输出异常，并继续使用最后一次有效配置；
- 不部分应用、不自动修复用户文件；
- 应用写入采用同目录临时文件、`flush`、原子重命名；
- 配置变化不写入 NDJSON。

## 决策 42（第一问）：应用修改 TOML 时是否保留注释与未知字段？

**AI 建议：必须保留。** 应用写配置时，不应根据当前强类型配置对象重新生成整份 TOML，否则会丢失：用户注释；自定义字段；新版本添加但旧客户端尚不认识的字段；原有分组和排列；用户用于运维说明的文本。建议把 TOML 同时保留为可编辑语法树；PWA 修改某个配置项时，只修改对应节点，其他内容原样保留。无法安全定位或修改时，应拒绝写入并输出错误，而不是重写整份文件。

**用户回答（12:17:19）：** "不，重写即可"

**已确认（12:17:26）：** 应用修改配置时，可以根据当前内存配置**完整重写整个 TOML 文件**；无需保留原注释、字段顺序、格式布局或未知字段。无法识别的字段在重写后可以消失。
# 21. 配置热更新与冲突

> 覆盖对话决策 42（第二问）、43、44（两个子决策）。会话时间：2026-08-01 12:19–12:21。

## 决策 42（第二问）：外部编辑与应用写入冲突时怎么办？

**AI 建议：** 写入前比较当前文件内容哈希：
- Linux S 每次成功加载 TOML 后保存其内容哈希；
- PWA 修改配置时，基于该已加载版本生成新配置；
- 真正写入前重新读取文件；
- 若文件哈希已变化，说明期间有人通过编辑器修改过文件；
- 服务端拒绝本次写入，重新加载外部版本，并通知 PWA 配置已变化；
- 不静默覆盖外部编辑；
- 若哈希一致，则完整生成 TOML，通过临时文件和原子重命名替换。

**用户回答（12:19:35）：** "不检测，强行覆盖，寄希望于监听配置文件的变化，以及写入修改前的物理时间较短，不足以让用户用编辑器修改"

**已确认（12:19:42）TOML 写入冲突策略：**
- 应用写入前不检查文件哈希、mtime 或版本；
- 基于当前内存配置直接完整重写；
- 外部编辑若恰好与应用写入并发，允许被覆盖；
- 依赖文件监听尽快吸收外部变化；
- 不提供冲突合并、恢复或覆盖警告。

## 决策 43：配置监听如何处理文件系统的重复通知？

应用使用"临时文件 + 原子重命名"写 TOML 时，文件监听器可能连续收到创建、重命名、修改等多个通知；编辑器保存也可能产生相同现象。

**AI 建议：** 采用可重置的短暂 debounce，但不忽略应用自己的写入。规则：
- 任意相关文件事件都启动或重置一次约 100 ms 的 debounce；
- debounce 到期后完整读取并校验 TOML；
- 多个连续通知合并为一次加载；
- 应用写完后也通过相同监听路径重新读取文件，验证磁盘内容确实可以成为当前配置；
- 若监听通知丢失，应用写入成功后主动触发一次同样的 reload；
- reload 仍遵循"完整有效才替换，失败则保留最后有效配置"。

这样不存在"内存更新一套、磁盘写入另一套"的双路径语义。

**用户回答（12:20:47）：** "自己要修改配置，也是先落盘后加载，一样的"

**已确认（12:20:53）配置变更只有一条路径：**

```text
生成完整 TOML
→ 写临时文件
→ flush
→ 原子替换正式文件
→ 从正式文件重新读取
→ 完整解析与校验
→ 成功后替换内存配置
```

应用自身修改配置时，也不能直接先改内存；必须先落盘，再通过同一加载逻辑生效。这样磁盘文件始终是唯一权威来源。

## 决策 44（第一问）：何时向客户端确认配置修改成功？

**AI 建议：** 只有新 TOML 已落盘，并从正式文件重新读取、解析和应用成功后，才发送成功事件。若任一步失败：
- 保留最后一次有效内存配置；
- 向 `stderr` 输出完整异常；
- 向发起修改的客户端发送失败事件；
- 不把"文件已写入但尚未成功加载"视为成功；
- 若文件已经被替换成无效内容，则运行时仍继续使用旧配置，等待下一次文件变化。

**用户回答（12:21:07）：** "可以"

**已确认（12:21:12）：** 配置修改只有在 **TOML 已成功落盘，并从正式文件重新读取、完整解析、校验并应用成功后**，才向客户端发送成功事件；否则继续使用上一份有效内存配置，并报告失败。

> 注：对话中该条目编号为"第 44 个决策：何时向客户端确认配置修改成功？"，此处与决策 44（第一问）合并记录。
# 22. 认证：配对与令牌

> 覆盖对话决策 45、46（两个子决策）、47。会话时间：2026-08-01 12:21–12:25。

## 决策 45：PWA 如何认证到 Linux S？

**AI 建议：** 首版使用单一预共享访问令牌，配置在 TOML 中。建议语义：
- WebSocket 建连时必须携带令牌；
- 服务端在升级连接前完成校验；
- 所有成功连接拥有相同权限，不做用户、角色或会话级 ACL；
- 令牌可以通过 TOML 热更新；
- 令牌变化后，已有连接继续有效到断开，新连接使用新令牌；
- PWA 只在当前浏览器运行内存中持有令牌，不写入 IndexedDB、LocalStorage 或其他持久存储；
- TLS 由反向代理或 Linux S 自身提供，禁止通过明文公网 WebSocket 传输令牌。

首版只有单进程、单配置文件、多个对等客户端，没有必要立即引入账户系统、刷新令牌和权限数据库。

**用户回答（12:21:43）：** "可以"

**已确认（12:21:50）首版认证模型：**
- TOML 中配置单一预共享访问令牌；
- WebSocket 建连前完成校验；
- 所有认证成功的客户端权限相同；
- 不引入账户、角色或会话级 ACL；
- PWA 不持久保存令牌；
- 令牌热更新后，新连接必须使用新值，已有连接可持续到断开。

## 决策 46（第一问）：PWA 如何获得访问令牌？

**AI 建议：** 由用户每次打开 PWA 时手动输入，令牌只保存在页面内存中。原因是：符合 PWA 不持久保存数据的约束；不把令牌放入 URL、查询参数或 fragment，避免浏览器历史、截图、代理日志及复制链接泄漏；不依赖 Cookie、LocalStorage 或 IndexedDB；页面刷新或浏览器关闭后，需要重新输入；认证失败时保留输入框内容，允许用户修正后重新连接。

**用户回答（12:24:06）：** "我改主意了，采用每客户端一个令牌，初次使用时候用 stderr 和6位数字配对码鉴权，并令牌永久化。能看到 stderr 的用户就可以用。"

**已确认（12:24:12）修改后的首版认证模型：**
- 每个客户端拥有独立令牌；
- 首次连接时，Linux S 将 **6 位数字配对码**输出到 `stderr`；
- 能读取该实例 `stderr` 的用户被视为有权批准新客户端；
- 客户端提交配对码后，服务端签发长期客户端令牌；
- 令牌在客户端永久保存，后续连接不再需要配对；
- 服务端需要在 TOML 中永久保存已签发客户端及其令牌信息；
- 某个客户端令牌可单独吊销，不影响其他客户端。

## 决策 46（第二问）：配对码的生命周期

**AI 建议：** 配对码由服务端按需生成，单次使用，5 分钟过期。推荐流程：

```text
客户端发送 pairing.requested
→ 服务端生成密码学随机 000000-999999
→ stderr 输出配对码及到期时间
→ 客户端提交 pairing.attempted
→ 校验成功后立即销毁配对码
→ 签发该客户端的永久令牌
```

并且：同一时刻只允许一个有效配对码；新生成的配对码使旧码立即失效；连续失败尝试应限速，避免在线枚举；配对码不写入 TOML 或 NDJSON；签发的令牌必须是高熵随机值，不能由 6 位配对码推导。

**用户回答（12:24:32）：** "可以"

**已确认（12:24:40）配对码规则：**
- 由服务端按需生成；
- 6 位十进制数字，使用密码学安全随机源；
- 单次使用；
- 5 分钟过期；
- 同一时刻仅允许一个有效配对码；
- 新码生成后旧码立即失效；
- 配对码只输出到 `stderr`，不进入 TOML 或 NDJSON；
- 配对成功后签发独立、高熵、永久客户端令牌。

## 决策 47：服务端如何保存客户端令牌？

**AI 建议：** 客户端保存令牌原文，服务端 TOML 只保存令牌哈希。建议令牌使用至少 256 bit 随机值，例如：

```text
kelivo_client_<base64url(32 random bytes)>
```

TOML 中记录：

```toml
[[auth.clients]]
id = "019c..."
name = "Firefox on laptop"
token_hash = "sha256:..."
created_at = "2026-08-01T04:24:00Z"
```

验证时对客户端提交的令牌计算 SHA-256，并进行常量时间比较。这样即使 TOML 被读取，攻击者也不能直接拿其中内容连接服务端；同时仍然支持逐客户端吊销。这里无需慢密码哈希，因为令牌具有足够高的随机熵，不是用户选择的密码。

**用户回答（12:25:47）：** "可以。另外请注意：新的项目不得提及 kelivo, agent-framework, avalonia 字样。正式名称是 "万象", wanxiang"

**已确认（12:26:06）：**
- 正式产品名：**万象**
- 英文及技术标识：**wanxiang**
- 新项目的一方命名、UI、协议、配置项、日志和文档不得出现三个旧名称
- 每客户端永久令牌：客户端保存原文，服务端 TOML 仅保存 SHA-256 哈希。

> 注：对话中令牌示例前缀使用了旧名称（`kelivo_client_`），该前缀在正式实现中应改为 `wanxiang_client_`（或按禁名规则处理），令牌格式本身为「至少 256 bit 随机值 + base64url」。
# 23. 命名边界与 clean-room

> 覆盖对话决策 48（三个子决策）。会话时间：2026-08-01 12:26–12:29。

## 决策 48（第一问）：禁名规则是否覆盖第三方依赖声明？

这里存在一个实际冲突：项目文件、依赖锁文件、许可证清单及构建产物元数据通常必须出现第三方包的官方名称。若要求整个仓库逐字禁止三个旧名称，就无法正常声明当前选定的 UI 与 Agent 依赖。

**AI 建议：** 禁名规则覆盖所有一方内容，但豁免不可改写的第三方标识。允许出现的范围仅包括：包引用与锁文件；第三方许可证和 NOTICE；构建系统自动生成的依赖元数据；必须匹配第三方 API 的命名空间或类型名。除此之外，目录名、程序集名、命名空间、代码中的一方类型、协议事件、TOML 字段、UI、日志和项目文档全部只使用 `万象` / `wanxiang`。

**用户回答（12:27:30）：** "允许 avalonia agent-framework 依赖，可以提及但不主动提及。kelivo 只是灵感来源，会写入 Acknowledgement 但不是依赖，也不协调其许可证"

**已确认（12:27:40）命名与致谢边界：**
- **万象 / wanxiang** 是唯一的一方产品名称；
- Avalonia 与 Agent Framework 作为实际第三方依赖，可以在依赖声明、技术文档和必要代码位置出现，但不作为产品宣传重点；
- Kelivo 仅作为灵感来源，在 Acknowledgements 中说明，不作为依赖或产品组成部分。

## 决策 48（第二问）：是否采用 clean-room 边界？

背景：现有项目材料同时要求"UI 基本不变"，并建议直接阅读原项目的源码、数据模型和界面截图进行逐项移植；材料也注明其许可证为 AGPL-3.0。这与"仅是灵感来源、万象不按其许可证协调"不能同时保持模糊。

**AI 建议：采用。** 具体限制为：
- 不复制、翻译或机械改写原项目源码；
- 不复制其资源、图标、文案或生成代码；
- 不追求逐像素复刻；
- 只参考公开可观察的产品行为、功能类别和一般交互思想；
- 万象的数据模型、协议、UI 组件结构和实现独立设计；
- Acknowledgements 只说明灵感来源，不暗示派生或兼容关系。

**用户回答（12:28:43）：** "当然是按 clean-room 方式，只作为灵感来源，UI 也仅仅是启发而已"

**已确认（12:29:04）clean-room 边界：**
- 万象独立设计与实现；
- 原项目仅提供产品灵感和功能启发；
- 不复制、翻译或机械改写其源码、资源、数据结构及文案；
- UI 不做逐像素复刻；
- Acknowledgements 仅说明灵感来源，不建立依赖、兼容或衍生关系。

## 决策 48（第三问）：Linux 客户端与 PWA 是否共享 UI 代码？

**AI 建议：** 共享同一套 F# UI、ViewModel 和 WebSocket 客户端代码，仅保留不同的平台入口。
- Linux x64 入口同时启动本地 UI、客户端连接层和服务端；
- PWA 入口只启动 UI 与远程客户端连接层；
- Linux UI 即使连接本机服务，也走与远程客户端相同的 muxed WebSocket 协议，不直接调用服务端内存对象；
- 文件系统、进程、托盘等 Linux 专属功能放在平台适配层；
- 浏览器端不引用服务端、NDJSON、TOML 或本机工具实现。

这样能够持续验证"自己的 C 指挥自己的 S"与远程调用使用完全相同的协议路径，避免本机模式形成隐藏的第二套行为。

**用户回答（12:29:23）：** "同意 Linux C 与 PWA 共用一套 UI/客户端代码，并且 Linux 本机连接也强制经过 WebSocket"

**已确认（12:29:30）Linux x64 与 PWA 的客户端层：**
- 共用同一套 F# UI、ViewModel 与协议客户端代码；
- Linux 本机客户端也必须通过 loopback WebSocket 连接本机服务端；
- 禁止通过进程内对象、共享内存或特殊本地接口绕过协议；
- 因此"本机控制自己"和"远程客户端控制服务端"走同一条行为路径。
# 24. 运行模式：单进程与三开关

> 覆盖对话决策 49、50（两个子决策）。会话时间：2026-08-01 12:29–12:31。

## 决策 49：Linux UI 与服务端采用同进程还是独立进程？

**AI 建议：** 首版采用一个进程、一个可执行文件，同时承载 Linux UI 与服务端。进程内部仍保持严格边界：

```text
Linux UI Client
      │
      │ loopback muxed WebSocket
      ▼
Linux Server
      │
      ├─ Agent runtime
      ├─ NDJSON 单写者
      ├─ 内存投影
      └─ TOML 配置
```

这意味着：
- 启动一个 `wanxiang` 进程即可得到本地 C+S；
- UI 与服务端不得直接调用彼此业务对象；
- 即使同进程，也必须经过真实 WebSocket 编解码、认证和事件分发；
- 关闭 Linux UI 即结束整个实例及其服务端；
- 需要纯服务端运行时，使用同一可执行文件的 `--headless` 模式，不启动窗口；
- 多开 `wanxiang` 时，每个进程使用独立配置目录、日志目录和监听端口，彼此完全无关。

相比拆成两个操作系统进程，这更符合已经确定的"每个实例严格单进程"，也减少首版的进程监管、升级协调和本地发现复杂度。

**用户回答（12:29:58）：** "同意首版每个 Linux 实例只有一个 wanxiang 进程，UI 与 S 同进程但只能通过 loopback WebSocket 交互，并支持同进程的 --headless 模式"

**已确认（12:30:09）首版 Linux 运行拓扑：**
- 每个实例只有一个 `wanxiang` 进程；
- 默认模式同时运行桌面 UI 与服务端；
- `--headless` 只运行服务端；
- 本地 UI 也必须通过真实 loopback WebSocket、认证和协议编解码访问服务端；
- 多开进程彼此完全独立。

## 决策 50（第一问）：PWA 是否由同一个服务端托管？

**AI 建议：是。** Linux S 同时提供 PWA 静态资源和 muxed WebSocket 端点，默认使用同一个 origin：

```text
https://host/
https://host/ws
```

这样可以：一个进程、一个端口完成部署；避免 CORS 与跨域 WebSocket 配置；PWA 天然连接提供它的那个实例；PWA 版本与服务端协议版本一起发布；反向代理只需转发一个站点。桌面 UI 的资源仍打包进 Linux 可执行程序；PWA 是同一客户端代码的浏览器构建产物，由服务端内嵌或从安装目录读取。

**用户回答（12:31:04）：** "可以。另外 linux 有 pwa only 和 client only 模式，说白了是 server client pwa 三个布尔八种组合"

**已确认（12:31:15）Linux 单一可执行文件由三个独立布尔开关组成：**
- `server`：运行万象服务端、Agent 执行、NDJSON、内存投影和 WebSocket 端点；
- `client`：运行 Linux 桌面客户端；
- `pwa`：托管 PWA 静态资源。

因此存在八种组合，包括完整 `server + client + pwa`、`pwa-only`、`client-only`、`server-only` 等。客户端与服务端仍通过 WebSocket 隔离。

## 决策 50（第二问）：三个开关全为 `false` 时做什么？

**AI 建议：** 允许该组合，但只执行配置加载和校验，随后以成功状态退出：

```text
server = false
client = false
pwa = false
```

它不进入常驻状态，也不监听端口。这样八种组合在语法上都有效，同时该模式可用于部署前检查 TOML 是否可读取、解析和通过约束校验。

**用户回答（12:31:41）：** "三个开关全为 false 时就是一般意义上的 doctor 模式"

**已确认（12:31:56）：** `server=false, client=false, pwa=false` 为标准 **doctor 模式**，而不只是 TOML 语法检查。它应检查配置、目录权限、NDJSON 连续性与可 replay 性、端口可用性、运行依赖及静态资源完整性，然后输出诊断结果并退出。
# 25. doctor 与 --fix

> 覆盖对话决策 51（两个子决策）。会话时间：2026-08-01 12:32–12:33。

## 决策 51（第一问）：doctor 是否允许修复数据？

**AI 建议：** doctor 默认严格只读。即使发现 NDJSON 尾部损坏，也只向 `stderr` 报告损坏文件、字节偏移、预期 `id` 和异常，不执行正常启动时的静默截尾。否则用户只是运行诊断，就可能永久改变现场。正常 `server` 启动仍按既定规则自动截尾；未来可另设显式 `--doctor-fix`，但不属于首版默认行为。

**用户回答（12:32:39）：** "另设 --fix ，因此共 16 模式"

**已确认（12:32:44）运行模式由四个布尔量构成：**
- `server`
- `client`
- `pwa`
- `fix`

共 **16 种组合**。其中：
- `0000`：只读 doctor；
- `0001`：doctor + 自动修复；
- 其余组合启动所选运行组件；
- `fix` 决定启动检查发现问题时是否允许修复。

## 决策 51（第二问）：`--fix` 在非 doctor 模式中的执行顺序

**AI 建议：** 先执行完整 doctor/fix，再决定是否启动组件：

```text
加载并检查
→ fix=false：发现阻断性问题则退出
→ fix=true：执行可用修复并重新检查
→ 检查通过后启动 server/client/pwa
```

也就是说，`server=true, fix=true` 不是运行期间持续修复，而是**启动前修复一次，然后正常运行**。运行期间仍按既定的自动截尾、stderr 和 best-effort 规则处理。这样 16 种模式的语义统一，也避免 `--fix` 在服务运行期间不断修改现场。

**用户回答（12:32:57）：** "对的"

**已确认（12:33:05）：** `--fix` 只作用于**启动前的 doctor 修复阶段**。完成修复并重新检查通过后，才启动所选择的组件；运行期间不会常驻执行 doctor 修复。
# 26. PWA 模式与凭据存储

> 覆盖对话决策 52（三个子决策）、53。会话时间：2026-08-01 12:33–12:36。

## 决策 52（第一问）：`pwa=true, server=false` 如何运行？

**AI 建议：** PWA 组件拥有独立的最小静态资源 HTTP 服务。因此：
- `pwa=true, server=true`：静态资源服务与 WebSocket 服务可共用同一监听端口和 origin；
- `pwa=true, server=false`：仍启动静态资源 HTTP 服务，但不提供本机 WebSocket 服务；
- PWA 加载后由用户选择或输入远端万象 S 地址；
- `pwa=false`：完全不托管浏览器资源；
- PWA 静态服务不读取 NDJSON、不运行 Agent、不拥有业务投影，只负责 HTML、JS、CSS、manifest 和 service worker。

这样 `pwa-only` 才是真正可运行的模式，而不是单纯导出静态文件。

**用户回答（12:33:46）：** "pwa=true, server=false 就是能连别人没有自己呗"

**已确认（12:33:55）语义：**
- `pwa=true, server=false`：本实例只提供 PWA 客户端，不运行自己的 S；
- PWA 可以连接其他万象 S；
- 为了让浏览器打开 PWA，进程仍需提供最小静态资源服务，但它不提供业务 WebSocket、Agent、NDJSON 或投影；
- `pwa=true, server=true` 时，PWA 默认可以连接同实例 S，也可以切换连接其他 S。

## 决策 52（第二问）：PWA 的永久客户端令牌存在哪里？

此前已经确定"每客户端一个永久令牌"，这意味着 PWA 必须突破最初的"浏览器端完全不持久化"约束。

**AI 建议：** 令牌保存在 IndexedDB，按服务端身份分别存储。记录关系类似：

```text
serverIdentity → serverUrl + clientId + clientToken
```

不建议放在 `localStorage`：IndexedDB 更适合结构化、多服务端凭据；可以按 origin 隔离；后续可加入迁移、吊销和多个连接配置；浏览器环境无法真正防止同源恶意脚本读取，因此不应假装能做到操作系统级密钥保护。

**用户回答（12:35:06）：** "IndexedDB 在 ios 端是本地的吗"

**AI 调研结论（12:35:08）：** 是的。iOS 上的 IndexedDB 数据保存在 **iPhone/iPad 本机的 WebKit 网站数据存储**中，按 origin 隔离；不是存在万象服务器，也不会天然跨设备同步。关闭页面或重启设备后通常仍保留。但它不是 Keychain 式的"不可丢失安全存储"：
- 用户清除网站数据或卸载主屏幕 PWA 后，令牌可能消失；
- 系统存储压力下，默认的 best-effort 数据可能被清理；
- 隐私浏览中的 IndexedDB 只维持到该隐私会话结束；
- 同源 JavaScript 能读取它，因此 XSS 仍可窃取令牌。

主屏幕 PWA 可以调用 `navigator.storage.persist()` 请求持久存储；WebKit 会结合是否为主屏幕 Web App 等条件决定是否批准，但仍不能抵抗用户主动清除数据。

**对万象的建议：** PWA 将长期客户端令牌保存在 IndexedDB；启动后请求 persistent storage。令牌丢失不视为服务端故障，用户重新用 6 位码配对即可。

**用户回答（12:36:00）：** "好的，这就对了"

**已确认（12:36:06）PWA 凭据恢复策略：**
- 每个万象 S 的长期客户端令牌保存在 IndexedDB；
- IndexedDB 被清除、PWA 被卸载或浏览器数据被重置后，令牌丢失属于正常情况；
- 用户重新通过 6 位配对码配对即可；
- PWA 仍不缓存会话或消息，持久化内容仅限连接配置与客户端令牌。

## 决策 53：如何识别同一个服务端实例？

不能只用 URL 作为身份。域名、端口、反向代理地址可能变化；一个 URL 也可能后来指向另一个实例。

**AI 建议：** 每个 S 在 TOML 中拥有永久、稳定的 `instanceId`，首次创建时生成 UUIDv7。配对后，PWA 保存：

```text
instanceId
serverUrls[]
clientId
clientToken
displayName
```

规则：
- `instanceId` 是服务端实例的真实身份；
- URL 只是可变连接地址；
- 服务端在连接握手早期声明自己的 `instanceId`；
- 客户端发现该 URL 返回了不同 `instanceId` 时，不发送已有令牌，要求用户确认或重新配对；
- 多个 URL 可以映射到同一个实例；
- 多开万象进程时，各自必须使用独立 `instanceId`。

**用户回答（12:36:22）：** "可以"

**已确认（12:36:30）：**
- 每个万象 S 在 TOML 中持久保存稳定的 UUIDv7 `instanceId`；
- URL 只是连接地址，不是实例身份；
- 同一实例可以拥有多个 URL；
- 客户端发现原 URL 对应的 `instanceId` 变化时，不复用旧令牌，必须重新确认或配对；
- 多开实例各自拥有独立 `instanceId`。
# 27. 令牌即身份与轮换

> 覆盖对话决策 54（两个子决策）。会话时间：2026-08-01 12:37。

## 决策 54（第一问）：`clientId` 由谁生成？

**AI 建议：** 配对成功时由服务端生成 UUIDv7 `clientId`，与永久令牌一起返回客户端。服务端 TOML 保存：

```toml
[[auth.clients]]
id = "client UUIDv7"
name = "用户提供的设备名称"
token_hash = "sha256:..."
created_at = "..."
```

客户端保存：

```text
instanceId + clientId + token
```

这样服务端可以稳定地：单独吊销某个客户端；修改客户端显示名称；查看最近连接时间；避免客户端自行伪造或复用其他客户端身份。浏览器数据被清除后重新配对，会获得新的 `clientId`；旧记录仍可由用户在管理界面中吊销。

**用户回答（12:37:08）：** "永久令牌就是 clientId 可以吗"

**AI 回应（12:37:16）：** 可以，而且更简洁。建议取消独立 `clientId`：
- 客户端永久保存高熵令牌原文；
- 服务端 TOML 只保存令牌的 SHA-256 哈希；
- 认证后的客户端身份就是该令牌哈希；
- 管理界面使用哈希前缀作为可见标识，例如 `7f3a91c2`；
- 客户端名称、创建时间、最近连接时间、吊销状态都挂在该哈希记录下；
- 协议中不再单独传输 `clientId`。

这样不会出现"ID 与令牌必须保持对应"的重复状态。需要注意，令牌原文是秘密，不能把它当普通 ID 写进日志、错误信息或 UI；可公开使用的只是哈希或哈希指纹。

## 决策 54（第二问）：永久令牌是否允许轮换？

**AI 建议：** 首版不做原地轮换。需要更换时，吊销旧令牌并重新配对生成新令牌，语义最简单。

**用户回答（12:37:37）：** "嗯"

**已确认（12:37:47）：**
- 永久令牌本身就是客户端身份；
- 服务端只保存其 SHA-256 哈希；
- 不再单独维护 `clientId`；
- 首版不支持原地轮换；
- 更换凭据时吊销旧令牌并重新配对。
# 28. 令牌提交、吊销与并发连接

> 覆盖对话决策 55、56、57、58、59。会话时间：2026-08-01 12:38–12:40。

## 决策 55：WebSocket 建连时如何提交永久令牌？

浏览器 WebSocket API 不能可靠设置自定义 `Authorization` 请求头；把令牌放在 URL 查询参数中又容易进入代理日志和浏览器记录。

**AI 建议：** 先建立 TLS WebSocket，再把令牌作为第一条协议事件发送。连接在认证前处于受限状态：

```json
{ "type": "auth.present", "token": "<永久令牌>" }
```

服务端在认证完成前：
- 只接受认证和配对相关事件；
- 不发送任何业务状态；
- 不允许 `observe` 或写命令；
- 设置较短认证超时；
- 认证失败后立即关闭连接；
- 不把令牌写入 stderr、NDJSON 或普通诊断日志。

整个过程必须使用 `wss://`；明文 `ws://` 仅允许明确配置的 loopback 本机连接。

**用户回答（12:38:07）：** "嗯"

**已确认（12:38:14）：**
- 永久令牌通过已建立的 `wss://` 连接，以第一条受限认证事件提交；
- 不放入 URL、Cookie 或握手 Header；
- 认证前连接只能处理认证与配对事件；
- 明文 `ws://` 仅可用于明确允许的 loopback 场景。

## 决策 56：配对成功时，先持久化还是先下发令牌？

**AI 建议：** 先将令牌哈希写入 TOML 并重新加载成功，再将令牌原文发送给客户端。顺序如下：

```text
生成高熵永久令牌
→ 将 SHA-256 哈希加入配置
→ 完整重写 TOML
→ 从正式文件重新加载成功
→ 消耗并销毁 6 位配对码
→ 仅向该连接发送一次令牌原文
```

这样客户端收到令牌时，服务端一定已经能够使用它完成后续认证。代价是：若 TOML 已写成功，但发送令牌前连接断开，服务端会留下一个客户端并未取得原文的孤立哈希记录。由于服务端不保存令牌原文，无法补发；用户只能重新配对，之后再吊销孤立记录。相比先发令牌再持久化，这不会产生"客户端以为配对成功，但令牌实际上无法登录"的更危险状态。

**用户回答（12:39:17）：** "嗯"

**已确认（12:39:27）配对提交顺序：**

```text
生成永久令牌
→ 写入令牌哈希到 TOML
→ 从正式文件重新加载成功
→ 销毁配对码
→ 向该连接一次性发送令牌原文
```

客户端收到令牌时，它已经可以用于认证；发送前断线可能留下未被客户端取得的孤立哈希记录。

## 决策 57：孤立令牌记录是否自动清理？

**AI 建议：** 不自动清理。服务端无法可靠区分：客户端完全没收到令牌；客户端收到了令牌，但暂时还没有再次连接；客户端保存成功，但长期离线。因此记录保留在 TOML 中，并通过 `last_seen` 是否为空供管理界面识别。用户可以手动吊销从未成功认证过的记录，避免自动清理误删有效客户端。

**用户回答（12:39:36）：** "嗯"

**已确认（12:39:40）：** 孤立令牌哈希记录不会自动清理，永久保留到用户手动吊销。

## 决策 58：吊销令牌后，现有连接是否立即失效？

**AI 建议：** 立即失效。规则：
- TOML 中删除或标记该令牌哈希为已吊销；
- 配置重新加载成功后，服务端查找所有使用该令牌认证的现有 WebSocket；
- 立即关闭这些连接；
- 后续重连认证失败；
- 不等待连接自然断开；
- 吊销动作不写入 NDJSON，因为认证配置的权威来源是 TOML。

否则被盗令牌即使已吊销，现有长连接仍可能继续观察会话和发送命令。

**用户回答（12:39:50）：** "嗯"

**已确认（12:40:01）：** 令牌一旦在 TOML 中被吊销并成功重新加载，服务端立即断开该令牌对应的全部现有 WebSocket；后续认证也会失败。

## 决策 59：同一个永久令牌能否建立多个并发连接？

**AI 建议：允许。** 同一令牌可以同时用于多个标签页、窗口或同一设备上的重连交接：
- 每条 WebSocket 独立维护观察集合、全局游标和追赶状态；
- 所有连接共享同一个认证身份；
- 一个连接变慢不影响同令牌下的其他连接；
- 吊销令牌时关闭其全部连接；
- 不限制"一个令牌只能在线一次"，避免新连接误踢掉旧标签页；
- 真正不同的设备应分别配对，以便单独吊销。

**用户回答（12:40:12）：** "同意"

**已确认（12:40:22）：** 同一永久令牌允许建立多个并发 WebSocket 连接；每条连接独立维护观察集合、游标和追赶状态，吊销时统一断开。
# 29. 模式开关优先级与默认组合

> 覆盖对话决策 60、61。会话时间：2026-08-01 12:40–12:41。

## 决策 60：四个模式开关由哪里决定？

**AI 建议：** 默认值来自 TOML，命令行只做本次启动覆盖。例如：

```toml
[runtime]
server = true
client = true
pwa = true
fix = false
```

命令行可覆盖：

```text
wanxiang --server=false --client=true --pwa=false --fix
```

优先级固定为：

```text
命令行显式值 > TOML > 程序默认值
```

这样常规部署可以永久配置，临时 doctor、client-only 或修复启动又无需修改配置文件。命令行覆盖仅影响当前进程，不回写 TOML。

**用户回答（12:40:41）：** "同意"

**已确认（12:40:45）四个运行开关的优先级：**

```text
命令行显式值 > TOML 配置值 > 程序默认值
```

命令行覆盖只影响本次启动，不回写 TOML。

## 决策 61：程序默认组合是什么？

**AI 建议：** 默认启用 `server + client + pwa`，关闭 `fix`。也就是没有 TOML 对应项、命令行也未显式指定时：

```text
server = true
client = true
pwa    = true
fix    = false
```

理由：
- 普通用户直接运行 `wanxiang`，应得到完整 Linux C+S 与 PWA 托管能力；
- `fix` 会修改永久数据，不能默认开启；
- headless、client-only、pwa-only 等部署属于显式选择；
- 三个组件虽然同时启用，但桌面客户端仍严格通过 loopback WebSocket 访问本机 S。

**用户回答（12:41:10）：** "同意"

**已确认（12:41:16）默认模式：**

```text
server = true
client = true
pwa    = true
fix    = false
```

即直接运行 `wanxiang` 时，默认启动完整 Linux C+S，并托管 PWA；修复功能必须显式开启。
# 30. 连接目标选择与自动配对

> 覆盖对话决策 62、63、64、65。会话时间：2026-08-01 12:41–12:43。

## 决策 62：Linux Client 默认连接哪个 S？

**AI 建议：**
- `server=true, client=true`：桌面 Client 默认连接本进程的 loopback WebSocket；
- `server=false, client=true`：连接 TOML 中配置的远端默认 S；
- 本地 S 连接失败时，不自动回退到远端，避免用户误操作另一个实例；
- 用户可以在 UI 中显式切换到其他已配对 S；
- 当前选择的 S 地址与永久令牌作为客户端配置持久化。

**用户回答（12:41:54）：** "有本地 S 时连接本地，否则让用户选择。没有默认"

**已确认（12:41:59）Linux Client 的目标选择：**
- `server=true, client=true`：自动连接本进程的 loopback S；
- `server=false, client=true`：不设置默认远端，启动后由用户选择已配对的 S；
- 不自动回退，也不根据"上次使用"隐式连接。

## 决策 63：同源 PWA 是否自动连接本机 S？

**AI 建议：**
- `server=true, pwa=true`：该实例托管的 PWA 自动连接同源 WebSocket；
- `server=false, pwa=true`：PWA 不设默认 S，由用户选择；
- 用户进入 UI 后仍可主动切换到其他已配对 S。

这与桌面 Client 的规则一致：**有随实例启动的本地 S，就连接它；没有就让用户选择。**

**用户回答（12:42:13）：** "嗯"

**已确认（12:42:21）：**
- `server=true, pwa=true`：同源 PWA 自动连接本实例的 WebSocket；
- `server=false, pwa=true`：不设默认目标，由用户选择已配对的 S；
- 用户仍可主动切换到其他实例。

## 决策 64：本机桌面 Client 如何首次获得本机 S 的令牌？

**AI 建议：** 自动配对，不走 6 位码。当 `server=true, client=true` 时，同一进程在启动阶段：
1. 为本机桌面 Client 生成高熵永久令牌；
2. 在 TOML 的服务端授权列表中保存其哈希；
3. 在 TOML 的客户端连接配置中保存令牌原文；
4. 完整重写并重新加载 TOML；
5. 桌面 Client 再通过真实 loopback WebSocket 使用该令牌认证。

若已有本机令牌则直接复用。这样仍然完整经过 WebSocket 和认证，只是信任根来自"能启动并写入该实例 TOML 的本机用户"，无需再从同一进程的 `stderr` 抄配对码。

**用户回答（12:42:42）：** "嗯"

**已确认（12:42:47）：** 当 `server=true, client=true` 时，本机桌面 Client 自动建立永久配对；令牌原文保存于客户端配置，哈希保存于服务端授权配置，但实际通信仍完整经过 loopback WebSocket 和认证。

## 决策 65：同源 PWA 是否自动配对？

**AI 建议：** 不自动配对，仍使用 stderr 的 6 位配对码。原因是"由本机 S 托管页面"并不等于"访问页面的人可信"。只要站点可从局域网或公网访问，任何能打开 PWA 的浏览器都属于新客户端。与同进程桌面 Client 不同，服务端无法证明浏览器由启动万象的本机用户控制。因此建议：
- 本机桌面 Client：同进程启动，可自动配对；
- PWA：无论是否同源，首次使用都必须输入 6 位配对码；
- 配对后永久令牌保存到该浏览器的 IndexedDB；
- 后续同源自动连接时直接使用已有令牌；
- IndexedDB 被清除后重新配对。

**用户回答（12:42:59）：** "嗯"

**已确认（12:43:07）：** **同源 PWA 不自动配对**。首次连接仍需输入 `stderr` 输出的 6 位配对码；成功后，永久令牌保存在该浏览器的 IndexedDB 中。
# 31. TLS 与监听端点

> 覆盖对话决策 66（两个子决策）、67（两个子决策）。会话时间：2026-08-01 12:43–12:45。

## 决策 66（第一问）：万象 S 是否自己终止 TLS？

**AI 建议：** 首版不内置公网 TLS 证书管理。建议边界：
- loopback 本机连接允许 `ws://127.0.0.1` 或 Unix socket 上的 WebSocket；
- 非 loopback 监听默认只绑定内网地址，是否允许明文由 TOML 显式开启；
- 公网部署由 Caddy、Nginx、Traefik 等反向代理提供 HTTPS/WSS；
- 万象 S 接收代理转发后的 WebSocket；
- 不在首版处理 ACME、证书续期、私钥权限、SNI 与多域名；
- PWA 若通过 HTTPS 加载，只能连接 `wss://`，避免浏览器混合内容限制。

这样能保持单进程业务架构简单，同时不把证书生命周期管理引入首版。

**用户回答（12:43:21）：** "未来用 caddy"

**已确认（12:43:30）：** 首版不内置 TLS 与证书管理；未来公网入口统一交给 **Caddy**，万象只提供其后的 HTTP/WebSocket 服务。

## 决策 66（第二问）：默认监听地址

**AI 建议：** 默认只监听 loopback。

```toml
[network]
listen = "127.0.0.1:8765"
```

这样：本机 Client 可直接连接；Caddy 可反向代理到该地址；未配置 Caddy 时，不会意外向局域网或公网暴露；需要局域网直连时，用户必须显式改为 `0.0.0.0:8765` 或指定网卡地址；`server` 的 WebSocket 与 `pwa` 的静态资源共用该监听器；只有 `pwa-only` 时，该监听器只提供静态资源。

**用户回答（12:43:45）：** "嗯"

**已确认（12:43:50）：** 默认只绑定 `127.0.0.1`；任何非本机监听都必须在 TOML 中显式配置。

## 决策 67（第一问）：`server` 与 `pwa` 是否共用监听端点？

**AI 建议：** 分别配置监听端点，但允许默认合并。例如：

```toml
[network.server]
listen = "127.0.0.1:8765"

[network.pwa]
listen = "127.0.0.1:8765"
```

两者地址完全相同时，由同一个 HTTP listener 同时提供 WebSocket 与 PWA 静态资源；地址不同时，则分别监听。这样能自然覆盖 `server + pwa` 同源部署、`server-only`、`pwa-only`、PWA 对外开放而 S 仅留在 loopback 后由 Caddy 代理等部署方式。

**用户回答（12:44:57）：** "就同一个地址，只不过功能可以开启关闭而已"

**已确认（12:45:02）：** `server` 与 `pwa` **永久共用同一个监听地址和 HTTP listener**，不存在两套网络配置。
- `server=true`：启用业务 WebSocket 路由；
- `pwa=true`：启用 PWA 静态资源路由；
- 两者都启用：同源提供 PWA 与 WebSocket；
- 只启用其中之一：同一 listener 只开放相应功能；
- 两者都关闭：不创建 listener。

## 决策 67（第二问）：业务 WebSocket 使用固定路径

**AI 建议：** 固定为 `/ws`，PWA 使用根路径 `/`：

```text
GET /                  PWA 入口与静态资源
GET /ws                WebSocket Upgrade
GET /manifest.webmanifest
GET /service-worker.js
```

未启用的功能直接返回 `404`，不返回"当前模式已关闭"等额外信息。这样 Caddy、PWA 同源连接和客户端配置都只需要一个基础地址。

**用户回答（12:45:18）：** "可以"

**已确认（12:45:25）固定路由：**
- `/`：PWA 与静态资源；
- `/ws`：唯一业务 WebSocket 端点；
- 功能未启用时对应路由直接返回 `404`。
# 32. WebSocket 帧格式与版本协商

> 覆盖对话决策 68、69、70（两个子决策）。会话时间：2026-08-01 12:46–12:48。

## 决策 68：WebSocket 帧采用文本 JSON 还是二进制格式？

**AI 建议：** 首版只使用 UTF-8 JSON 文本帧。规则建议为：
- 每个实时协议事件占一个 WebSocket message；
- 不依赖换行分隔，也不把多个事件拼接进一个文本帧；
- 追赶历史时允许一个事件携带 `items` 数组，作为有限批次；
- Agent 消息 payload 直接嵌入 JSON，不二次转义为字符串；
- 首版不支持 MessagePack、CBOR、Protobuf 或自定义二进制编码；
- 收到二进制帧即视为协议错误并关闭连接；
- 未来若数据量证明 JSON 成为瓶颈，再通过明确的协议版本协商增加二进制编码。

这样最符合"透明映射"、易调试以及 `stderr` 忠实诊断的目标，也避免首版同时维护两套序列化格式。

**用户回答（12:46:33）：** "好的"

**已确认（12:46:38）：** 首版 `/ws` 只使用 UTF-8 JSON 文本帧；每个协议事件对应一个 WebSocket message，不接受二进制帧。

## 决策 69：WebSocket 协议如何处理版本不兼容？

**AI 建议：** 连接建立后、认证之前，双方先交换一个最小协议声明；只接受完全匹配的主版本。例如服务端首先发送：

```json
{
  "type": "protocol.hello",
  "protocol": "wanxiang",
  "major": 1,
  "minor": 0,
  "instanceId": "..."
}
```

客户端随后发送自己的 `protocol.hello`，再进入认证或配对流程。建议规则：
- `major` 不同：立即关闭连接；
- `major` 相同、`minor` 不同：允许连接，未知事件类型忽略；
- 协议版本不写入每一帧，避免重复；
- 不依赖 PWA 静态资源版本推断协议兼容性，因为 client-only 可以连接远端不同版本的 S；
- 关闭原因只说明协议不兼容，不泄漏认证或实例内部信息。

**用户回答（12:46:55）：** "好的"

**已确认（12:47:01）协议握手：**
- WebSocket 建立后，双方先交换 `protocol.hello`；
- 握手先于认证与配对；
- 服务端声明 `instanceId`；
- 主版本不一致立即断开；
- 主版本一致时允许次版本不同。

## 决策 70（第一问）：未知事件能否一律忽略？

**AI 建议：** 不能一律忽略；事件外壳增加 `required`：

```json
{
  "type": "conversation.message-committed",
  "required": true,
  "payload": {}
}
```

处理规则：
- 已知事件：正常处理；
- 未知且 `required=false`：忽略，连接继续；
- 未知且 `required=true`：立即关闭连接，要求升级客户端或服务端；
- 所有会改变权威状态、认证状态、游标或写权限的事件必须为 `required=true`；
- 纯提示、可选 UI 元数据、未来增强信息可为 `required=false`；
- 默认值应为 `true`，避免发送方忘记标记时被接收方静默忽略。

否则旧客户端若忽略新的关键事件，可能仍以为自己已经追平并获得错误写权限。

**用户回答（12:47:56）：** "不，我认为 hello 时候版本号只有1个，版本不够就提示升级"

**已确认（12:48:04）协议版本策略：**
- `protocol.hello` 只携带一个整数版本号，例如 `version: 1`；
- 客户端与服务端版本必须**完全相等**；
- 不划分 major/minor；
- 不引入 `required` 字段；
- 不支持同版本内"忽略未知事件"的兼容策略；
- 版本一致却收到未知事件，视为实现错误或协议违规，记录到 `stderr` 并关闭连接。

## 决策 70（第二问）：版本不匹配时如何提示升级？

**AI 建议：** 关闭连接前发送一条最小事件：

```json
{
  "type": "protocol.upgrade-required",
  "serverVersion": 2,
  "clientVersion": 1
}
```

客户端收到后展示升级界面，然后服务端关闭连接。该事件发生在认证前，因此不附带配置、下载地址或其他实例信息。

**用户回答（12:48:17）：** "好的"

**已确认（12:48:34）：** 协议版本不匹配时，服务端先发送 `protocol.upgrade-required`，随后关闭 WebSocket；客户端展示升级提示。协议只使用一个整数版本，双方必须完全一致。
# 33. 附件传输与内容寻址

> 覆盖对话决策 71、72。会话时间：2026-08-01 12:57。

## 决策 71：附件如何通过单一 WebSocket 传输？

目前协议只允许 JSON 文本帧，但图片、文档等附件可能很大。考虑到已经确定"一个连接处理一切"，建议：
- 附件仍走同一 WebSocket；
- 使用多个 fire-and-forget JSON 事件分块传输；
- 每个附件拥有 `attachmentId`；
- 二进制内容按固定大小切块后 Base64 编码；
- 顺序发送 `attachment.begin`、多个 `attachment.chunk`、`attachment.complete`；
- 服务端在 `complete` 后校验总长度与 SHA-256，成功后才允许消息引用该附件；
- 未完成上传只保存在临时文件中，不写 NDJSON；
- 连接断开后删除未完成附件，不支持断点续传；
- 完成后的附件作为独立文件永久保存，NDJSON 消息只记录引用和元数据，不把大文件嵌入事件日志。

这样维持了文本 JSON 和单连接约束，同时避免 NDJSON 被 Base64 大对象撑大。

**用户回答（12:57:09）：** "可以"

**已确认（12:57:14）附件传输与持久化边界：**
- 附件经同一 WebSocket 使用 JSON 分块事件上传；
- 分块内容使用 Base64；
- 上传完成前只存在于临时文件；
- 完成时校验总长度与 SHA-256；
- 断线时删除未完成上传；
- 首版不支持断点续传；
- 完整附件独立于 NDJSON 保存；
- NDJSON 中只保存附件引用与元数据。

## 决策 72：附件文件如何命名和去重？

**AI 建议：** 使用内容寻址存储，以 SHA-256 作为永久文件标识。例如：

```text
attachments/
  ab/
    cd/
      abcdef...完整哈希
```

消息中的附件引用记录：

```json
{
  "sha256": "abcdef...",
  "size": 123456,
  "mediaType": "image/png",
  "fileName": "photo.png"
}
```

相同内容只保存一份；原始文件名仅作为展示元数据，不参与磁盘路径。这样可以避免路径注入、文件名冲突和重复上传，并能在 `attachment.complete` 时直接判断文件是否已存在。

**用户回答（12:57:25）：** "可以"

**已确认（12:57:32）：** 附件采用 SHA-256 内容寻址存储：
- 哈希作为永久附件标识与磁盘路径依据；
- 相同内容只保存一份；
- 原始文件名仅作为展示元数据；
- NDJSON 只记录附件引用，不内嵌文件内容。
# 34. 附件清理与删除记账

> 覆盖对话决策 73（两个子决策）。会话时间：2026-08-01 12:58。

## 决策 73（第一问）：无引用附件如何清理？

**AI 建议：** 正常运行时不自动删除；仅由 `doctor --fix` 清理。`doctor --fix` 通过完整 replay 构建当前投影，收集所有仍被消息引用的 SHA-256，然后删除：不被任何有效消息引用的永久附件；超过短暂宽限期的临时上传文件；文件名与实际内容哈希不一致的附件。建议为永久孤儿设置 **24 小时宽限期**，避免"附件已完成保存，但引用消息尚未来得及记账"时被另一个修复进程立即删除。正常运行中的消息删除、会话删除只改变引用关系，不立即碰磁盘文件。

**用户回答（12:58:05）：** "垃圾回收未来有了压缩功能在压缩时处理"

**已确认（12:58:13）：**
- 首版**不执行附件垃圾回收**；
- 未被消息引用、引用后来被删除、或上传完成但最终未记账的内容寻址附件，均可继续留在磁盘；
- 未来引入日志压缩时，由压缩流程根据压缩后的有效状态计算活跃附件集合，并同步清理不可达附件；
- `doctor --fix` 首版不负责附件 GC。

## 决策 73（第二问）：删除消息或会话如何记账？

**AI 建议：** 只追加删除事件，不修改或删除历史 NDJSON。例如删除一条消息：

```json
{
  "type": "message.deleted",
  "version": 1,
  "conversationId": "...",
  "messageId": "..."
}
```

删除会话同理。内存投影和客户端 snapshot 不再展示已删除对象，但旧消息事件仍保留在历史日志中；未来压缩时才真正从新的基线状态中移除。这意味着：
- "删除"是逻辑删除；
- replay 可以确定性恢复删除后的当前状态；
- 不需要跨多个 UTC 文件搜索并改写旧记录；
- 被删除消息引用的附件暂不回收，留待未来压缩处理。

**用户回答（12:58:28）：** "可以"

**已确认（12:58:36）：** 删除消息或会话采用追加式 tombstone；历史 NDJSON 永不原地修改，附件清理由未来压缩阶段处理。
# 35. 编辑即 fork

> 覆盖对话决策 74（两个子决策）、75、76、77。会话时间：2026-08-01 12:59–13:00。

## 决策 74（第一问）：编辑消息如何记账？

**AI 建议：** 编辑等价于"追加新完整消息 + 替换旧消息"：
- 新内容按 Agent Framework 的完整消息 JSON 单独记账；
- 再由应用级外壳声明它替换哪个旧消息；
- 旧消息仍保留在历史日志中，但当前投影只展示新版本；
- 每次再次编辑都生成新消息，不原地修改旧 payload；
- Provider 上下文只使用当前选中的版本；
- 未来压缩时才丢弃不可达的旧版本。

**用户回答（12:59:08）：** "编辑就是 fork 了整个对话"

**已修正（12:59:19）：** **消息编辑不是产生同一会话内的新版本，而是从该位置 fork 出一条新对话。** 具体语义：
- 原对话完全不变；
- 新建一个 `conversationId`；
- 继承被编辑消息之前的历史；
- 用编辑后的完整消息替代原消息；
- 原消息之后的所有消息不进入新分支；
- 新分支从编辑后的消息继续生成；
- 删除、继续对话、再次编辑都只影响各自分支。

## 决策 74（第二问）：fork 时是否复制历史消息？

**AI 建议：** 不复制，采用结构共享。新对话只记录：

```text
parentConversationId
forkPoint
编辑后的新消息
后续新增消息
```

`forkPoint` 应锚定到父对话中一个确定的历史位置，而不是父对话"当前可见状态"。查询新对话时，读取父分支截至该位置的不可变前缀，再拼接新分支消息。这样：fork 是 O(1) 创建；不重复写入大量 Agent 消息 JSON；原分支后续新增或删除不会改变已经 fork 出去的历史前缀；未来压缩时，只要保留所有分支仍可达的共享前缀即可。

**用户回答（12:59:42）：** "同意"

**已确认（12:59:50）：** fork 采用结构共享，新对话只保存父对话引用、固定 fork 点、替换后的消息及后续新增消息；不复制父分支历史。

## 决策 75：fork 点如何定位？

**AI 建议：** 使用父对话中最后一条被继承消息的全局 NDJSON `id`。例如编辑消息 `id=120`，新分支继承到它之前，则记录：

```json
{
  "parentConversationId": "...",
  "forkAfterId": 119
}
```

若编辑第一条消息，则 `forkAfterId` 为空。这样 fork 点稳定、唯一，不受父对话后续删除、重命名或新增消息影响，也无需额外设计消息版本号。

**用户回答（12:59:58）：** "可以"

**已确认（13:00:05）：** fork 点使用父对话中最后一条被继承消息的全局提交 `id`。新分支的历史前缀因此由 `parentConversationId + forkAfterId` 唯一确定。

## 决策 76：父对话删除后，子分支是否仍然可用？

**AI 建议：** 仍然可用。删除父对话只应影响它自身在当前投影中的可见性，不能破坏已经 fork 出去的子分支。否则结构共享会让删除操作产生级联数据损坏。因此：
- 父对话 tombstone 后，不再显示在普通会话列表；
- 子分支仍能读取父对话截至 `forkAfterId` 的历史前缀；
- 父对话的底层消息事件不能因逻辑删除而从 replay 投影中彻底丢弃；
- 未来压缩时，只要仍有子分支引用该前缀，就必须保留这部分数据；
- 删除父对话不自动删除其子分支。

**用户回答（13:00:16）：** "可以"

**已确认（13:00:27）：** 父对话逻辑删除后，子分支仍完整可用；父分支的共享历史前缀只要仍被任何子分支引用，就必须继续参与 replay，并在未来压缩时保留。

## 决策 77：fork 创建与编辑后消息是否原子提交？

**AI 建议：** 必须是同一次 NDJSON 原子提交。一次"编辑并 fork"命令应在同一行提交中包含：
- 创建新的 `conversationId`；
- 记录 `parentConversationId`；
- 记录 `forkAfterId`；
- 写入编辑后的完整消息；
- 写入新对话的初始元数据。

只有整条记录完成 `flush` 后，新分支才存在。这样不会出现只有空分支却没有编辑消息，或消息已经存在但分支关系缺失的中间状态。

**用户回答（13:00:36）：** "可以"

**已确认（13:00:49）：** fork 元数据、初始配置和编辑后的第一条完整消息必须位于同一条 NDJSON 原子提交中，完成 `flush` 后新分支才正式存在。
# 36. fork 语义深化

> 覆盖对话决策 78、79、80、81（两个子决策）、82。会话时间：2026-08-01 13:00–13:03。

## 决策 78：是否允许多层 fork？

**AI 建议：** 允许任意深度的 fork 链。例如：

```text
A
└─ B（从 A fork）
   └─ C（从 B fork）
      └─ D（从 C fork）
```

规则建议为：
- 新分支只引用它的**直接父对话**；
- 不复制或扁平化祖先消息；
- 创建 fork 时，父对话必须已经存在；
- `forkAfterId` 必须指向父对话当前可见历史中的有效消息提交；
- 父关系创建后不可修改，因此不会形成循环；
- 查询时递归展开祖先前缀，内存中可缓存展开结果；
- 未来压缩功能负责把过深的共享结构重新整理，而不是首版限制 fork 深度。

**用户回答（13:00:58）：** "可以"

**已确认（13:01:07）：** 允许任意深度的不可变 fork 父链；新分支只引用直接父对话，不复制或扁平化祖先历史。

## 决策 79：消息是否需要独立 `messageId`？

**AI 建议：** 不需要。直接使用记录该消息的全局提交 `id` 作为应用级消息标识。理由：
- 每条完整消息本来就单独记账，因此天然拥有唯一且永久的提交 `id`；
- fork 点已经使用该 `id`；
- 删除、引用、回复、编辑 fork、附件归属都可以引用同一个值；
- 避免同时维护 `messageId` 与 `commitId` 两套身份；
- Agent Framework 原始 payload 仍保持透明，其内部可能存在的 ID 不作为万象的权威标识；
- 一个原子提交若包含 fork 元数据和第一条消息，该提交 `id` 就是该消息的标识。

例外是一个提交包含多条消息，但此前已经确定**每条完整消息分别触发记账**；因此普通消息提交始终只含一条消息。

**用户回答（13:01:17）：** "可以"

**已确认（13:01:29）：** 每条完整消息直接使用其全局 NDJSON 提交 `id` 作为消息标识，不额外生成 `messageId`。

## 决策 80：fork 后父分支的后续删除是否影响子分支？

**AI 建议：** 不影响。子分支继承的是 fork 创建时的历史快照，而不是父分支的实时投影。否则会出现这种问题：

```text
A 中有消息 10、11、12
B 从 A 的消息 12 之前 fork
之后 A 删除消息 11
```

若 B 每次都读取 A 的当前状态，B 的历史也会被间接改变，这违背"分支创建后不可变"的语义。因此 fork 元数据除了 `parentConversationId`、`forkAfterId`，还应隐含或显式保存一个时间边界：`forkBaseCommitId = 创建该 fork 的提交 id`。解析子分支历史时，只考虑父分支在 `forkBaseCommitId` 时已经发生的事件；父分支之后的新增、删除或其他 tombstone 都不影响子分支。

**用户回答（13:01:41）：** "可以"

**已确认（13:01:48）：** fork 继承的是**创建时快照**。因此子分支展开父历史时：
- 只考虑 `forkBaseCommitId` 及之前已经成立的父分支状态；
- 父分支之后新增、删除、再次 fork 或其他变化，都不影响既有子分支；
- 子分支一旦创建，其继承前缀即固定不变。

## 决策 81（第一问）：是否显式保存 `forkBaseCommitId`？

**AI 建议：** 显式保存。虽然它通常等于"创建 fork 的那次提交 `id`"，但明确写入元数据更稳妥：

```json
{
  "parentConversationId": "...",
  "forkAfterId": 120,
  "forkBaseCommitId": 1843
}
```

这样查询层无需依赖事件外壳位置推导语义，未来压缩或迁移后也能保持清晰。

**用户回答（13:02:23）：** "强制等于“创建 fork 的那次提交 id”，SSOT"

**已确认（13:02:33）：** **不单独保存 `forkBaseCommitId`**。它强制等于创建 fork 的那条外层提交 `id`，以提交外壳作为唯一事实来源：

```json
{
  "id": 1843,
  "events": [
    {
      "type": "conversation.forked",
      "parentConversationId": "...",
      "forkAfterId": 120
    },
    {
      "type": "agent-message-recorded",
      "conversationId": "...",
      "payload": {}
    }
  ]
}
```

查询层将 `1843` 直接解释为该 fork 的快照边界；事件内部不重复记录同一个值。单写者先分配提交 `id`，再构造并序列化整条原子提交。

## 决策 81（第二问）：fork 是否继承父对话的配置？

**AI 建议：** 继承 fork 创建时父对话的全部会话级配置快照。包括所选模型、系统指令、启用的工具和其他影响后续生成的会话设置；新分支创建后，这些配置独立演化，父分支之后的配置修改不影响它。

**用户回答（13:02:49）：** "同意"

**已确认（13:02:58）：** fork 创建时，父对话的会话级配置按当时状态复制到新分支；此后父子分支各自独立演化。

## 决策 82：fork 是否包含未提交的运行时状态？

**AI 建议：** 不包含。fork 只能基于当时已经提交到 NDJSON 的状态：
- 已提交的完整消息；
- 已生效的会话级配置；
- 已提交的附件引用；
- 已提交的删除或其他投影变化。

以下内容不进入新分支：
- 正在流式生成但尚未形成完整消息的内容；
- 仍在内存队列中的用户消息；
- 尚未完成的附件上传；
- 当前 Provider 调用、Tool 执行或其他运行时任务；
- 连接观察关系、游标等传输状态。

若父对话正在生成，用户仍可 fork，但新分支只能看到 fork 提交发生前的最后一个已提交状态。

**用户回答（13:03:11）：** "同意"

**已确认（13:03:23）：** fork 只复制**已提交状态**，不包含流式增量、排队消息、未完成上传、Provider/Tool 运行状态、观察关系或连接游标。
# 37. 会话生命周期

> 覆盖对话决策 83、84、85、86、87。会话时间：2026-08-01 13:04–13:07。

## 决策 83：`conversationId` 由客户端还是服务端生成？

**AI 建议：** 由发起创建或 fork 的客户端预先生成 UUIDv7，服务端负责校验并接受。原因：
- 协议是 fire-and-forget，客户端无需等待服务端分配 ID，便可立即建立临时 UI 状态；
- 网络重试复用相同 `invocationId`、`commandId` 和 `conversationId`，不会创建重复会话；
- fork 命令可以在一次原子提交中直接包含确定的新 `conversationId`；
- 其他同批附件、配置或后续动作可以提前引用该 ID；
- 服务端仍检查格式、是否已占用，以及相同 ID 是否绑定同一幂等命令；
- UUIDv7 的碰撞概率足够低，不需要中心化序列分配。

若该命令最终被拒绝，客户端删除对应的临时 UI 状态即可。

**用户回答（13:04:49）：** "嗯"

**已确认（13:05:02）：** 新建会话与 fork 的 `conversationId` 都由发起客户端预先生成 UUIDv7；服务端负责格式、占用和幂等校验。

## 决策 84：新会话创建是否必须和第一条消息原子提交？

**AI 建议：** 不强制绑定。先单独提交 `conversation.created`，使空会话也可以拥有会话级配置；第一条用户消息仍按正常规则，在真正到达可插入点时单独记账。这样用户可以先选择模型、指令和工具，再发送第一条消息。代价是可能留下没有消息的空会话，删除时按普通 tombstone 处理即可。

**用户回答（13:05:11）：** "嗯"

**已确认（13:05:17）：** 新会话可以先独立提交为**空会话**，第一条用户消息之后在实际插入点单独记账。这样允许先配置模型、指令和工具，再开始对话。

## 决策 85：创建空会话时保存哪些配置？

**AI 建议：** 提交完整的会话级配置快照，而不是只保存相对于全局配置的差异。`conversation.created` 至少包含当时生效的：
- 模型与 Provider 选择；
- 系统指令；
- 工具启用状态；
- 推理与生成参数；
- 其他会影响后续上下文或生成结果的会话设置。

全局默认配置之后发生变化，不应静默改变已经创建的会话；只有用户明确执行"应用新默认值"或修改会话配置，才追加新的配置事件。这样能保证 replay 后，同一会话的行为不依赖当前 TOML 中已经变化的默认值。

**用户回答（13:06:58）：** "好的"

**已确认（13:07:11）：** 创建会话时固化当时生效的完整会话级配置快照；之后 TOML 中的全局默认值变化，不会自动影响既有会话。

## 决策 86：修改会话配置时记录补丁还是完整快照？

**AI 建议：** 每次修改都记录修改后的完整会话配置快照。这样 replay 时只需用最新快照覆盖旧配置，不需要按顺序解释补丁、处理字段删除或依赖当前默认值。代价是配置数据会有少量重复，但相较消息内容通常很小，也更符合当前优先简单、确定性的设计。

**用户回答（13:07:23）：** "好的"

**已确认（13:07:37）：** 每次会话配置变更都追加一份**完整配置快照**，不记录字段级 patch；replay 时以最新快照覆盖此前状态。

## 决策 87：配置修改何时影响正在进行的生成？

**AI 建议：** 只影响下一次 Provider 调用，不影响已经发出的调用。也就是说：
- 当前模型请求已经发出后，修改模型、系统指令、工具或推理参数，不取消也不篡改该请求；
- 新配置先独立记账并生效；
- 当前请求返回的完整消息仍按原调用时的配置记账；
- 到达下一个可插入点、Tool Result 返回或会话重新 idle 后，下一次 Provider 调用使用最新配置；
- 如用户希望立即应用新配置，应先显式取消当前生成。

这样每次外部调用都有稳定输入，不会产生"请求中途换配置但结果归属不明"的语义。

**用户回答（13:07:46）：** "同意"

**已确认（13:07:54）：** 会话配置变更只影响**尚未发起的下一次 Provider 调用**；已经发出的调用继续按其启动时配置完成并记账。
# 38. 生成取消与 generationId

> 覆盖对话决策 88、89、90、91、92。会话时间：2026-08-01 13:25–13:27。

## 决策 88：取消生成的语义

**AI 建议：** 取消只停止当前正在进行的 Provider／Tool 流程，不回滚任何已提交消息。具体语义：
- 已提交的用户消息、思考消息、Tool Call、Tool Result 都保留；
- 尚未形成完整消息的流式内容直接丢弃；
- 内存中排队但尚未插入的用户消息继续保留，等待下一个插入点；
- 取消完成后，会话进入下一个可插入点；
- 不额外写入"已取消"消息，除非上游实际产生了一条完整的取消相关消息；
- 取消本身不进入 NDJSON，因为它只改变运行时任务，不改变已提交历史。

**用户回答（13:25:04）：** "好的"

**已确认（13:25:18）：** 取消生成不记账，只终止当前运行；已经提交的消息保留，未完成的流式内容丢弃，排队消息继续等待下一个插入点。

## 决策 89：取消与"迟到的完整结果"发生竞争时如何处理？

**AI 建议：** 区分 Provider 输出与 Tool Result：
- 取消已被运行时接受后，来自该次 Provider 调用的迟到助手消息、思考消息，即使随后完整返回，也直接丢弃，不记账。
- Tool 已经实际执行并返回完整结果时，仍将 Tool Result 单独记账，即使取消请求更早到达。因为工具可能已经产生外部副作用，保留结果比假装它没有发生更忠实。
- 若 Tool 成功取消且没有完整结果，则不补写任何消息。
- 取消判断只看该次运行的内存状态，不增加取消事件。

**用户回答（13:25:38）：** "嗯"

**已确认（13:25:56）取消竞争语义：**
- 取消生效后，迟到的 Provider 完整输出不记账；
- 已经实际完成的 Tool Result 仍单独记账；
- Tool 成功取消且没有完整结果时，不补写消息；
- 取消本身不进入 NDJSON。

## 决策 90：是否需要 `generationId`？

**AI 建议：需要。** 它是领域标识，不是此前否决的传输层 `streamId`。每次开始新的 Provider/Tool 循环时，由服务端生成 UUIDv7 `generationId`。所有临时流式事件携带：

```json
{
  "type": "generation.delta",
  "conversationId": "...",
  "generationId": "...",
  "payload": {}
}
```

作用是：
- 取消旧生成并快速开始新生成后，客户端能丢弃旧生成的迟到 delta；
- 多客户端观察同一对话时，都能识别当前运行；
- `generation.cancel` 可以明确取消某次生成，避免误取消刚刚启动的新生成；
- 它只存在于运行时和 WebSocket 事件中，不写入 NDJSON；
- 完整消息一旦记账，仍只以提交 `id` 作为消息身份。

**用户回答（13:26:15）：** "嗯"

**已确认（13:26:26）：**
- 每次生成流程拥有临时 UUIDv7 `generationId`；
- 用于关联流式增量、当前运行状态与精确取消；
- 不写入 NDJSON；
- 完整消息仍以全局提交 `id` 作为永久身份。

## 决策 91：一次 Tool 循环是否沿用同一个 `generationId`？

**AI 建议：** 沿用。一次插入批次触发一个 `generationId`，它覆盖整个连续执行过程：

```text
插入一批用户消息
→ Provider 输出思考或 Tool Call
→ Tool 执行并返回结果
→ 再次调用 Provider
→ 最终助手消息
→ idle
```

建议规则：
- Provider 与 Tool 之间往返时不生成新的 `generationId`；
- 同一流程中的所有临时 delta 都携带同一个 `generationId`；
- 最终进入 `idle`、失败或取消时，该 `generationId` 结束；
- 下一个插入点排空新一批消息并启动新流程时，才生成新的 `generationId`；
- 已排队消息在 Tool Result 返回的插入点被加入当前上下文时，仍属于当前 `generationId`，因为它延续的是同一条正在运行的 Agent 循环。

**用户回答（13:26:38）：** "嗯"

**已确认（13:26:53）：** 一个 `generationId` 覆盖整段连续的 Provider／Tool 循环，直到进入 `idle`、失败或取消；Tool 往返及中途插入的排队消息仍属于同一次生成。

## 决策 92：一次响应包含多个 Tool Call 时，是否并行执行？

**AI 建议：** 并行执行同一批 Tool Call，全部完成后再统一返回 Provider。语义如下：
- Provider 一次完整消息中产生多个 Tool Call，先将每个完整 Tool Call 分别记账；
- 各 Tool 独立并行执行；
- 每个完整 Tool Result 一完成就分别记账并实时推送给客户端；
- 只有这一批 Tool Call 全部得到结果、错误或取消结果后，才到达下一次 Provider 调用的插入点；
- 排队的用户消息也在此时一次性 FIFO 插入；
- Tool Result 传回 Provider 时保持原 Tool Call 顺序，不按完成时间重排。

这样既利用并行能力，又避免某个先完成的工具立即触发下一轮 LLM，导致同批其他工具结果被遗漏。

**用户回答（13:27:08）：** "那当然"

**已确认（13:27:15）：**
- 同一次 Provider 响应中的多个 Tool Call 并行执行；
- 每条完整 Tool Call 分别记账；
- 每条完整 Tool Result 完成后立即分别记账并推送；
- 必须等待整批 Tool Call 全部结束，才再次调用 Provider；
- 返回 Provider 时保持原 Tool Call 顺序，而不是完成顺序；
- 排队用户消息在这一批结束后的插入点统一加入。
# 39. Tool 执行

> 覆盖对话决策 93（两个子决策）、94。会话时间：2026-08-01 13:27–13:28。

## 决策 93（第一问）：需要用户批准的 Tool Call 由哪个客户端批准？

**AI 建议：** 会话级共享审批，任意已追平且正在观察该会话的授权客户端都可以决定，先到的有效决定胜出。语义如下：
- Tool Call 完整记账后进入"等待批准"的运行时状态；
- 所有观察该会话且相关投影已追平的客户端都会看到审批 UI；
- 任一客户端发送批准或拒绝事件后，服务端原子接受第一个有效决定；
- 后续其他客户端的决定视为过期并拒绝；
- 审批决定本身不必额外记账；批准后产生的 Tool Result 或拒绝结果按完整消息正常记账；
- 若当前没有客户端在线，则保持等待，不自动批准；
- 客户端断线不取消等待中的审批。

**用户回答（13:27:46）：** "第一版没有权限控制，全批准"

**已确认（13:27:55）首版 Tool 策略：**
- 不做权限控制；
- 不展示审批流程；
- 所有 Tool Call 自动批准并立即执行；
- 多客户端无需参与批准；
- Tool Call 与完整 Tool Result 仍分别记账；
- 未来若引入权限系统，再单独设计审批与授权模型。

## 决策 93（第二问）：Tool 在哪里执行？

**AI 建议：** 首版全部在万象 S 进程内执行，不做子进程隔离或容器沙箱。理由是首版已经选择单进程、全自动批准；此时再引入 worker 进程、IPC、超时回收和沙箱权限，会形成另一套复杂运行架构。建议边界：
- Tool 由 S 进程直接调用；
- 每次调用支持取消令牌与超时；
- Tool 异常转换为完整 Tool Result，而不是让进程崩溃；
- Tool 可以访问的文件、网络和系统资源由万象进程本身的 Linux 用户权限决定；
- 明确禁止 Tool 绕过单写者直接修改 NDJSON 或内存投影；
- 所有永久业务变化仍必须通过正式消息或命令进入单写者。

**用户回答（13:28:11）：** "对的"

**已确认（13:28:23）首版 Tool 执行模型：**
- 所有 Tool Call 自动批准；
- 全部在万象 S 进程内执行；
- 以运行万象进程的 Linux 用户权限作为唯一隔离边界；
- Tool 异常不得直接终止进程；
- Tool 不得绕过 NDJSON 单写者直接修改永久状态。

## 决策 94：Tool 是否强制设置超时？

**AI 建议：** 不设统一强制超时，由每个 Tool 自己决定是否配置超时。原因是 Tool 的性质差异很大：
- 普通 HTTP 查询适合短超时；
- 长时间构建、下载或远程任务可能合理地运行很久；
- 人为设置统一上限容易误杀合法操作；
- 用户仍可通过取消当前 `generationId` 请求终止支持取消的 Tool。

建议规则：
- Tool 定义可以声明可选超时；
- 未声明时无限等待，直到完成、进程退出或收到取消；
- 超时、取消和异常均转换成完整 Tool Result，并单独记账；
- 如果底层 Tool 不支持真正取消，取消后允许它在后台完成，但迟到结果仍按此前规则忠实记账。

**用户回答（13:28:36）：** "对的"

**已确认（13:28:46）Tool 超时策略：**
- 万象不设置统一强制超时；
- 每个 Tool 可独立声明超时，也可无限等待；
- 超时、异常与取消均形成完整 Tool Result，并分别记账；
- 底层 Tool 若无法真正取消，可继续在后台运行；迟到的完整 Tool Result 仍忠实记账。
# 40. Tool 来源与 MCP

> 覆盖对话决策 95、96、97、98、99、100。会话时间：2026-08-01 13:28–13:31。
> 决策 100 是对话中最后一题（用户确认 100 个问题已提完）。

## 决策 95：首版 Tool 的来源范围

**AI 建议：** 首版同时支持两类 Tool：
1. **进程内内置 Tool**：随万象编译发布，由代码注册。
2. **MCP Tool**：在 TOML 中配置远端或本地 MCP Server，由 S 连接并映射为运行时 Tool。

会话配置快照只保存稳定的 Tool 标识和当时参数，不复制 Tool 实现；每次 Provider 调用前，从当前运行时注册表解析这些标识。某个 Tool 当前不可用时，应向 Provider 返回完整失败 Tool Result，而不是悄悄移除或改写历史。

**用户回答（13:28:59）：** "对的"

**已确认（13:29:12）首版 Tool 来源：**
- 编译期注册的内置 Tool；
- TOML 配置的 MCP Tool；
- 会话配置快照只保存稳定 Tool 标识及参数；
- Tool 实现由运行时注册表解析；
- Tool 不可用时产生完整失败结果，不篡改历史。

## 决策 96：本地 MCP Server 如何运行？

**AI 建议：** 本地 MCP Server 作为万象的子进程运行，而不是加载进万象进程。建议语义：
- TOML 配置可执行文件、参数、工作目录和环境变量；
- 万象 S 负责启动、监视和关闭子进程；
- 使用标准输入/输出通信；
- 同一个 MCP 配置在整个 S 进程中共享一个实例，不为每段对话重复启动；
- 子进程退出后按需重新启动；
- MCP 子进程不能直接访问万象内存或 NDJSON，只能通过 Tool 协议返回结果；
- 远程 MCP 则直接连接配置的网络端点，不启动本地进程。

这不违背"Tool 在 S 中执行"的产品语义：Tool 调度和结果处理仍属于 S，只是第三方 MCP 实现拥有进程隔离。

**用户回答（13:29:25）：** "对的"

**已确认（13:29:34）本地 MCP 运行模型：**
- 本地 MCP Server 由万象 S 作为共享子进程启动、监视和关闭；
- 通过标准输入/输出通信；
- 同一 MCP 配置在整个 S 进程内共享，不按会话重复启动；
- 远程 MCP 通过网络连接；
- MCP 实现不能直接访问万象内存投影或 NDJSON，只能通过 Tool 协议返回结果。

## 决策 97：共享 MCP 子进程崩溃后如何重启？

**AI 建议：** 按需立即重启，但不自动重放失败中的 Tool Call。具体语义：
- 子进程退出时，将当前所有尚未完成的调用分别结束为失败 Tool Result，并正常记账；
- 新的 Tool Call 到达时，再启动一个新的 MCP 子进程；
- 不在无人调用时循环拉起；
- 不自动重新执行崩溃时的调用，避免有副作用的 Tool 被执行两次；
- 连续启动失败时仍按每次调用尝试，不引入全局熔断或指数退避；
- 启动、退出和协议异常完整写入 `stderr`。

**用户回答（13:29:44）：** "对的"

**已确认（13:29:53）本地 MCP 子进程恢复策略：**
- 调用时按需启动；
- 子进程崩溃时，进行中的调用分别形成失败 Tool Result 并记账；
- 后续调用再启动新子进程；
- 不自动重放失败调用；
- 不空闲常驻拉起；
- 启动、退出和协议异常写入 `stderr`。

## 决策 98：MCP 配置热更新如何影响正在运行的子进程？

**AI 建议：** 配置加载成功后立即切换注册表，但旧子进程进入排空状态。具体语义：
- 新 Tool Call 只使用新配置；
- 旧配置对应的 MCP 子进程不再接受新调用；
- 已经开始的调用允许继续完成；
- 旧进程没有进行中调用后立即关闭；
- 若配置被删除，同样先停止接收新调用，再等待已有调用结束；
- 不强制中断在途调用，避免已经产生副作用却丢失结果；
- 新配置首次被调用时再按需启动新子进程。

**用户回答（13:30:10）：** "对的"

**已确认（13:30:20）MCP 配置热更新语义：**
- 新调用立即使用新配置；
- 旧配置对应的 MCP 子进程停止接收新调用；
- 已在执行的调用允许完成；
- 旧进程排空后关闭；
- 新配置在首次调用时按需启动；
- 删除配置时采用同样的排空关闭逻辑。

## 决策 99：同一个 MCP Server 是否允许并发 Tool 调用？

**AI 建议：** 默认允许并发，由 MCP Server 自身处理；万象不在外层强制串行。具体语义：
- 同一批多个 Tool Call 可以并发发送给同一个 MCP Server；
- 每个调用有独立的 MCP request id；
- 返回顺序可以与发起顺序不同；
- 万象仍按原 Tool Call 顺序整理结果后再交回模型；
- 若某个 MCP 实现不支持并发，可在 TOML 中显式配置 `max_concurrency = 1`；
- 其他服务可配置更高并发数或不设限制；
- 并发限制应作用于单个 MCP 配置实例，而不是整个万象进程。

**用户回答（13:30:32）：** "对的"

**已确认（13:30:42）MCP 并发规则：**
- 同一 MCP Server 默认允许并发调用；
- 每个调用使用独立的 MCP request ID；
- 返回可以乱序，但交回 Provider 时保持原 Tool Call 顺序；
- TOML 可为每个 MCP 配置设置独立的 `max_concurrency`；
- 限制仅作用于该 MCP 配置实例。

## 决策 100：达到 MCP 并发上限后如何处理新调用？

**AI 建议：** 在内存中按 FIFO 等待，不立即失败，也不持久化队列。具体语义：
- 每个 MCP 配置拥有独立等待队列；
- 有并发名额释放时，按入队顺序启动调用；
- 等待中的调用仍属于当前 `generationId`；
- 用户取消该生成时，尚未启动的调用直接移出队列，并形成取消 Tool Result；
- S 进程崩溃时等待队列丢失，符合运行时状态不持久化的既定原则；
- TOML 降低并发上限时，不中断已运行调用，只限制后续启动；
- 配置排空时，已排队但尚未启动的旧配置调用仍按旧配置执行，避免一次 Provider 响应中的同批 Tool Call 被配置更新拆分。

**用户回答（13:31:59）：** "对的。你已经提了100个问题了。你根据我所有的回答，归纳反推出我决策的第一性原理，然后自动回答自己剩余问题，仅当置信度不够时候再问。"

**已确认（13:32:13）：** 每个 MCP 配置拥有独立的内存 FIFO 等待队列；达到并发上限后排队，不持久化、不立即失败。

**确认后的后续约定（13:32:13）：** 从现在开始，不再逐题征求确认。能由既有决策唯一或高置信度推出的事项，直接落定；只有原则发生冲突、涉及不可逆产品定位，或存在两个同样合理但用户体验明显不同的方案时才提问。
# 41. 反推出的第一性原理（12 条）

> 以下是根据前 100 个回答归纳出的设计哲学，并非原始文件的逐字内容。会话时间：2026-08-01 13:32。

## 1. 确定性优先于灵活性

系统应尽量只有一种明确解释：
- 一个永久写入入口；
- 一个全局连续提交序号；
- 一个配置事实来源；
- 一个真实网络通信路径；
- 完整快照优于补丁；
- 精确版本相等优于复杂兼容协商；
- 明确失败优于隐式猜测和自动修复。

当两个方案功能相近时，默认选择状态空间更小、replay 更容易、失败语义更明确的方案。

## 2. 只持久化已经成为事实的东西

永久事实必须满足：
1. 已形成完整业务对象；
2. 已经通过单写者；
3. 已追加并 `flush`；
4. 能从日志确定性重放。

因此：
- 完整消息持久化；
- 流式 delta 不持久化；
- 排队消息不持久化；
- observe、cursor、generation、取消状态不持久化；
- 正在运行的 Provider、Tool 和上传过程属于运行时；
- "已发送到客户端"不等于"客户端已应用"；
- "进入队列"不等于"发送成功"。

## 3. 追加历史，不改写过去

历史一旦发布便保持语义稳定：
- NDJSON 只追加；
- 删除使用 tombstone；
- 编辑通过 fork；
- 旧事件通过升级函数读取；
- 不重写历史；
- 父会话后续变化不影响已有分支；
- 附件先保留，未来压缩时再做可达性回收。

任何需要"修改旧事实"的需求，默认建模为新的事实。

## 4. 永久状态串行，外部工作并行

必须严格串行的只有永久提交临界区：

```text
分配 id
→ 序列化
→ append
→ flush
→ 更新投影
→ 发布权威变化
```

Provider、Tool、MCP、网络读取和附件传输可以并行。并行结果可以按完成顺序记账，但再次交给 Provider 时恢复原 Tool Call 顺序。

也就是：**并发计算，串行承诺。**

## 5. 不自动重复可能产生副作用的操作

只要执行结果存在歧义，就不能假设它没有发生：
- MCP 崩溃中的调用不自动重放；
- Tool 超时或断连不自动重试；
- Provider 失败不进行隐藏重试；
- 配置更新不重启在途调用；
- 取消无法真正停止 Tool 时，迟到的完整结果仍忠实记账。

重试必须是新的显式决策；只有明确属于同一次逻辑执行时才复用原 `invocationId`。

## 6. 权威状态不能被慢客户端拖住

客户端是观察者和命令发起者，不是服务端进度的一部分：
- 每条连接拥有独立 cursor；
- 慢客户端从自己的位置追赶；
- 追赶期间暂停不可重放的实时流；
- 慢客户端不阻塞 Agent、写入器或其他客户端；
- 写命令只检查它实际涉及的投影是否追平；
- 无关提交可以跨越。

默认采用局部相关性，而不是全局同步屏障。

## 7. 传输层保持薄，业务标识承担关联

WebSocket 只运输独立事件，不建设第二套 RPC 系统：
- 无通用 request/response；
- 无传输层 requestId；
- 无通用 streamId；
- 使用 `conversationId`、`invocationId`、`commandId`、`generationId` 等业务标识；
- 断线后重新观察权威状态，而不是重放所有网络帧。

只有确实属于运行时业务实体的标识才存在，例如临时 `generationId`。

## 8. 故障恢复以最长可信前缀为边界

系统不尝试猜测损坏内容的意图：
- 保留最长有效前缀；
- 从首个不可恢复点截断；
- 后续内容即使看似有效也不采用；
- 尾部提交失败时尽量截断并复用 ID；
- 无法截断时才考虑隔离；
- 既不能截断也不能隔离才崩溃。

恢复目标不是最大化抢救数据，而是恢复一个**确定、连续、可解释**的历史。

## 9. 配置必须经过磁盘才能成为现实

配置不是内存中的可变对象，而是磁盘 TOML 的投影：

```text
生成完整 TOML
→ 同目录临时文件
→ flush
→ 原子 rename
→ 重新读取正式文件
→ 完整解析和验证
→ 替换内存配置
```

因此：
- 外部编辑和应用内编辑走同一路径；
- 无局部应用；
- 无内存先改；
- 无普通 watcher 自动修复；
- 新配置影响新工作；
- 旧工作按旧配置排空。

## 10. 上游数据透明，应用元数据外置

Agent Framework 的完整消息 JSON 应原样保存。万象只在外层增加自身所需的：
- `conversationId`
- commit `id`
- 事件类型和版本
- 分支关系
- 配置快照
- 其他应用级元数据

不重新发明 Agent 消息模型，也不把展示层结构误当成永久领域模型。

## 11. 首版优先边界清晰，而不是能力完备

首版明确推迟：
- TLS 内建；
- RBAC 和审批；
- Snapshot；
- 压缩与附件 GC；
- Token 原地轮换；
- 通用帧重放；
- 上传续传；
- Provider 会话状态依赖；
- 多进程协调；
- 分布式一致性；
- 沙箱与复杂隔离。

默认规则是：非核心能力只有在缺失会破坏数据正确性时才进入首版。

## 12. 安全模型务实且明确

首版不是零信任平台，但边界不能含糊：
- 默认只监听 loopback；
- 非 loopback 必须显式配置；
- 永久 token 每客户端独立；
- 服务端只存 SHA-256；
- 本地同进程桌面端可自动配对；
- PWA 必须走配对；
- Tool 继承 Linux 用户权限；
- MCP 子进程提供进程边界；
- 不把密钥写入 NDJSON；
- 已知密钥不得出现在 stderr。

不声称拥有尚未实现的权限隔离能力。
# 42. 我将自动采用的后续判断规则（16 条）

> 对话原文（2026-08-01 13:32）。接下来未明确讨论的设计项，默认按这些规则落定：

- 独立工作可并行；永久写入必须经过单写者。
- 热更新只影响新工作；在途工作按旧配置排空。
- 队列默认内存 FIFO，不为运行时队列设计持久化恢复。
- 取消首先是合作式取消，不承诺终止不合作的外部代码。
- 不隐藏重试 Provider、Tool 或 MCP 调用。
- 运行失败通过 WebSocket 临时状态和 `stderr` 报告；除非形成完整 Agent 消息，否则不制造永久"错误消息"。
- 重复或冲突的稳定标识在配置加载时直接判为无效，不采用"最后一个覆盖"。
- Tool 和 MCP 使用稳定、带命名空间的标识，显示名称不作为身份。
- 配置降低并发上限时不终止已运行任务，只限制后续启动。
- 配置提高并发上限时立即从 FIFO 队列补充启动。
- 服务关闭时停止接收新工作，合作式取消运行时任务，排空单写者并 flush；未提交队列允许丢失。
- 后台任务异常不得成为无人观察的异常；写入 stderr，并转换为对应运行失败状态。
- 不增加独立数据库、缓存数据库、消息总线或内部 RPC 层。
- Doctor 只验证和修复可由既定规则确定的问题，不发明业务内容，不做附件 GC。
- 测试重点优先放在 replay、截断恢复、幂等、fork、cursor、配置原子切换和竞争条件，而不是 UI 快照数量。
- 任何优化都不能引入第二个事实来源。
# 43. 只有这些情况我还会提问（5 条）

> 对话原文（2026-08-01 13:32）。只会在下列情况停下来询问：

1. 两个选择都符合上述原则，但决定了明显不同的产品体验；
2. 涉及用户数据保留、隐私或不可逆删除策略；
3. 需要确定具体外部兼容标准，而现有回答不能推出；
4. 需要人为选择数值，例如默认大小上限、时间窗口或资源配额，且没有自然安全值；
5. 新需求与已经确认的原则直接冲突。

除此之外，直接给出自动决策及推导依据，不再累计"第 101、102 个问题"。这套原则延续了原始项目对 F#、Avalonia、Agent Framework、客户端/服务端结构以及 clean-room 实现边界的要求。
# 44. 自动问答 一：进程、启动与关闭（Q101–110）

> 第 101–200 项自动问答的一部分。答案以已确认的 100 项决策为主要依据；上传文件仅提供 F#、Avalonia、Agent Framework 与 C/S 架构的初始背景，后续 clean-room、NDJSON、WebSocket 等决定以对话为准。

**101. 问：收到 SIGTERM 后是否立即退出？**
答：不立即退出。停止接受新命令和新生成，取消运行时工作，排空单写者并 flush，随后退出。

**102. 问：优雅关闭是否设置内部固定超时？**
答：首版不设置全局固定超时。第一次终止信号进入优雅关闭，第二次终止信号立即退出；外部服务管理器可设置最终 kill 超时。

**103. 问：commit 已 flush，但投影更新前进程崩溃怎么办？**
答：commit 已经是事实。重启时从 NDJSON replay，重新得到投影，不回滚该 commit。

**104. 问：投影更新成功，但权威增量尚未发送时崩溃怎么办？**
答：客户端重连并重新 observe，从权威投影恢复；不需要重放旧 WebSocket 帧。

**105. 问：同一数据目录能否同时被两个 S 进程打开？**
答：不能。S 启动时获取数据目录独占锁；获取失败则启动失败，不尝试多写者协调。

**106. 问：两个完全独立的 S 如何共存？**
答：使用不同数据目录、不同 TOML、不同 `instanceId` 和不同监听地址，它们之间没有隐式关系。

**107. 问：客户端模式是否需要数据目录独占锁？**
答：不需要。纯客户端只读取自己的连接配置；只有启用 S、可能写永久业务状态的进程需要服务端数据锁。

**108. 问：系统时区变化是否影响日志分片？**
答：不影响。日志文件日期、commit 时间和所有永久时间均使用 UTC；本地时区只用于 UI 展示。

**109. 问：系统时钟向后跳时是否重新写入较早日期文件？**
答：不重新打开更早日期文件。当前日志日期不得小于最后持久化日志日期，避免更高 ID 出现在文件序列前部。

**110. 问：启动组件的顺序是什么？**
答：解析 CLI 和 TOML → 获取目录锁 → doctor/fix → replay → 建立投影 → 初始化配置注册表 → 启动监听器 → 启动客户端 UI。任何客户端连接都不能早于服务端就绪。
# 45. 自动问答 二：日志格式与恢复（Q111–120）

**111. 问：commit 外层至少包含哪些字段？**
答：`formatVersion`、全局 `id`、`committedAtUtc` 和有序 `events`。事件包含稳定 `type`、`version` 与 payload。

**112. 问：`committedAtUtc` 能否参与一致性判断？**
答：不能。它只用于审计和展示；顺序与身份只由全局 `id` 决定。

**113. 问：一个 commit 内的多个事件是否有顺序？**
答：有，数组顺序就是领域应用顺序，发布后不可改变。

**114. 问：多事件 commit 的投影更新是否原子？**
答：是。要么整条 commit 的全部事件成功应用，要么整条 commit 被截断、隔离或触发崩溃处理。

**115. 问：NDJSON 使用什么文本格式？**
答：UTF-8，无 BOM；每条 commit 序列化为一行，以 `\n` 结束。

**116. 问：事件 payload 是否要求统一字段顺序？**
答：日志本身不依赖字段顺序；只有生成 `commandId` 的规范化 payload 必须使用稳定 canonical JSON。

**117. 问：日志中是否允许非有限浮点数？**
答：不允许 `NaN`、正负无穷等非标准 JSON 值；命令验证阶段直接拒绝。

**118. 问：日志文件权限如何设置？**
答：创建时仅允许当前 Linux 用户读写；附件目录和 TOML 同样采用最小用户权限。

**119. 问：只读 doctor 检查什么？**
答：配置可读性、目录权限、实例锁、日志文件命名、ID 连续性、JSON/事件版本、投影 replay、附件引用与 blob 存在性，但不修改数据。

**120. 问：`doctor --fix` 可以做什么？**
答：只执行确定性修复：截断无效尾部、移除更晚日志内容、重建派生索引或临时文件。不得猜测业务内容、修改历史事件或清理未引用附件。
# 46. 自动问答 三：投影与查询（Q121–130）

**121. 问：投影是否持久化到独立数据库？**
答：不持久化。首版投影完全由 NDJSON replay 得到，避免第二事实来源。

**122. 问：投影类型升级是否需要迁移文件？**
答：不需要。修改 replay 与事件升级函数后重新构建投影。

**123. 问：普通查询是否直接扫描 NDJSON？**
答：不扫描。正常查询读取内存投影；日志扫描只用于启动、doctor、修复和按 cursor 补发。

**124. 问：conversation list 默认如何排序？**
答：按最后一次永久业务变更的全局 commit ID 降序；不使用可能回拨的时间戳排序。

**125. 问：会话摘要包含什么？**
答：`conversationId`、标题、最后可见消息摘要、最后 commit ID、删除状态、当前运行状态和有效配置摘要，不包含完整消息正文。

**126. 问：是否维护跨设备同步的"未读数"？**
答：首版不维护。未读依赖客户端阅读位置，会引入新的持久化用户状态；各客户端可以本地显示临时未读提示。

**127. 问：长会话如何查询历史？**
答：按全局消息 commit ID 反向分页；页面边界使用稳定 ID，不使用 offset。

**128. 问：消息显示顺序如何确定？**
答：按会话的有效历史结构以及消息全局 commit ID 排序；同一 atomic commit 内按事件顺序处理。

**129. 问：多层 fork 查询是否复制完整历史到内存？**
答：投影建立结构共享视图，沿不可变父链解析有效前缀；可以缓存结果，但缓存不是事实来源。

**130. 问：被 tombstone 的会话或消息是否从查询中物理消失？**
答：普通视图隐藏，审计和 fork 可达性逻辑仍能访问其历史事实。
# 47. 自动问答 四：Observe、快照与慢客户端（Q131–140）

**131. 问：重复 observe 同一 scope 怎么处理？**
答：幂等处理，替换该连接上相同 scope 的观察状态并重新发送当前快照。

**132. 问：客户端是否需要显式 unobserve？**
答：支持显式 unobserve，以便释放服务端状态；断线时也自动释放全部 observe。

**133. 问：同一连接能否观察多个会话？**
答：可以，每个会话是独立 scope，但共享该连接的全局 applied cursor。

**134. 问：大型快照是否放在一个巨大 WebSocket 消息中？**
答：不放。使用 `snapshot.begin`、若干 snapshot item、`snapshot.end`，全部以 scope 和 `lastCommitId` 关联。

**135. 问：客户端何时为快照发送 `cursor.advanced`？**
答：只有完整应用 `snapshot.end` 后，才能推进到该快照的 `lastCommitId`。

**136. 问：快照中途断线怎么办？**
答：丢弃未完成快照；重连后重新 observe，不做快照续传。

**137. 问：服务端发送队列是否无限增长？**
答：不能。每条连接只保留有限在途批次；一旦落后，就停止堆积权威增量，转为从 cursor 分批 catch-up。

**138. 问：落后期间的流式 delta 怎么办？**
答：直接丢弃。客户端追平后只恢复新的实时 delta，不补发旧 delta。

**139. 问：客户端长时间不推进 cursor 是否断开？**
答：不因"慢"而自动断开；只要连接活着且协议正常，服务端维持受限状态，不无限占用内存。

**140. 问：是否增加业务层 heartbeat 事件？**
答：不增加。使用 WebSocket/服务器基础设施的 ping、pong 和空闲检测，不污染业务协议。
# 48. 自动问答 五：命令、幂等与错误（Q141–150）

**141. 问：fire-and-forget 下客户端如何知道命令成功？**
答：服务端发送独立的 `command.committed` 权威事件，其中包含 `commandId`、`invocationId` 和产生的 commit ID。

**142. 问：命令在写入前是否发送 accepted？**
答：可以发送临时 `command.accepted`，但它不代表成功；只有 `command.committed` 才是永久成功。

**143. 问：命令失败使用什么格式？**
答：发送独立 `command.rejected`，包含稳定错误代码、面向用户的信息和可选诊断信息，不暴露密钥。

**144. 问：重复收到已提交的同一 `commandId` 怎么办？**
答：不再次执行，重新发送原提交结果，视为幂等成功。

**145. 问：同一 `commandId` 对应不同规范 payload 怎么办？**
答：视为严重实现或哈希冲突错误，写 stderr 并拒绝；绝不把它当作正常重复。

**146. 问：canonical JSON 如何处理对象字段？**
答：字段按稳定字典序排列；字符串和数字使用固定编码；数组保持业务顺序；忽略纯传输字段。

**147. 问：命令 schema 是否独立版本化？**
答：命令类型包含协议内固定语义；不在同一协议版本中悄悄改变。破坏性修改提升整体 protocol version。

**148. 问：客户端发送命令后立即断线，命令可能已提交怎么办？**
答：重连后通过投影和相同 `commandId` 查询得到结果；不得自动换新 invocation 重发。

**149. 问：客户端没有发送最新 cursor，但其实已展示内容，能否写？**
答：不能。服务端只信任已明确确认的 `cursor.advanced`，不推测客户端状态。

**150. 问：客户端离线时是否本地持久化待发送命令？**
答：首版不持久化。编辑器内容可保留在当前 UI 状态，但重启后的可靠离线发送队列不属于首版。
# 49. 自动问答 六：Provider 与生成（Q151–160）

**151. 问：Provider 定义来自哪里？**
答：全部来自 TOML，包括类型、端点、模型目录、密钥和网络参数；会话只冻结其行为配置快照。

**152. 问：Provider 配置热更新如何影响在途请求？**
答：在途请求使用启动时捕获的完整配置；新调用使用成功 reload 后的新配置。

**153. 问：Provider 被删除时，在途请求是否取消？**
答：不自动取消。允许完成；之后的新调用返回 Provider 不可用错误。

**154. 问：缺少 API key 时是否启动失败？**
答：只在该配置本身被声明为必需却无效时拒绝整份 TOML；某会话引用了后来不可用的 Provider，则生成时失败，不篡改会话配置。

**155. 问：模型名称是否由万象强验证？**
答：只验证结构和非空等本地规则；真实可用性由 Provider 调用决定，不主动依赖远程模型列表作为启动条件。

**156. 问：Provider 返回部分流式内容后失败，是否保存部分助手消息？**
答：不保存。部分 delta 消失，只记录运行失败状态；没有完整消息就没有永久消息事实。

**157. 问：Provider 调用是否自动重试网络错误？**
答：不自动重试。外部调用可能已经被处理，隐藏重试会产生重复副作用或费用。

**158. 问：Provider 自带 SDK 的内部透明重试怎么办？**
答：首版应尽量关闭或限制；无法关闭时必须明确记录其行为，不能在万象层再叠加重试。

**159. 问：Token usage 缺失时是否估算并持久化？**
答：不伪造 Provider usage。可在 UI 中显示本地估算，但必须标记为估算，并与原始消息 payload 分离。

**160. 问：一次 generation 失败后排队消息怎么办？**
答：保留在内存队列。会话进入失败/idle 状态后，可由下一次显式用户动作触发新的 generation。
# 50. 自动问答 七：Tool 与 MCP（Q161–170）

**161. 问：Tool 的稳定身份如何命名？**
答：使用命名空间化稳定 ID，例如 `builtin:file.read`、`mcp:<server-id>/<tool-name>`；显示名称不是身份。

**162. 问：两个 MCP 配置能否使用同一 server ID？**
答：不能。TOML 完整验证失败，不采用后出现者覆盖前者。

**163. 问：本地 MCP 子进程能否把普通日志写到 stdout？**
答：不能。stdout 专用于 MCP 协议；普通日志必须写 stderr。

**164. 问：MCP stderr 如何处理？**
答：转发到万象 stderr，并附加 MCP 配置 ID、进程 ID 和时间；已知密钥值必须脱敏。

**165. 问：MCP 子进程环境变量是否继承全部父进程环境？**
答：默认继承必要基础环境，并叠加 TOML 显式配置；敏感环境应尽可能白名单化，而不是无条件完整复制。

**166. 问：未配置 `max_concurrency` 时是多少？**
答：不额外限流，受 MCP Server、系统资源和同批 Tool 数量约束。

**167. 问：排队中的 MCP 调用被取消时是否启动子进程？**
答：不启动，直接从 FIFO 移除并生成取消 Tool Result。

**168. 问：Tool Result 特别大时是否截断？**
答：不静默截断。超过配置的协议安全上限时产生明确失败结果；大二进制内容应使用附件引用。

**169. 问：Tool 返回二进制数据怎么处理？**
答：保存到附件内容寻址存储，Tool Result 中只保留附件引用和元数据。

**170. 问：Tool 参数解析失败是否调用 Tool？**
答：不调用。生成完整失败 Tool Result，保留原 Tool Call，并让后续 Provider 自行处理。
# 51. 自动问答 八：附件（Q171–180）

**171. 问：首版单附件默认大小上限是多少？**
答：默认 **64 MiB**，可在 TOML 修改；这是安全资源上限，不改变附件领域语义。置信度：中。

**172. 问：默认上传 chunk 多大？**
答：每块原始数据默认 **256 KiB**，Base64 后放入一个 JSON WebSocket 事件；服务端可以在握手后公布上限。置信度：中。

**173. 问：附件上传完成是否立即成为会话历史？**
答：不会。完成只产生可引用 blob；只有包含该附件引用的消息 commit 后，它才进入会话历史。

**174. 问：上传内容与已有 SHA-256 相同时怎么办？**
答：复用已有 blob，不重复写文件；仍可保留本次消息提供的原始文件名和媒体类型元数据。

**175. 问：客户端提供的文件名是否直接用于路径？**
答：绝不用于存储路径。它只是经过长度和字符清理后的展示元数据。

**176. 问：媒体类型相信客户端吗？**
答：作为声明值保存，同时可做轻量嗅探；冲突时保留两者，不因嗅探结果改写原始声明。

**177. 问：附件下载走 HTTP 还是 WebSocket？**
答：首版走同一业务 WebSocket，以 begin/chunk/complete 事件传输，不增加新的业务 HTTP API。

**178. 问：附件下载支持 Range 或断点续传吗？**
答：首版不支持；中断后重新请求完整附件。

**179. 问：历史引用的 blob 丢失怎么办？**
答：历史引用保留，查询中标记附件缺失，doctor 报告；不删除消息，也不伪造替代文件。

**180. 问：已完成但尚未被消息引用的附件何时删除？**
答：首版不自动删除，与"不做附件 GC"保持一致；未来压缩按可达性统一处理。
# 52. 自动问答 九：配置、认证与配对（Q181–190）

**181. 问：TOML 路径如何确定？**
答：使用程序默认路径，也允许 CLI 显式指定；路径选择本身不写回 TOML。

**182. 问：首次启动时 TOML 不存在怎么办？**
答：生成一份完整默认 TOML，包括稳定 `instanceId`，通过同样的临时文件、flush、rename、reload 路径落盘。

**183. 问：TOML 中未知字段怎么处理？**
答：拒绝整份配置。虽然应用重写时不保留未知字段，但读取时也不能忽略拼写错误造成的假配置。

**184. 问：TOML 是否有顶层版本？**
答：有单一 `configVersion`。旧版本通过显式升级函数读入，应用写回时统一输出当前版本。

**185. 问：配置验证错误是否打印完整 TOML？**
答：不打印完整文件，因为含明文密钥。输出字段路径、错误原因和脱敏后的相关值。

**186. 问：`instanceId` 缺失时 watcher 是否自动生成？**
答：普通 watcher 不修复，继续使用最后有效配置；只有首次创建或显式 `--fix` 可以生成。

**187. 问：Token hash 在 TOML 中怎么编码？**
答：固定使用小写十六进制 SHA-256，并在加载时验证长度和字符集。

**188. 问：配对失败如何限流？**
答：默认每个远端地址一分钟最多 5 次失败；超过后冻结该地址 5 分钟，同时仍保持全局只有一个有效配对码。置信度：中。

**189. 问：六位配对码是否允许 `000000`？**
答：允许。它是固定六位十进制字符串，不把前导零当成数字格式丢失。

**190. 问：本地同进程自动 token 如何复用？**
答：TOML 保存带固定用途元数据的 hash，客户端侧保存对应原 token；任一侧丢失时创建新的 token/hash，旧 hash 保留到手动撤销。
# 53. 自动问答 十：PWA、UI、测试与交付（Q191–200）

**191. 问：PWA 的 IndexedDB 按什么键保存连接？**
答：以 `instanceId` 为主键，保存 URL 列表、原 token 和展示名称；URL 不是身份。

**192. 问：PWA Service Worker 可以缓存什么？**
答：只缓存版本化静态资源，不缓存 API、WebSocket 内容、消息、附件或会话快照。

**193. 问：PWA 新版本何时生效？**
答：静态资源版本变化后提示刷新；不在用户编辑或生成过程中强制 reload。

**194. 问：Linux 桌面端允许启动多个窗口或进程吗？**
答：允许多个纯客户端进程；同一数据目录只能有一个 S。一个客户端进程是否多窗口属于 UI 层，不改变连接语义。

**195. 问：窗口尺寸和主题是否写入服务端 TOML？**
答：不写入业务 TOML。它们是本机客户端偏好，保存在客户端本地设置中，不经 NDJSON 同步。

**196. 问：UI 是否照搬原项目的模型和实现？**
答：不照搬。只参考信息架构和视觉体验，使用 clean-room F#/Avalonia 实现，不复制源代码、数据模型或 Flutter 依赖。

**197. 问：首版无权限控制是否意味着忽略基础可访问性？**
答：不意味着。键盘导航、焦点可见性、屏幕阅读标签、缩放和减少动画属于 UI 基础质量，应从首版保留。

**198. 问：运行日志采用什么格式？**
答：stderr 默认输出结构化 JSON Lines；错误对象、堆栈和原始损坏内容作为转义字段保存，便于人工读取和机器分析。

**199. 问：哪些测试是发布阻塞项？**
答：日志 replay 与截断、ID 连续性、单写者竞争、命令幂等、fork 可达性、配置原子 reload、cursor catch-up、配对撤销、MCP 崩溃和附件哈希验证必须有自动化测试。

**200. 问：首版达到什么条件才算完成？**
答：Linux x64 能以默认 UI+S 模式运行，也能独立 headless S 和 client-only；PWA 能托管、配对、观察、发送、流式展示、调用 Tool、上传附件、fork/edit；重启能只靠 TOML、NDJSON 和附件目录恢复全部永久业务状态。
# 54. 自动问答结论与首版完成定义

## 自动问答结论（200 项总览）

这 100 项（第 101–200 项自动问答）中：

- **97 项：高置信度**，可由既有原则直接推出；
- **3 项：中等置信度**，分别是：
  - 附件默认上限（64 MiB，Q171）；
  - 默认 chunk 大小（256 KiB，Q172）；
  - 配对限流数值（每远端地址每分钟 5 次失败、冻结 5 分钟，Q188）；
- **0 项：低置信度阻塞项**，因此本轮不需要继续提问。

数值型中置信度答案应被视为首版默认参数，而不是协议永久语义；以后可通过 TOML 调整，不需要修改历史或升级协议。

## 首版完成定义（Q200 的展开）

Linux x64 能以默认 UI+S 模式运行，也能独立 headless S 和 client-only；PWA 能托管、配对、观察、发送、流式展示、调用 Tool、上传附件、fork/edit；重启能只靠 TOML、NDJSON 和附件目录恢复全部永久业务状态。

## 本 SSOT 的后续使用规则

- 凡是 01–43 号文件已覆盖的决策，以及 44–53 号文件中的高置信度问答，都视为已定案，不再向用户重复提问；
- 中等置信度的三项数值（附件上限、chunk 大小、配对限流）是实现时的可调默认参数，具体数值在实现时可按 TOML 默认值确定；
- 新需求与已确认原则冲突、或需要人为选择数值/产品取舍时，才重新进入提问流程（见 43.md）。
---
name: agent-framework
description: Agent layer for the F#/Avalonia kelivo rewrite.
version: 0.1.0
author: Hermes
metadata:
  hermes:
    tags: [FSharp, Avalonia, AgentHosting, A2A, MultiAgent]
---

# Microsoft Agent Framework in the kelivo Rewrite

This skill orients work on the vendored `agent-framework/` repo (a clone of Microsoft Agent Framework, MAF) as the Agent layer for the kelivo rewrite mandated by AGENTS.md: F# + Avalonia, UI unchanged, multi-platform, and a peer C/S architecture where every instance is both client and server. It covers the .NET side only (F# consumes it directly); the Python packages in `python/` are out of scope, and Avalonia UI work is out of scope. The local repo under `agent-framework/` is the source of truth — code verbatim from its `dotnet/samples/` is preferred over docs.

## When to Use

- "Wire agent-framework into the F# project" / "which MAF package do we need"
- Creating or running an `AIAgent` (single-turn or multi-turn) from F#/C#
- Hosting an agent as a server so other instances can call it (C/S peer architecture)
- Making a client that calls another instance's server (or its own, loopback)
- Agent-to-agent delegation (A2A), tools, sessions, streaming
- "Which sample should I copy?" — anything MAF-related in this repo

## Prerequisites

- .NET SDK 10 (`net10.0` is the samples' target framework). Check with `dotnet --version`.
- The vendored repo at `/home/kunweiz/Desktop/vibe/wanxiang/agent-framework/`.
- Env vars used by samples (never hardcode secrets):
  - `FOUNDRY_PROJECT_ENDPOINT` + `FOUNDRY_MODEL` (default `gpt-5.4-mini`) for Foundry-backed agents; run `az login` first.
  - `OPENAI_API_KEY` / `OPENAI_CHAT_MODEL_NAME` for OpenAI-backed agents.
  - `ASPNETCORE_URLS` to override the hosting bind URL; `RESPONSES_SERVER_URL` (default `http://localhost:5000`) for the client side.
- The kelivo rewrite consumes MAF via NuGet (`dotnet add package Microsoft.Agents.AI ...`); samples use `<ProjectReference>` only because they live inside the MAF repo.

## How to Run

Invoke through the `terminal` tool. Run a sample project straight from the repo:

```bash
cd agent-framework/dotnet/samples/01-get-started/01_hello_agent && dotnet run
```

For the hosting pair (server then client, two terminals):

```bash
cd agent-framework/dotnet/samples/04-hosting/af-hosting/local_responses/Server && dotnet run
cd agent-framework/dotnet/samples/04-hosting/af-hosting/local_responses/Client && dotnet run
```

For the kelivo F# project, add the core package and build:

```bash
dotnet add package Microsoft.Agents.AI
dotnet build
```

## Quick Reference

Packages (names match `dotnet/src/` directories): `Microsoft.Agents.AI` (core), `.OpenAI`, `.Foundry`, `.Anthropic`, `.Hosting`, `.Hosting.OpenAI`, `.Hosting.AspNetCore`, `.Hosting.A2A.AspNetCore`, `.A2A`, `.Workflows`, `.AGUI`, `.Mcp`, `.Declarative`.

Core types (`Microsoft.Agents.AI` + `Microsoft.Extensions.AI`): `AIAgent` (abstract base), `AgentSession` (multi-turn state), `ChatClientAgent`, `IChatClient`, `AITool` / `AIFunction`, `AIFunctionFactory`, `AgentResponse` (has `.Text`), `AgentSessionStore` / `InMemoryAgentSessionStore`.

Key methods: `agent.RunAsync(msg)` / `RunAsync(msg, session)` -> `AgentResponse`; `RunStreamingAsync(...)` -> async enum of updates; `agent.CreateSessionAsync()` -> `AgentSession`; `chatClient.AsAIAgent(model:, instructions:, name:, tools:)`; `AIFunctionFactory.Create(method, name:)`.

Hosting helpers (`Microsoft.Agents.AI.Hosting.OpenAI`): `OpenAIResponses.ToAgentRunRequest`, `.GetSessionStoreId`, `.CreateResponseId`, `.WriteResponse`, `.WriteResponseStreamAsync`; batteries-included `MapOpenAIResponses` also exists.

A2A (`Microsoft.Agents.AI.A2A`): `A2ACardResolver(url, httpClient)`, `cardResolver.GetAIAgentAsync()`, `remoteAgent.AsAIFunction()` (expose a remote agent as a tool).

Env vars: `FOUNDRY_PROJECT_ENDPOINT`, `FOUNDRY_MODEL`, `OPENAI_API_KEY`, `OPENAI_CHAT_MODEL_NAME`, `ASPNETCORE_URLS`, `RESPONSES_SERVER_URL`.

Sample map (`agent-framework/dotnet/samples/`): `01-get-started/01_hello_agent` (minimal agent, `Program.cs`), `04-hosting/af-hosting/local_responses/{Server,Client}` (own-route hosting + consuming client — the C/S template), `05-end-to-end/A2AClientServer/{A2AServer,A2AClient}` (A2A peer-to-peer), `02-agents/AgentProviders/{openai,ollama,anthropic,...}` (providers), `02-agents/Agents/` (tools, middleware, structured output), `03-workflows/` (graph orchestration).

## Procedure

1. Locate the framework: everything is under `agent-framework/` — README.md (overview), `dotnet/src/` (packages), `dotnet/samples/` (copy-paste code), `docs/specs/` (design specs, e.g. 003-dotnet-hosting-protocol-helpers.md). Use `search_files`/`read_file` on samples before writing new code.
2. Pick packages for the F# layer. Minimum: `Microsoft.Agents.AI` plus one provider (`Microsoft.Agents.AI.OpenAI` or `.Foundry`); add `.Hosting` + `.Hosting.OpenAI` (or `.Hosting.A2A.AspNetCore`) when an instance exposes its agent as a server.
3. Create an agent. Verbatim from `01_hello_agent/Program.cs`:
   ```csharp
   AIAgent agent = new AIProjectClient(new Uri(endpoint), new DefaultAzureCredential())
       .AsAIAgent(model: model, instructions: "You are good at telling jokes.", name: "Joker");
   Console.WriteLine(await agent.RunAsync("Tell me a joke about a pirate."));
   ```
   OpenAI equivalent: `new OpenAIClient(new ApiKeyCredential(apiKey)).GetChatClient(modelId).AsAIAgent(instructions: ..., name: ..., tools: ...)` (see `A2AClient/HostClientAgent.cs`).
4. Multi-turn: create one `AgentSession` via `agent.CreateSessionAsync()` and pass it to every `RunAsync(msg, session)` call — the session threads conversation state (see `A2AClient/Program.cs` and the MAF path in `af-hosting/local_responses/Client/Program.cs`).
5. Add tools: `tools: [AIFunctionFactory.Create(LookupWeather, name: "lookup_weather")]` with a `[Description]`-annotated static method (verbatim from `af-hosting/local_responses/Server/Program.cs`).
6. Host the instance as a server (the "S" in C/S). Copy the pattern from `04-hosting/af-hosting/local_responses/Server/Program.cs`: `WebApplication.CreateBuilder`, an `AgentSessionStore` (e.g. `InMemoryAgentSessionStore`), then `app.MapPost("/responses", ...)` converting the body with `OpenAIResponses.ToAgentRunRequest(body)`, running the agent, persisting the session, and writing `OpenAIResponses.WriteResponse(...)` (or SSE via `WriteResponseStreamAsync` for `stream: true`). Run with `app.Run("http://localhost:5000")`.
7. Consume another instance's server (the "C"): either plain `IChatClient` (`responseClient.AsIChatClient(model)` from a `ResponsesClient` pointed at the server URL, threading `ChatOptions.ConversationId`), or higher-level `responseClient.AsAIAgent(...)` + `AgentSession` (see the two paths in `af-hosting/local_responses/Client/Program.cs`). For loopback self-command, point the client at your own instance's URL.
8. Peer-to-peer agent delegation (A2A): resolve remote agents with `new A2ACardResolver(url, httpClient).GetAIAgentAsync()`, cast each to `AITool` via `AsAIFunction()`, and hand them as `tools:` to your own orchestrator agent (verbatim `HostClientAgent.InitializeAgentAsync` in `05-end-to-end/A2AClientServer/A2AClient/HostClientAgent.cs`).
9. From F#: `open Microsoft.Agents.AI` and call the Task-returning APIs with `Async.AwaitTask` (or the `task {}` CE), e.g. `let! resp = agent.RunAsync(msg, session) |> Async.AwaitTask`. Keep the agent code in an F# library project referencing the MAF packages; the Avalonia UI project references that library, leaving the UI unchanged per AGENTS.md.

## Pitfalls

- `DefaultAzureCredential` is dev-only: samples warn it causes latency and credential probing; in production use a specific credential (e.g. `ManagedIdentityCredential`).
- The Foundry samples throw if `FOUNDRY_PROJECT_ENDPOINT` is unset — set env vars or use the OpenAI provider before running.
- Samples target `net10.0`; older SDKs will fail to build.
- The `af-hosting` server deliberately owns routing/auth/session storage; the sample's `Authorize` is a stub — a real deployment must authenticate callers and bind session ids to principals. Concurrent runs against one conversation id are NOT serialized; provide per-conversation single-writer coordination.
- Local A2A/Responses sample servers ignore the API key, but the OpenAI SDK still requires a credential — pass `new ApiKeyCredential("not-needed")`.
- Do not re-derive APIs from memory or MS Learn: read the vendored samples first; the local repo is the source of truth.
- Session id from the request is untrusted input — validate before using as a storage key (see `OpenAIResponses.GetSessionStoreId` handling in the Server sample).
- This skill covers the .NET/F# path only; `agent-framework/python/` is a parallel implementation, not a reference for this rewrite.

## Verification

Build the hosting server sample (no credentials needed):

```bash
cd agent-framework/dotnet/samples/04-hosting/af-hosting/local_responses/Server && dotnet build
```

With credentials set, `dotnet run` in `01-get-started/01_hello_agent` prints an agent reply; the full C/S round-trip is verified by starting Server then Client and getting answers across a 3-turn conversation.
---
name: avalonia
description: UI framework reference for the F# kelivo rewrite.
version: 0.1.0
author: Hermes
metadata:
  hermes:
    tags: [FSharp, Avalonia, XAML, MVVM, MultiPlatform]
---

# Avalonia in the kelivo Rewrite

This skill maps the vendored `Avalonia/` repo (the Avalonia UI framework source, MIT-licensed, .NET 10 era) as the UI framework for the kelivo rewrite mandated by AGENTS.md: F# + Avalonia, UI basically unchanged, multi-platform. It captures the F# project pattern Avalonia itself validates (the in-repo `BuildTests.FSharp` project), the package set, and the sample catalog to crib from. It is NOT an Avalonia tutorial; kelivo's screen inventory lives in the `kelivo` skill and the agent layer in the `agent-framework` skill. The rewrite consumes Avalonia via NuGet; building this repo from source is only needed for framework-level work.

## When to Use

- Creating the F#/Avalonia project skeleton for the kelivo rewrite
- "How do I structure an F# Avalonia app?" (entry point, App/Window/View, XAML loading, MVVM)
- Choosing packages/themes for the app (Fluent vs Simple, desktop backends)
- Finding the right control or pattern (list virtualization, dialogs, rendering) in the samples
- Building or running the vendored framework itself (samples, local NuGet packages)
- Multi-platform build questions (Windows/macOS/Linux; Android/iOS/Browser caveats)

## Prerequisites

- .NET SDK 10.0.201 — the repo pins it in `global.json` (`"version": "10.0.201"`, `rollForward: latestFeature`); older SDKs fail.
- The vendored repo at `/home/kunweiz/Desktop/vibe/wanxiang/Avalonia/` (source of truth; `readme.md` overview, `docs/build.md` build instructions, `docs/nuget.md` local package building).
- For the full solution (mobile/browser): `dotnet workload install android ios tvos maccatalyst wasm-tools` (sudo on Unix). Desktop-only work (`Avalonia.Desktop.slnf`) needs no workloads.
- The rewrite app itself needs no repo build: `dotnet add package Avalonia`, `Avalonia.Desktop`, `Avalonia.Themes.Fluent`.

## How to Run

Invoke through the `terminal` tool. Run the framework's reference sample:

```bash
cd /home/kunweiz/Desktop/vibe/wanxiang/Avalonia/samples/ControlCatalog.Desktop && dotnet run
```

Build the in-repo F# proof that F#+Avalonia works (the pattern to copy):

```bash
cd /home/kunweiz/Desktop/vibe/wanxiang/Avalonia/tests/BuildTests && dotnet build BuildTests.FSharp/BuildTests.FSharp.fsproj -p:AvaloniaVersion=12.0.0
```

For the rewrite project: `dotnet run` from the F# app's own directory.

## Quick Reference

Packages: `Avalonia` (core), `Avalonia.Desktop` (desktop head, pulls platform backends), `Avalonia.Themes.Fluent` / `Avalonia.Themes.Simple` (themes), `Avalonia.Fonts.Inter`, `Avalonia.Diagnostics` (dev tools), `Avalonia.Headless` (tests). Platform backends live in `src/`: `Avalonia.Win32` (Windows), `Avalonia.X11` + `Avalonia.Wayland` + `Avalonia.FreeDesktop` (Linux), `Avalonia.Native` (macOS; see `docs/macos-native.md`), plus `src/Skia`, `src/Browser`, `src/Android`, `src/iOS`.

Core API names (from the F# build test, verbatim): `AppBuilder.Configure<App>()`, `.UseSkia()`, `.UseWin32()`, `.LogToTrace(areas = ...)`, `.StartWithClassicDesktopLifetime(argv)`; `AvaloniaXamlLoader.Load(this)`; `IClassicDesktopStyleApplicationLifetime` with `desktop.MainWindow <- MainWindow()`; XAML root `<Window xmlns="https://github.com/avaloniaui" x:Class="...">`, `<Application.Styles><FluentTheme /></Application.Styles>`, `RequestedThemeVariant="Default"`.

Samples (`samples/`): `ControlCatalog` (control gallery + `ControlCatalog.Desktop` runner), `MiniMvvm` (minimal MVVM: ViewModelBase + MiniCommand), `VirtualizationDemo`, `TextTestApp`, `RenderDemo`, `GpuInterop`, `SingleProjectSandbox`, `AppWithoutLifetime`.

Source layout: `src/Avalonia.Base`, `src/Avalonia.Controls`, `src/Avalonia.Desktop`, `src/Markup` (XAML), `src/Skia`, `src/Themes.*`, platform dirs `src/Windows`, `src/Linux`, `src/Android`, `src/iOS`, `src/Browser`, `src/Headless`. Build tooling: `build.sh`/`build.ps1`/`build.cmd` (Nuke), `nuke CreateNugetPackages`, `nuke --target BuildToNuGetCache` (pushes `9999.0.0-localbuild` into `~/.nuget/packages`).

## Procedure

1. Locate the framework: `Avalonia/readme.md` (overview, license = MIT via `licence.md`), `docs/build.md` (build/run), `docs/nuget.md` (local package builds), `src/` (framework source), `samples/` (reference apps). Use `search_files`/`read_file` before writing new UI code.
2. Scaffold the F# app project. Copy the repo's own `tests/BuildTests/BuildTests.FSharp/BuildTests.FSharp.fsproj` shape: `OutputType=WinExe`, `TargetFramework=net10.0`, `AvaloniaUseCompiledBindingsByDefault=true`, packages `Avalonia`, `Avalonia.Themes.Fluent`, `FSharp.Core` (+ `Avalonia.Win32`/`Avalonia.X11` for desktop backends). Compile order matters: MainView, MainViewModel, MainWindow, App, then Program.
3. Write the entry point (verbatim pattern from `Program.fs`): `[<EntryPoint; STAThread>] let main argv = AppBuilder.Configure<App>().UseSkia().UseWin32().LogToTrace(areas = Array.empty).StartWithClassicDesktopLifetime(argv)`.
4. Define `App` inheriting `Application()`: `Initialize()` calls `AvaloniaXamlLoader.Load(this)`; `OnFrameworkInitializationCompleted()` matches `IClassicDesktopStyleApplicationLifetime` and sets `desktop.MainWindow <- MainWindow()`. Ship an `App.axaml` with `<Application.Styles><FluentTheme /></Application.Styles>` (see `BuildTests/App.axaml`).
5. Views: `Window`/`UserControl` F# subclasses with `do this.InitializeComponent()` → `AvaloniaXamlLoader.Load(this)`; companion `.axaml` declares `x:Class` matching the F# type. Register axaml files as `<AvaloniaXaml Include=...>` and assets as `<AvaloniaResource Include=...>` in the project file (pattern: `tests/BuildTests/IncludeBuildTestsAvaloniaItems.props`).
6. ViewModels: plain F# classes with `member val X = ... with get, set` (see `MainViewModel.fs`) or INotifyPropertyChanged base; `MiniMvvm` sample shows the minimal MVVM base. Bind in XAML with `{Binding ...}` (compiled bindings are on by default).
7. Theming: kelivo's Material You contract (from the `kelivo` skill: light/dark + dynamic color) maps to `FluentTheme` + `RequestedThemeVariant` (`Default`/`Light`/`Dark`); dynamic color needs manual palette work — Avalonia has no Material You equivalent.
8. Multi-platform: `Avalonia.Desktop` covers Windows/macOS/Linux. Mobile (Android/iOS) and Browser targets exist in `src/` and `samples/ControlCatalog.{Android,iOS,Browser,MacCatalyst,tvOS}` but need the extra .NET workloads and have platform caveats — treat desktop as the solid target, verify mobile before committing to it.
9. Reference `ControlCatalog` (run it, then read its `Pages/` + `Views/` + `ViewModels/` via `read_file`) for any control you need to port a kelivo screen with.

## Pitfalls

- SDK pin: `global.json` requires SDK 10.0.201; the repo hardcodes a known-compatible SDK because new SDK releases can break the build. Check `dotnet --version` first.
- Full-solution builds need workloads (`dotnet workload install android ios tvos maccatalyst wasm-tools`); use `Avalonia.Desktop.slnf` for desktop-only, which needs none.
- Error MSB4062 (GenerateAvaloniaResourcesTask): build `src/Avalonia.Build.Tasks` once (or run a Nuke build) before IDE builds.
- Keep submodules updated when working against the repo: `git submodule update --init --recursive` (from `docs/build.md`).
- Building local NuGet packages: `CreateNugetPackages` does NOT build `Avalonia.Native` on non-macOS, which breaks `Avalonia.Desktop` consumption; use `nuke --target BuildToNuGetCache` instead (installs `9999.0.0-localbuild` to `~/.nuget/packages`).
- Avalonia 12 has breaking changes vs 11 — the docs reference an `avalonia12-breaking-changes` page; don't assume 11-era APIs.
- Compiled bindings (`AvaloniaUseCompiledBindingsByDefault=true`) surface binding errors at build time — keep it on and treat build errors as real bugs.
- F# note: the build-test F# project is WinExe/desktop-only; the `.UseWin32()` call in `Program.fs` is the Windows backend — swap to the platform's backend (X11/Wayland/Native) per target, or reference `Avalonia.Desktop` for auto-selection.
- This skill covers the framework only; kelivo's UI inventory and agent-framework integration are in their respective skills.

## Verification

Build the F# proof project (no workloads needed):

```bash
cd /home/kunweiz/Desktop/vibe/wanxiang/Avalonia/tests/BuildTests && dotnet build BuildTests.FSharp/BuildTests.FSharp.fsproj -p:AvaloniaVersion=12.0.0
```

For the rewrite itself, `dotnet build` the F# app project and `dotnet run` it to see the window; run `ControlCatalog.Desktop` to verify any control you port from it.
---
name: kelivo
description: Map the Flutter chat client for the F# rewrite.
version: 0.1.0
author: Hermes
metadata:
  hermes:
    tags: [FSharp, Avalonia, Flutter, ChatClient, Rewrite]
---

# Kelivo: Reference Map for the F#/Avalonia Rewrite

This skill maps the `kelivo/` source tree (a Flutter LLM chat client, Material You design inspired by RikkaHub) as the reference for the rewrite mandated by AGENTS.md: F# + Avalonia, agent-framework as the Agent layer, UI basically unchanged, multi-platform. It is a read-and-port guide — it inventories screens, data models, services, and platform layers so nothing in the original is lost, and tells you where each piece lives. It does NOT cover agent-framework wiring (see the `agent-framework` skill) or Avalonia itself; it is source-code reading only (Flutter SDK is needed only if you want to run the original).

## When to Use

- Porting any kelivo screen/feature to the F#/Avalonia app ("keep the UI unchanged")
- "What did kelivo do for X?" — search, TTS, MCP tools, backup, message editing, token display, etc.
- Porting the data model (conversations, messages, assistants, provider configs)
- Checking platform behaviors (desktop tray/hotkeys, mobile background generation)
- Deciding what the new app must ship to be feature-parity with kelivo

## Prerequisites

- The repo at `/home/kunweiz/Desktop/vibe/wanxiang/kelivo/` (source of truth; PRODUCT.md states the product brief, `docx/screenshot_*.png` show the reference UI).
- Nothing else strictly: the workflow is `read_file`/`search_files` over the tree. Flutter SDK is only required to run the original (`flutter run -d <linux|windows|macos>`), which is optional.

## How to Run

Invoke through the `terminal` tool only to build/run the original:

```bash
cd /home/kunweiz/Desktop/vibe/wanxiang/kelivo && flutter run -d linux
```

All exploration and porting reads go through `read_file` (single files) and `search_files` (feature/class discovery), e.g. `search_files(pattern='*.dart', target='files', path='kelivo/lib/features/chat')`.

## Quick Reference

Platform targets: Android, iOS, Windows, macOS, Linux (PRODUCT.md also names Harmony — kelivo-ohos is a separate fork, out of scope for .NET). License: AGPL-3.0. UI language: English + Chinese (zh, zh_Hans, zh_Hant) via `lib/l10n/*.arb`.

Feature inventory (`lib/features/`, 17 folders — the parity checklist): assistant, backup, chat, home, instruction_injection, mcp, migration, model, provider, quick_phrase, scan, search, settings, stats, translate, world_book.

lib/ map:
- `core/models/` (19 files) — Hive models: chat_message, conversation, assistant, assistant_memory, assistant_regex, assistant_tag, api_keys, provider_group, model_types, chat_item, chat_input_data, preset_message, quick_phrase, token_usage, world_book, instruction_injection, backup.
- `core/database/` — Hive→SQLite migration in flight: `app_database.dart` (drift), `business_repository.dart`, `business_preferences.dart`, `business_settings_router.dart`, `business_startup_gate.dart`, `chat_database_gateway.dart`, `chat_database_repository.dart`, `chat_database_observer.dart`, `database_installation_gate.dart`, `business_migration_engine.dart`, restore services.
- `core/services/` — `api/` (chat_api_service, builtin_tools, gemini_tool_config, google_service_account_auth, provider_request_headers, providers/), `chat/` (chat_service, prompt_transformer, document_text_extractor), `network/` (dio_http_client, request_logger), plus api_key_manager, model_override_resolver, memory_store, tts/, search/, storage/, backup/, mcp/ (mcp_tool_service), notification_service, provider_balance_service, quick_phrase_store, world_book_store, logging/.
- `core/providers/` (state, provider-package ChangeNotifiers) — assistant, settings, user, model, mcp, tts, tag, update, quick_phrase, instruction_injection(+group), world_book, memory, backup, s3_backup, backup_reminder, hotkey.
- `features/chat/` — pages: chat_history_page, message_edit_page, select_copy_page, html_preview_page, image_viewer_page; widgets: chat_message_widget, chat_suggestion_bubbles, bottom_tools_sheet, citation_sources_sheet, context_management_sheet, image_preview_sheet, message_edit_sheet, message_export_sheet, message_more_sheet, reasoning_budget_sheet, select_copy_sheet, token_detail_popup, token_display_widget.
- `features/home/` — home_page + home_desktop_layout / home_mobile_layout (responsive split), ask_user_interaction_service, tool_approval_service.
- `desktop/` — desktop_home_page, desktop_window_controller, desktop_tray_controller, hotkeys/, setting/, widgets/ (bitsdojo_window + window_manager + tray_manager).
- `theme/` — theme_factory, palettes, dynamic_color (Material You).
- `features/settings/pages/` — settings, display, theme, image, tts (+ tts_services), network_proxy, google_fonts_picker, storage_space, about, debug, log_viewer, more, sponsor.
- Other pages: providers_page + provider_detail/balance/network/groups + multi_key_manager, default_model_page, stats_page, translate_page, world_book_page, scan (mobile_scanner).

API providers (`core/services/api/providers/`): openai_chat_completions, openai_responses, openai_images, claude_official, google_gemini, google_vertex, google_common, openai_common. Web search engines: Bing, DuckDuckGo, Exa, Tavily, Zhipu, LinkUp, Brave, Metaso, SearXNG, Ollama, Jina, Perplexity, Bocha, Serper, Grok.

## Procedure

1. Orient: read `PRODUCT.md` (product brief + design principles: conversation-primary UI, Material 3 vocabulary, light/dark + dynamic color, localization, reduced motion), `README.md` (feature list), and `docx/screenshot_*.png` for the reference look. AGENTS.md constraint: UI stays basically unchanged, so screenshots + feature folders are the contract.
2. Inventory screens for parity: run `search_files(pattern='*.dart', target='files', path='kelivo/lib/features')`, then for each feature folder read its `pages/` (top-level screens) and `widgets/` (components) — these map 1:1 to Avalonia views/controls.
3. Port the data model first — it is the contract between UI and agent layer. Read `core/models/chat_message.dart` and `conversation.dart` fully; port every field (see Pitfalls for the subtle ones). Other models in `core/models/` follow the same Hive-annotation shape (typeId, HiveField N).
4. Port persistence: the F# app should target the drift SQLite schema (`core/database/app_database.dart`, `drift_schemas/app_database/`), NOT the legacy Hive boxes — the Flutter app itself is mid-migration from Hive to SQLite (`features/migration/hive_to_sqlite_migration_*.dart`), so SQLite is the forward-compatible shape. Keep backup/restore (`services/backup/`, restore receipt/lease/pruner) since data portability is a PRODUCT.md principle.
5. Port the service layer: `services/api/` (provider adapters + request headers + API key manager), `services/chat/chat_service.dart` (generation loop — this is where agent-framework replaces direct provider calls), `services/mcp/mcp_tool_service.dart` + builtin tools, `services/search/` (web search engines), `services/tts/`, `services/storage/`. Map `core/providers/` ChangeNotifiers to whatever state mechanism the F# app uses; they are view-model-equivalent.
6. Port platform layers: `desktop/` (window controls, tray, hotkeys, background behaviors) for Windows/macOS/Linux; `services/android_background.dart` / `ios_background_generation.dart` / `notification_service.dart` for mobile. Avalonia covers the desktop trio; mobile per PRODUCT.md targets Android/iOS.
7. Port theming + l10n: `theme/theme_factory.dart` + `palettes.dart` + dynamic_color give the Material You theming contract (Avalonia has no Material You — recreate light/dark palettes and dynamic-color behavior manually); `lib/l10n/app_*.arb` are the string tables (en, zh, zh_Hans, zh_Hant).

## Pitfalls

- Message versioning is a distinctive kelivo feature, easy to lose: `ChatMessage.groupId` + `version` (0-based, increments on regeneration) and `Conversation.versionSelections` (groupId -> chosen version) — regenerate/rollback UI depends on it. Port it.
- `Conversation.messageIds` is an ordered list (display order); `truncateIndex` (-1 = none) marks context truncation; `assistantId` null = global/default conversation; `mcpServerIds` is per-conversation MCP enablement. All must survive the port.
- `ChatMessage.role` is the string `'user'` / `'assistant'`; token accounting is split across `totalTokens`, `promptTokens`, `completionTokens`, `cachedTokens`; reasoning has `reasoningText`, `reasoningSegmentsJson`, `reasoningStartAt/FinishedAt`; `translation` holds translated content. The token/reasoning/translation UI sheets in `features/chat/widgets/` depend on these fields.
- The data layer is mid-migration: Hive models and drift tables coexist (`app_database.g.dart`, `chat_message.g.dart`, `conversation.g.dart` are generated). For the rewrite, model the drift SQLite schema, not the Hive annotations.
- Don't copy Flutter-only packages 1:1 — syncfusion (PDF), mobile_scanner, flutter_math_fork (LaTeX), webview_flutter, easy_image_viewer, gpt_markdown, highlight all need Avalonia/.NET equivalents; keep the behavior, not the dependency.
- `docx/` also contains document-generation files (docx/ dir name is coincidental, screenshots live there); `assets/` has app_icon.png.
- Harmony OS support is a separate fork (kelivo-ohos) — do not treat it as part of this repo's contract.
- License is AGPL-3.0 — the rewrite must keep compatible licensing.
- This skill covers the kelivo reference only; agent-framework integration and Avalonia UI work are covered by their own skills.

## Verification

Confirm parity: enumerate kelivo's 17 feature folders with `search_files(pattern='*', target='files', path='kelivo/lib/features')` and check each has a counterpart screen/module in the F# app; for the data model, verify every field of `ChatMessage` and `Conversation` (from `read_file` on `core/models/chat_message.dart` and `conversation.dart`) exists in the ported F# records/classes.
