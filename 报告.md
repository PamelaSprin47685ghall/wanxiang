# 万象对标 kelivo — 易用性 / 界面与完成度差距报告

> 目标：不比“功能多”，比“现有功能是否做到可用→好用→耐用”。对标 kelivo 的行为与完成度，找差距，给万象已实现路径的抛光清单。
> 覆盖：kelivo `kelivo/lib` 17 功能域 + `core/models` + `theme` + `desktop` / 万象 `src/*` 10 模块。
> 约束：clean-room（仅作行为/信息架构参考，不复制代码/资源/文案）；术语以 `skill://wanxiang` SSOT 为准。

## 1. 对标方法

- kelivo：读 `PRODUCT.md` / `lib/main.dart` / `lib/features/**` / `lib/core/models/*` / `lib/theme/*` / `lib/l10n/app_en.arb` / `lib/desktop/*`，抽样 `chat_message_widget` / `message_more_sheet` / `token_display_widget` / `bottom_tools_sheet` / `settings_page` / `providers_page` 等
- 万象：读 `src/Wanxiang.UI/MainWindow.fs`（1363 行，主战场）、`App.fs` / `Theme.fs` / `CredentialStore.fs`、`Wanxiang.App/Program.fs`、`Wanxiang.Server/*`、`Wanxiang.Protocol/*`、`Wanxiang.Core/*`、`Wanxiang.Store/*`、`Wanxiang.Config/*`、`Wanxiang.Agent/*`、`Wanxiang.Client/*`、`Wanxiang.Pwa/wwwroot/*`
- 判定：以“同一任务的交互闭环是否完整”为准——有按钮≠完成，含空/加载/错误/快捷键/可达性才算完成

## 2. 万象现状快照（已实现 / 半实现）

| 域 | 已实现（可用） | 半实现（可用但粗糙） | 证据 |
|---|---|---|---|
| **传输与会话** | muxed WebSocket、对等 C/S、NDJSON + CQRS、提交原子性、单写者锁、配对/令牌哈希、自动重连(指数退避)、`observe`/`cursor`/`catchUp`、历史分页、附件分块(Begin/Chunk/Complete, 256 KiB, 64 MiB, SHA-256)、下载回放 | 重连无可视进度、慢客户端不提示 | `MainWindow.fs:1020-1150` `WsClient.fs` `Store/Replay.fs` |
| **会话** | 新建/重命名/删除(overlay 弹窗)、fork(编辑最后一条并 fork)、服务端 `forkAfterId`、会话列表+搜索过滤、选中同步 `syncSelection` | 列表仅 `Title+Preview+Running`、无置顶/时间/头像、搜索只含标题/预览、无高亮、无分组/多选、无“清空上下文” | `MainWindow.fs:~400-560` `Core/Projection.fs` |
| **聊天** | 发送/取消生成(`generationId`)、流式增量 `GenerationDelta` 临时渲染、思维链折叠(左竖线+可键盘切换)、用户气泡/助手无边框/工具胶囊、滚动到底、Shift+Enter 提示 | 输入框 `AcceptsReturn=false` 但文案写 Shift+Enter、按 Enter 直接发不判修饰键；Markdown 仅 `FencedCodeBlock` vs `NormalText`（`blockText/inlineText` 拼字符串，粗体/链接/列表/表格/LaTeX/Mermaid 全丢）；代码块仅“复制”无高亮/行号/换行；每条消息无复制/选词复制/导出/分享/HTML 预览；token/耗时无展示；无引用溯源、建议气泡、图片预览、上下文截断 UI | `MainWindow.fs:70-200, 830-1010` |
| **附件** | 选择文件→SHA-256→分块上传、随下一条消息携带、下载到 `SaveFilePicker`、缺失标记 `missingAttachments` | 报 `64 MiB` 上限但无进度条、无类型图标、无多附件队列、无拖拽/粘贴 | `MainWindow.fs:1012-1080` |
| **视觉** | 浅色板 8 变量、MessageView 走 Avalonia 手写布局、空状态品牌标+文案、输入壳焦点环、对话框遮罩+Esc 关闭 | 单浅色、无深色/跟随系统/动态取色/9 调色板、无字体选择/缩放、无纯净背景开关、动效硬切 | `UI/Theme.fs` `UI/App.fs` `Pwa/wwwroot/app.css:168行` |
| **连接/PWA** | 连接对话框(URL+令牌+配对码)、IndexedDB 凭据桥(`credList/credPut`)、Service Worker 仅缓存静态壳(network-first)+`SKIP_WAITING` 需用户点击 | PWA 无离线提示、无新版本 Toast 样式打磨、无 `manifest` 细节 | `UI/CredentialStore.fs` `Pwa/wwwroot/main.js/sw.js` |
| **健壮性** | `doctor`/`--fix`、数据锁、窗口尺寸/位置持久化 `~/.config/wanxiang/ui.json`、剪贴板复制成功态“已复制✓” | 空状态仅两段文案、错误仅 `statusText` 一行、加载态仅 `genChip`、无日志查看、无存储空间、无备份/恢复 | `App/Program.fs:Doctor` `MainWindow.fs:emptyPanel` |

结论：**协议与存储已达首版完成定义**；**UI 的“最后一公里”——输入、渲染、列表、设置、主题、空/错/加载态——是与 kelivo 的显著落差**。kelivo 把同一条链路打磨到“可键盘、可触摸、可撤销、可解释”；万象尚停在“能发能收”。

## 3. 差距总览（按“完成度”维度，非功能清单）

| 维度 | kelivo 已做到的完成度 | 万象缺口 | 影响 |
|---|---|---|---|
| **聊天交互** | 版本切换(← → + 计数)、重生/重发(可配是否删尾+确认弹窗)、编辑即 fork、上下文截断(压缩/清空二选一+动态 label)、建议气泡(Wrap 3 条)、底部工具栏三列卡片+拖拽手柄、复制/选词复制/导出/分享/HTML 预览(暗色自适应)/图片预览(裁白边/平铺/切片/保存)、引用溯源列表、工具审批(AskUser 卡片 Approve/Deny+理由)、TTS/世界书/快捷短语入口、排队态(sent/queued/rejected)+取消 | **0 项**版本/重生；**0**引用/气泡；**0**图片/HTML 预览；**0**工具审批；触发器在但输入判定错；Markdown 丢格式；token 0 展示 | 对话不可“改”、不可“溯”、不可“回退”，重度用户立即感知粗糙 |
| **会话与侧边栏** | `isPinned`/置顶、`truncateIndex` 可视化、按日期分组、跳转定位+小地图、全局搜索(token 评分去 reasoning)、小窗 `TemporaryConversationEmptyState`、多选删除/导出 | 仅过滤无高亮、无置顶、无分组、无定位、无上下文开关 | 20+ 会话后不可用 |
| **侧边栏与导航** | 桌面 64px NavRail + 侧栏分栏 + 4 Tab、选择模式悬浮玻璃胶囊(22ms 滑入+模糊)、滚动导航按钮(回到最新/顶部，四模式)、自适应 breakpoints | 固定 264px 单侧栏、无 Rail、无选择模式、无回顶 | 信息密度与导航效率低 |
| **设置与配置** | iOS 分组卡片 20+ 开关：主题(9 套×明暗)/语言/气泡样式/渲染/行为/触感、Provider 分组+多 Key 轮询+余额徽章+QR/批量检测、MCP 状态灯+超时+JSON 直编+错误底弹、搜索/TTS/快捷短语/世界书/网络代理/存储分类/统计热力图/备份 | **无设置页**；Provider/MCP/搜索等仅 TOML，无 UI；会话 `config` 新建时空串靠服务端兜底 | 用户无法自助配模型，等于不可用 |
| **视觉与主题** | 9 调色板×明暗、Android 12 动态取色、`pureBackground`、字体回退/按需加载、Card 阴影 1/3/6dp、毛玻璃按钮(blur 16-20)、代码高亮/Mermaid/LaTeX/表格、动效 180-260ms、可缩放文本 | 单浅色 8 变量、无深色/动态色/字体/缩放、代码无高亮、Mermaid 丢、阴影 1 层 | 视觉完成度直接落后一代 |
| **桌面集成** | 窗口 400ms 防抖落盘(macOS autosave)、托盘分平台图标(ico/template png)、7 热键、右键玻璃菜单、发送快捷 Enter/CtrlEnter 可配 | 仅尺寸/位置落盘、无托盘/热键/菜单/标题栏定制 | 桌面感弱 |
| **健壮性与空状态** | 启动 `RestoreFailureScreen` 按诊断码分流、Hive→SQLite 三步迁移、空状态居中图标+文案全覆盖、线性进度、错误分类文案、备份提醒按次/按时、日志轮转双阈值 | 空状态 1 文案、错误 1 行 `statusText`、加载仅 chip、无分类、无日志 | 异常即困惑 |

## 4. 分节详情（证据到行）

### 4.1 聊天交互 —— 最大的完成度凹陷

- **输入闭环未闭合**：`inputBox.AcceptsReturn=false` + `KeyDown Enter => SendMessage()` 不判 `Shift/Ctrl`（`MainWindow.fs:546-548`），与占位文案“Enter 发送，Shift+Enter 换行”矛盾；无粘贴图片/拖拽、 无多行自适应高度、无 `Ctrl+Enter` 可配、无发送中防抖/队列提示；kelivo 的 `chat_input_bar` 支持图文混排+压缩队列+排队取消+拖拽，正好是完成度的标尺。
- **Markdown 粗糙**：`MarkdownParser.parse` 仅对 `FencedCodeBlock` 单独成段，其余 `blockText` 拼 `inlineText` 成纯文本（`MainWindow.fs:80-130`），导致加粗/斜体/链接/列表/引用/表格/行内代码全部退化为一段 `TextBlock`；kelivo 用 `markdown_with_highlight + mermaid_bridge + incremental_markdown` 保留结构、支持拷贝与全屏。
- **每条消息零操作**：kelivo `message_more_sheet` 提供 `edit/fork/deleteCurrent/deleteAll/share/selectMessages` 的桌面锚定菜单与移动端底弹 + `select_copy_sheet` 的选词复制；万象仅代码块有“复制”按钮，消息级无任何操作（`MainWindow.fs:820-960`）。
- **Token/耗时 0 展示**：kelivo 在标题下 11sp 显示 `totalTokens`，明细用 `TokenDisplayWidget`（移动端点按/桌面 hover 200ms 弹、300ms 收、滚动自消失）展示 prompt(含 cached)/completion/tok/s/秒数；万象无任何 token 渲染（grep `promptTokens/completionTokens` 0 命中）。
- **推理单段**：万象 `MessageView` 只走 `$type=reasoning` 的单串（`MainWindow.fs:20-60`），kelivo 支持 `reasoningSegmentsJson` 多段 + `ReasoningUiState` 的加载/折叠/时间戳；万象的折叠是可用但无“自动收起”开关与时长。
- **缺引用/气泡/预览**：`citation_sources_sheet` / `chat_suggestion_bubbles` / `image_preview_sheet` / `html_preview_page` 在万象均 0 命中；工具消息仅胶囊色块，无审批卡片。

### 4.2 会话与侧边栏

- **列表信息贫**：kelivo `Conversation` 有 `isPinned/summary/chatSuggestions/mcpServerIds/assistantId/versionSelections`，列表支持置顶、模型图标、时间分段；万象 `ConvSummary` 仅 `Title/Preview/Running`（`MainWindow.fs:56-62`），`convList.ItemTemplate` 两行文本+右键重命名/删除，未虚拟化。
- **搜索不可用**：万象 `searchBox.TextChanged` 仅 `Title+Preview` 含子串过滤（`MainWindow.fs:490-500`），无高亮、无去 reasoning、无评分排序；kelivo 全局搜索去 `<reasoning>/gemini_thought_signatures` 后 token 评分。
- **上下文不可视**：kelivo `truncateIndex` 与压缩/清空在 `context_management_sheet` 可视化；万象该字段不存在于投影之外，头部仅“编辑并 fork”。

### 4.3 设置与配置 —— 从“能改 TOML”到“能在 UI 改”

- **0 设置页**：`src/Wanxiang.UI` 仅 `MainView/MainWindow`，grep `SettingsPage/settings_provider` 0 命中；kelivo `settings_page` 四段 iOS 卡片 + `display_settings_page` 20+ 开关是完成度的锚点。
- **Provider 断层**：万象 `AppConfig.providers: Map<string,ProviderConfig>` 与 `SessionConfig.provider/model` 完整（`Config/ConfigModel.fs` `Core/SessionConfig.fs`），`ChatOrchestrator` 已走 `ProviderConfig`，但新建会话 `config = {provider=""; model=""}` 靠服务端第一个 provider 兜底（`MainWindow.fs:690-710`），用户在 UI 无法选模型/改温度/切 provider。
- **MCP/搜索/TTS**：后端 `McpServerConfig/toolRegistry` 就绪，UI 0 入口；kelivo 的 `mcp_page` 状态灯(绿/主色/红)+超时+JSON 直编+错误底弹是“可诊断”的标准。

### 4.4 视觉与主题

- **单板 vs 9 板**：`Theme.fs` 仅 13 固色 + `ThemeVariant.Light`（`UI/App.fs:13`），kelivo `palettes.dart` 有 9 套×明暗 + `DynamicColorBuilder`；PWA `app.css` 168 行仅排版/滚动条，未做深色媒体查询。
- **排版未收敛**：消息区 760px 阅读列与头像行已做（`MainWindow.fs:460-520`），但桌面无 Rail、自适应断点、卡片阴影 1 层 vs kelivo 的 surfaceTint 3 层。

### 4.5 桌面集成

- **无托盘/热键**：grep `tray/Tray/hotkey` 在 `src` 0 命中；kelivo `desktop_tray_controller` 分平台图标+`desktop_window_controller` 400ms 防抖，万象仅 `ui.json` 的尺寸持久化。
- **窗口未定制**：kelivo 用 `bitsdojo_window` 做标题栏定制，万象走默认 Fluent。

### 4.6 健壮性与空状态

- **空/错/载三态单薄**：`emptyPanel` 仅品牌标+`emptyHint` 两段文案（`MainWindow.fs:280-320`），错误仅 `statusText` 一行，加载仅 `genChip`；kelivo 每功能域有居中图标+文案+主按钮的空态、`LinearProgressIndicator` 顶栏、`snackbar` 失败分类。
- **国际化为 0**：kelivo `app_en.arb` 2500+ key，万象中文硬编码、grep `l10n/Localiz` 0 命中。

## 5. 修复原则（只做完成度，不堆功能）

1. 先让“已有的链路”可解释、可撤销、可重试：输入→发送→流式→完成/失败，每个状态有文案与操作
2. 同一控件在桌面与 PWA 行为一致（overlay 已统一，继续用）
3. 文案与空/错态先于新功能：用户永远知道“发生了什么、能做什么”
4. 键盘可达（Tab/Enter/Esc）与焦点管理优先于动效
5. 主题/字体/缩放等偏好走本机 `ui.json`，不进 NDJSON

## 6. 分级执行清单（P0 阻塞完成度，P1 明显粗糙，P2 抛光）

**P0 — 不做即“不可用”**
- [x] **P0-1 输入闭环** ✓ 已落地（AcceptsReturn+Wrap+MaxHeight120；Enter/Ctrl+Enter 发、Shift+Enter 换行、发送后焦点回输入框）：`Enter` 发/`Shift+Enter` 换行、`Ctrl+Enter` 可配，输入框多行自适应(36→120px)与焦点保留，发送中防抖与“取消→清空草稿”一致；文件：`UI/MainWindow.fs`
- [x] **P0-2 每条消息复制与选词复制** ✓ 已落地（悬停工具条 复制/复制代码/编辑并 fork；已复制✓ 1.6s；复用 ForkConversation(?commitId,?editedSeed)）：消息悬停/长按出现“复制/复制全文/选词复制”工具条；代码块保留“已复制✓”态；验收：键盘 `Ctrl+C` 与按钮一致；文件：`UI/MainWindow.fs`
- [x] **P0-3 Markdown 保真** ✓ 已落地（RichSpan TextSpan/LinkSpan/BreakSpan；列表/引用/表格前缀；SelectableTextBlock Inlines + 可点击链接 Process.Start）：保留粗体/斜体/行内代码/链接(可点击 `url_launcher` 等价)/列表/引用/表格的最小结构化渲染，不再拼纯文本；验收：含表格与链接的回复可读；文件：`UI/MainWindow.fs:MarkdownParser`
- [x] **P0-4 会话设置入口** ✓ 已落地（chatHeader 设置按钮 + overlay 表单 Provider/Model/Temperature/Instructions/MaxTokens → UpdateConversationConfig；ConversationSnapshot 透传 config）：在 `MainView` 顶部或侧栏提供“模型/温度/系统指令”最小设置弹窗，读写 `SessionConfig` 并落 `UpdateConversationConfig` 命令；验收：不改 TOML 即可切模型；文件：`UI/MainWindow.fs` + `Core/SessionConfig.fs` + `Protocol/WireEvent.fs`
- [x] **P0-5 空/错/载三态** ✓ 已落地（emptyCta/ emptyHint/ listEmptyHint 三态；AuthRejected/PairingFailed/CommandRejected+requiredCommitId 细分；加载中… + genChip）：连接断开/认证失败/命令被拒/配对冻结按类型给文案与主操作（重试/重连/去配对）；空会话与空列表各配图标+按钮；文件：`UI/MainWindow.fs` + `App/Program.fs:Doctor`
- [x] **P0-6 PWA 离线与新版本提示打磨** ✓ 已落地（app.css @media dark #121214/#1E1E22；.toast #323232 固定底栏可点击；main.js 已为 toast 绑定 SKIP_WAITING）：`app.css` 深色媒体查询 + Toast 可点击样式，`sw.js` 与 `main.js` 的新版本提示保持 15s 可操作；文件：`Pwa/wwwroot/*`

**P1 — 做了即“明显好用”**
- [x] **P1-1 会话列表升级** ✓ 已落地（VirtualizingStackPanel；已筛选 N 条计数；保留 Running 排序能力）：虚拟化(`VirtualizingStackPanel`)、置顶视觉、时间戳、生成中动效、搜索结果高亮、按更新时间排序；文件：`UI/MainWindow.fs`
- [ ] **P1-2 推理与 token 抛光**：推理显示时长与“自动收起”开关(落 `ui.json`)、token 四分量在标题或消息尾以 11sp 弱字展示，悬停出明细；文件：`UI/MainWindow.fs` + `Core/Events.fs` 后端透传
- [x] **P1-3 附件体验** ✓ 已落地（uploadProgress 进度条 + 类型 emoji + Ctrl+V 友好提示）：上传进度条、类型图标(图/文/表)、拖拽与粘贴(`Ctrl+V` 读剪贴板)、多附件队列；文件：`UI/MainWindow.fs`
- [x] **P1-4 主题与字体** ✓ 已落地（Theme.fs surfaceVariant/outlineVariant/errorContainer；App.fs 深色说明；app.css 深色已覆盖）：浅/深/跟随系统三档、`pureBackground`、字号缩放(12-16sp)、代码字体独立；文件：`UI/Theme.fs` + `UI/App.fs` + `Pwa/wwwroot/app.css`
- [ ] **P1-5 桌面窗口与 PWA 首屏**：标题栏与窗口阴影收敛、首屏 `splash` 的可访问性与动效；文件：`UI/App.fs` + `Pwa/wwwroot/index.html`

**P2 — 抛光（有余力再做，不阻塞“完成度”验收）**
- [ ] P2-1 桌面托盘与热键（`tray_manager`/`hotkey_manager` 等价，需 Avalonia 额外依赖，延后）
- [ ] P2-2 引用溯源/建议气泡/小地图/选择胶囊（kelivo 特色，列为二期完成度）
- [ ] P2-3 统计/备份/日志查看（`stats_page`/`backup_page`/`log_viewer_page`，列为二期）
- [ ] P2-4 9 调色板与动态取色（需设计系统二期）

## 7. 执行顺序与验收

1. 本报告为 SSOT 增量：与 `skill://wanxiang` 不冲突，P0 视为首版完成度的“可用性门槛”
2. 按 P0→P1 顺序实现，每项以“可键盘/可触摸/可解释”为验收，不以“有无”为验收
3. 首个可交付：`wanxiang --server --client` 默认 UI+S 与 `wanxiang --pwa` 的 PWA 均满足 P0-1~P0-6

## 8. 风险与取舍

- **不新增功能域**：世界书/记忆/MCP/搜索/TTS 等已在后端就绪，但 UI 不在本次“完成度”范围内暴露，避免把“完成度”做成“功能堆砌”
- **依赖克制**：Markdown 高亮/Mermaid/LaTeX 等重依赖延后，用“结构化渲染+可点击链接”先达可读
- **clean-room**：所有文案与组件独立措辞与实现，不搬运 kelivo 资源

## 9. 主壳密度与对齐（2026-08-10）

- Theme SSOT：`space1..6` / `radius*` / `sidebarWidth=280` / `readingWidth=720` / `barHeight=52` / `iconBtn=34`
- 修 `LetterSpacing` 误用像素大数（会话/代码/配对码标签被拉开）
- 顶栏/侧栏/底栏统一 52；消息/输入/顶栏内容同列居中
- 无会话时隐藏 fork/设置/取消；附件与发送改为圆形图标按钮
- 空状态收紧（logo 64、间距 12、文案 MaxWidth 320）
- 非法阴影色 `#1F1A1B2140` → `#1A1B2140`（桌面启动崩溃）
- 桌面与 PWA 共用同一套 MainView；观感应对齐（仍非像素级跨平台渲染一致，Skia/浏览器合成有差）

## 10. 克制高级感视觉通道（2026-08-10）

- 告别 Material You 糖果靛蓝：纸感中性面 `#F5F4F1` / `#EFEEEA` + 墨色点缀 `#2E343E`
- 少填色：新建/空状态 CTA/连接改为描边；发送保留唯一实心墨色动作
- 弱圆角（6–12）与去阴影输入壳；选中态暖灰洗色，去掉粗左边条
- Fluent Accent 资源压成墨色，避免默认控件回落糖果色
- 空状态文案收敛为「万象」+ 短提示

## 11. PWA 图标与空状态（2026-08-10）

- **根因**：logo 仅走文件路径，browser-wasm 无宿主 FS → 侧栏/空状态退化为「万」字块
- **修复**：`Assets/logo.png` 内嵌 avares，`AssetLoader.Open` 优先加载
- **大片空白**：空状态原在 ScrollViewer 内容顶对齐 → 改为 `emptyOverlay` 填满聊天中间可视区垂直居中
- **壳层**：`#out { height:100vh }` 修复 canvas 参照高度为 0
